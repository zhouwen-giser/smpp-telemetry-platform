diff --git a/.env.example b/.env.example
index 6f0703f..100af85 100644
--- a/.env.example
+++ b/.env.example
@@ -3,6 +3,7 @@ COMPOSE_PROJECT_NAME=smpp-telemetry
 
 # 对外访问地址。SMPP Runtime 需要把 OTLP 数据推送到此地址。
 TELEMETRY_PUBLIC_HOST=127.0.0.1
+TELEMETRY_BIND_ADDRESS=127.0.0.1
 OTLP_GRPC_PORT=4317
 OTLP_HTTP_PORT=4318
 QUERY_API_PORT=8088
@@ -14,10 +15,17 @@ CLICKHOUSE_NATIVE_PORT=9000
 SMPP_DEPLOYMENT_ID=development
 COLLECTOR_ID=smpp-gateway-1
 TRUST_DOMAIN=local-compose
+SMPP_PROVIDER_ID=isr.vehicle.ugv.ugv1
+SMPP_RUNTIME_INSTANCE_ID=production-ugv-direct-1
 
 # 采集的 SMPP 实例清单。格式：名称|服务地址，多个实例用逗号分隔。
 # 注意：OpenTelemetry 是 SMPP 主动推送模型，此地址用于登记、生成接入配置和运维识别，Collector 不会主动轮询该地址。
-SMPP_SERVICES=http://host.docker.internal:3000
+SMPP_SERVICES='sdar-ugv-runtime|http://192.168.1.7:19100'
+
+# Runtime Prometheus 拉取端点（host:port，不含 scheme/path）
+SMPP_METRICS_TARGET=192.168.1.7:19100
+SMPP_METRICS_PATH=/metrics
+SMPP_METRICS_SCRAPE_INTERVAL=15s
 
 # 默认数据归属
 TELEMETRY_TENANT_ID=tenant-local
@@ -28,3 +36,7 @@ TELEMETRY_ENVIRONMENT=development
 GRAFANA_ADMIN_USER=admin
 # 留空时 deploy.sh 会自动生成并写入 .env
 GRAFANA_ADMIN_PASSWORD=
+
+# 开发模式使用明文 OTLP；qualification 配置会覆盖为双向 TLS。
+TELEMETRY_TRANSPORT_MODE=development
+TELEMETRY_IMAGE_REVISION=development
diff --git a/compose.yaml b/compose.yaml
index eebfb41..e14268e 100644
--- a/compose.yaml
+++ b/compose.yaml
@@ -9,7 +9,7 @@ services:
     volumes:
       - clickhouse-data:/var/lib/clickhouse
       - ./telemetry-schema/migrations:/docker-entrypoint-initdb.d:ro
-    ports: ["${CLICKHOUSE_HTTP_PORT:-8123}:8123", "${CLICKHOUSE_NATIVE_PORT:-9000}:9000"]
+    ports: ["${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${CLICKHOUSE_HTTP_PORT:-8123}:8123", "${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${CLICKHOUSE_NATIVE_PORT:-9000}:9000"]
     healthcheck:
       test:
         - CMD-SHELL
@@ -22,9 +22,11 @@ services:
     build:
       context: .
       dockerfile: telemetry-processor/Dockerfile
+      args:
+        VCS_REF: ${TELEMETRY_IMAGE_REVISION:-development}
     restart: unless-stopped
     depends_on:
-      clickhouse: { condition: service_healthy }
+      telemetry-migrate: { condition: service_completed_successfully }
     environment:
       PROCESSOR_HOST: 0.0.0.0
       PROCESSOR_PORT: 8443
@@ -47,6 +49,22 @@ services:
       timeout: 3s
       retries: 30
 
+  telemetry-migrate:
+    build:
+      context: .
+      dockerfile: telemetry-processor/Dockerfile
+      args:
+        VCS_REF: ${TELEMETRY_IMAGE_REVISION:-development}
+    restart: "no"
+    depends_on:
+      clickhouse: { condition: service_healthy }
+    command: ["node", "dist/telemetry-schema/tools/migrate.js"]
+    environment:
+      CLICKHOUSE_URL: http://clickhouse:8123
+      CLICKHOUSE_USER: default
+      CLICKHOUSE_PASSWORD_FILE: /run/secrets/clickhouse_password
+    secrets: [clickhouse_password]
+
   otel-collector:
     image: otel/opentelemetry-collector-contrib:0.157.0
     restart: unless-stopped
@@ -55,11 +73,16 @@ services:
       SMPP_DEPLOYMENT_ID: ${SMPP_DEPLOYMENT_ID:-development}
       COLLECTOR_ID: ${COLLECTOR_ID:-smpp-gateway-1}
       TRUST_DOMAIN: ${TRUST_DOMAIN:-local-compose}
+      SMPP_PROVIDER_ID: ${SMPP_PROVIDER_ID:-isr.vehicle.ugv.ugv1}
+      SMPP_RUNTIME_INSTANCE_ID: ${SMPP_RUNTIME_INSTANCE_ID:-production-ugv-direct-1}
+      SMPP_METRICS_TARGET: ${SMPP_METRICS_TARGET:-192.168.1.7:19100}
+      SMPP_METRICS_PATH: ${SMPP_METRICS_PATH:-/metrics}
+      SMPP_METRICS_SCRAPE_INTERVAL: ${SMPP_METRICS_SCRAPE_INTERVAL:-15s}
     depends_on:
       telemetry-processor: { condition: service_healthy }
     volumes:
       - ./telemetry-collector/config/gateway.yaml:/etc/otelcol-contrib/config.yaml:ro
-    ports: ["${OTLP_GRPC_PORT:-4317}:4317", "${OTLP_HTTP_PORT:-4318}:4318", "13133:13133", "8888:8888"]
+    ports: ["${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${OTLP_GRPC_PORT:-4317}:4317", "${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${OTLP_HTTP_PORT:-4318}:4318", "127.0.0.1:13133:13133", "127.0.0.1:8888:8888", "127.0.0.1:9464:9464"]
     healthcheck:
       test: ["CMD", "/otelcol-contrib", "validate", "--config=/etc/otelcol-contrib/config.yaml"]
       interval: 10s
@@ -70,9 +93,11 @@ services:
     build:
       context: .
       dockerfile: telemetry-dashboard/query-api/Dockerfile
+      args:
+        VCS_REF: ${TELEMETRY_IMAGE_REVISION:-development}
     restart: unless-stopped
     depends_on:
-      clickhouse: { condition: service_healthy }
+      telemetry-migrate: { condition: service_completed_successfully }
     environment:
       QUERY_API_HOST: 0.0.0.0
       QUERY_API_PORT: 8088
@@ -80,21 +105,27 @@ services:
       CLICKHOUSE_USER: default
       CLICKHOUSE_PASSWORD_FILE: /run/secrets/clickhouse_password
     secrets: [clickhouse_password]
-    ports: ["${QUERY_API_PORT:-8088}:8088"]
+    ports: ["${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${QUERY_API_PORT:-8088}:8088"]
 
   grafana:
     image: grafana/grafana:12.1.0
     restart: unless-stopped
-    depends_on: [clickhouse]
+    depends_on:
+      telemetry-migrate: { condition: service_completed_successfully }
     environment:
       GF_INSTALL_PLUGINS: grafana-clickhouse-datasource
       GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
       GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
+    entrypoint:
+      - /bin/sh
+      - -euc
+      - 'export CLICKHOUSE_PASSWORD="$$(cat /run/secrets/clickhouse_password)"; exec /run.sh'
+    secrets: [clickhouse_password]
     volumes:
       - ./telemetry-dashboard/grafana/provisioning:/etc/grafana/provisioning:ro
       - ./telemetry-dashboard/grafana/dashboards:/var/lib/grafana/dashboards:ro
       - grafana-data:/var/lib/grafana
-    ports: ["${GRAFANA_PORT:-3000}:3000"]
+    ports: ["${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${GRAFANA_PORT:-3000}:3000"]
 
 secrets:
   clickhouse_password:
diff --git a/config/generated/SMPP_RUNTIME_OTEL_CONFIG.md b/config/generated/SMPP_RUNTIME_OTEL_CONFIG.md
new file mode 100644
index 0000000..ca4b1ea
--- /dev/null
+++ b/config/generated/SMPP_RUNTIME_OTEL_CONFIG.md
@@ -0,0 +1,23 @@
+# SMPP Runtime OTLP 配置（自动生成）
+
+生成的精确来源身份：Provider `isr.vehicle.ugv.ugv1`，Runtime 实例 `production-ugv-direct-1`。
+
+OpenTelemetry 是主动推送模型。Prometheus 指标由本平台从 Runtime `/metrics` 主动拉取。
+
+## sdar-ugv-runtime
+SMPP 服务地址：http://192.168.1.7:19100
+
+```env
+OTEL_ENABLED=true
+OTEL_EXPORTER_OTLP_ENDPOINT=https://192.168.1.20:4318
+OTEL_EXPORTER_OTLP_TLS_MODE=required
+OTEL_EXPORTER_OTLP_TIMEOUT_MS=10000
+OTEL_SERVICE_INSTANCE_ID=production-ugv-direct-1
+OTEL_EXPORTER_OTLP_CA_PATH=/run/secrets/otel/ca.pem
+OTEL_EXPORTER_OTLP_CERT_PATH=/run/secrets/otel/tls.crt
+OTEL_EXPORTER_OTLP_KEY_PATH=/run/secrets/otel/tls.key
+# Optional file-backed exporter headers:
+# OTEL_EXPORTER_OTLP_HEADERS_FILE=/run/secrets/otel/headers.json
+```
+
+Prometheus scrape: `192.168.1.7:19100/metrics`, interval `15s`.
diff --git a/config/generated/source-mappings.json b/config/generated/source-mappings.json
new file mode 100644
index 0000000..8282c30
--- /dev/null
+++ b/config/generated/source-mappings.json
@@ -0,0 +1,30 @@
+{
+  "version": 3,
+  "serviceInventory": [
+    {
+      "name": "sdar-ugv-runtime",
+      "address": "http://192.168.1.7:19100"
+    }
+  ],
+  "mappings": [
+    {
+      "collectorId": "smpp-gateway-ugv-1",
+      "trustDomain": "sdar-ugv-runtime-mtls",
+      "deploymentId": "production-ugv-direct",
+      "providerId": "isr.vehicle.ugv.ugv1",
+      "instanceId": "production-ugv-direct-1",
+      "tenantId": "tenant-local",
+      "projectId": "smpp-local",
+      "environment": "qualification",
+      "sourceProduct": "sdar-mcp-provider-platform",
+      "mappingVersion": 3,
+      "policyVersion": 1,
+      "projectionRouteIds": [
+        "standalone-smpp"
+      ],
+      "status": "active",
+      "validFrom": "2026-01-01T00:00:00Z",
+      "validTo": null
+    }
+  ]
+}
diff --git a/config/source-mappings.example.json b/config/source-mappings.example.json
index f1c4521..dafb2c2 100644
--- a/config/source-mappings.example.json
+++ b/config/source-mappings.example.json
@@ -1,17 +1,17 @@
 {
-  "version": 2,
+  "version": 3,
   "mappings": [
     {
       "collectorId": "smpp-gateway-1",
       "trustDomain": "local-compose",
       "deploymentId": "development",
-      "providerId": "*",
-      "instanceId": "*",
+      "providerId": "isr.vehicle.ugv.ugv1",
+      "instanceId": "production-ugv-direct-1",
       "tenantId": "tenant-local",
       "projectId": "smpp-local",
       "environment": "development",
       "sourceProduct": "sdar-mcp-provider-platform",
-      "mappingVersion": 2,
+      "mappingVersion": 3,
       "policyVersion": 1,
       "projectionRouteIds": ["standalone-smpp"],
       "status": "active",
diff --git a/contracts/provider-ops-envelope.schema.json b/contracts/provider-ops-envelope.schema.json
index 711f2d1..dc4c860 100644
--- a/contracts/provider-ops-envelope.schema.json
+++ b/contracts/provider-ops-envelope.schema.json
@@ -24,8 +24,7 @@
       "const": "sdar.provider.ops.event"
     },
     "schemaVersion": {
-      "type": "string",
-      "pattern": "^1\\."
+      "const": "1.1.0"
     },
     "recordId": {
       "type": "string",
@@ -37,21 +36,46 @@
     },
     "recordType": {
       "type": "string",
-      "minLength": 1,
-      "maxLength": 128
+      "enum": [
+        "provider.task.lifecycle",
+        "provider.command.lifecycle",
+        "provider.scheduler.decision",
+        "provider.recovery.lifecycle",
+        "provider.ttl.lifecycle",
+        "provider.resource.state",
+        "provider.resource.metric",
+        "provider.resource.health",
+        "provider.execution.progress",
+        "provider.business_event.source.lifecycle",
+        "provider.business_event.ingest.lifecycle",
+        "provider.business_event.publication.lifecycle",
+        "provider.business_event.stream.lifecycle",
+        "provider.business_event.continuity",
+        "provider.business_event.delivery.lifecycle",
+        "provider.business_event.relation.lifecycle"
+      ]
     },
     "eventCategory": {
       "type": "string",
       "enum": [
-        "audit",
-        "diagnostic"
+        "task.lifecycle",
+        "command.lifecycle",
+        "command.dispatch",
+        "scheduler.decision",
+        "recovery.lifecycle",
+        "ttl.lifecycle",
+        "resource.state",
+        "resource.metric",
+        "resource.health",
+        "execution.progress",
+        "business_event.lifecycle"
       ]
     },
     "deliveryClass": {
       "type": "string",
       "enum": [
-        "durable",
-        "best_effort"
+        "audit",
+        "operational"
       ]
     },
     "providerId": {
@@ -75,7 +99,7 @@
     },
     "resourceId": {
       "type": "string",
-      "maxLength": 256
+      "maxLength": 512
     },
     "resourceType": {
       "type": "string",
@@ -95,11 +119,11 @@
     },
     "traceId": {
       "type": "string",
-      "maxLength": 64
+      "pattern": "^[a-f0-9]{32}$"
     },
     "spanId": {
       "type": "string",
-      "maxLength": 32
+      "pattern": "^[a-f0-9]{16}$"
     },
     "providerEventId": {
       "type": "string",
@@ -155,4 +179,4 @@
     "payload": {}
   },
   "additionalProperties": true
-}
\ No newline at end of file
+}
diff --git a/deploy/bin/generate-config.ts b/deploy/bin/generate-config.ts
index c50454e..d84789e 100644
--- a/deploy/bin/generate-config.ts
+++ b/deploy/bin/generate-config.ts
@@ -2,8 +2,9 @@ import { readFile, writeFile, mkdir } from 'node:fs/promises';
 import path from 'node:path';
 
 const root = path.resolve(process.argv[2] ?? '.');
-const envText = await readFile(path.join(root, '.env'), 'utf8');
-const env = Object.fromEntries(envText.split(/\r?\n/).filter(x => x && !x.startsWith('#') && x.includes('=')).map(line => { const i=line.indexOf('='); return [line.slice(0,i), line.slice(i+1)]; }));
+const envFile = path.resolve(process.argv[3] ?? path.join(root, '.env'));
+const envText = await readFile(envFile, 'utf8');
+const env = Object.fromEntries(envText.split(/\r?\n/).filter(x => x && !x.startsWith('#') && x.includes('=')).map(line => { const i=line.indexOf('='); const raw=line.slice(i+1); const value=(raw.startsWith("'")&&raw.endsWith("'"))||(raw.startsWith('"')&&raw.endsWith('"'))?raw.slice(1,-1):raw; return [line.slice(0,i), value]; }));
 const services = (env.SMPP_SERVICES ?? '').split(',').map(x=>x.trim()).filter(Boolean).map((item,index)=>{
   if(item.includes('|')){const [name,address]=item.split('|'); if(!name||!address) throw new Error(`SMPP_SERVICES 第 ${index+1} 项格式错误`); return {name,address};}
   return {name:`smpp-${index+1}`,address:item};
@@ -13,13 +14,20 @@ for(const s of services){new URL(s.address);}
 const collectorId=env.COLLECTOR_ID || 'smpp-gateway-1';
 const trustDomain=env.TRUST_DOMAIN || 'local-compose';
 const deploymentId=env.SMPP_DEPLOYMENT_ID || 'development';
-const mapping={collectorId,trustDomain,deploymentId,providerId:'*',instanceId:'*',tenantId:env.TELEMETRY_TENANT_ID||'tenant-local',projectId:env.TELEMETRY_PROJECT_ID||'smpp-local',environment:env.TELEMETRY_ENVIRONMENT||'development',sourceProduct:'sdar-mcp-provider-platform',mappingVersion:2,policyVersion:1,projectionRouteIds:['standalone-smpp'],status:'active',validFrom:'2026-01-01T00:00:00Z',validTo:null};
+const providerId=env.SMPP_PROVIDER_ID || '';
+const instanceId=env.SMPP_RUNTIME_INSTANCE_ID || '';
+if(!/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/.test(providerId)||providerId==='*') throw new Error('SMPP_PROVIDER_ID 必须是精确 Provider 标识，禁止通配符');
+if(!/^[A-Za-z0-9][A-Za-z0-9._:-]{0,255}$/.test(instanceId)||instanceId==='*') throw new Error('SMPP_RUNTIME_INSTANCE_ID 必须是精确 Runtime 实例标识，禁止通配符');
+if([collectorId,trustDomain,deploymentId].some(value=>!value||value==='*')) throw new Error('Collector、trust domain 和 deployment 标识必须精确且非通配');
+const mapping={collectorId,trustDomain,deploymentId,providerId,instanceId,tenantId:env.TELEMETRY_TENANT_ID||'tenant-local',projectId:env.TELEMETRY_PROJECT_ID||'smpp-local',environment:env.TELEMETRY_ENVIRONMENT||'development',sourceProduct:'sdar-mcp-provider-platform',mappingVersion:3,policyVersion:1,projectionRouteIds:['standalone-smpp'],status:'active',validFrom:'2026-01-01T00:00:00Z',validTo:null};
 await mkdir(path.join(root,'config','generated'),{recursive:true});
-await writeFile(path.join(root,'config','generated','source-mappings.json'),JSON.stringify({version:2,serviceInventory:services,mappings:[mapping]},null,2)+'\n');
+await writeFile(path.join(root,'config','generated','source-mappings.json'),JSON.stringify({version:3,serviceInventory:services,mappings:[mapping]},null,2)+'\n');
 const host=env.TELEMETRY_PUBLIC_HOST||'127.0.0.1';
 const port=env.OTLP_HTTP_PORT||'4318';
-const lines=['# SMPP Runtime 遥测接入配置（自动生成）','','OpenTelemetry 是主动推送模型。请在下列 SMPP 服务中把 OTLP Endpoint 指向本平台：',''];
-for(const s of services){lines.push(`## ${s.name}`,`SMPP 服务地址：${s.address}`,'','```env','OTEL_ENABLED=true',`OTEL_EXPORTER_OTLP_ENDPOINT=http://${host}:${port}`,'OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf','```','');}
-lines.push('> 服务地址仅用于资产登记和生成接入清单，Collector 不会轮询 SMPP HTTP 服务。');
+const qualification=env.TELEMETRY_TRANSPORT_MODE==='qualification';
+const scheme=qualification?'https':'http';
+const lines=['# SMPP Runtime OTLP 配置（自动生成）','',`生成的精确来源身份：Provider \`${providerId}\`，Runtime 实例 \`${instanceId}\`。`,'','OpenTelemetry 是主动推送模型。Prometheus 指标由本平台从 Runtime `/metrics` 主动拉取。',''];
+for(const s of services){lines.push(`## ${s.name}`,`SMPP 服务地址：${s.address}`,'','```env','OTEL_ENABLED=true',`OTEL_EXPORTER_OTLP_ENDPOINT=${scheme}://${host}:${port}`,`OTEL_EXPORTER_OTLP_TLS_MODE=${qualification?'required':'disabled'}`,'OTEL_EXPORTER_OTLP_TIMEOUT_MS=10000',`OTEL_SERVICE_INSTANCE_ID=${instanceId}`,...(qualification?['OTEL_EXPORTER_OTLP_CA_PATH=/run/secrets/otel/ca.pem','OTEL_EXPORTER_OTLP_CERT_PATH=/run/secrets/otel/tls.crt','OTEL_EXPORTER_OTLP_KEY_PATH=/run/secrets/otel/tls.key','# Optional file-backed exporter headers:','# OTEL_EXPORTER_OTLP_HEADERS_FILE=/run/secrets/otel/headers.json']:[]),'```','');}
+lines.push(`Prometheus scrape: \`${env.SMPP_METRICS_TARGET||'192.168.1.7:19100'}${env.SMPP_METRICS_PATH||'/metrics'}\`, interval \`${env.SMPP_METRICS_SCRAPE_INTERVAL||'15s'}\`.`);
 await writeFile(path.join(root,'config','generated','SMPP_RUNTIME_OTEL_CONFIG.md'),lines.join('\n')+'\n');
 console.log(`已登记 ${services.length} 个 SMPP 服务并生成接入配置。`);
diff --git a/deploy/compose/compose.yaml b/deploy/compose/compose.yaml
index eebfb41..fc2af3e 100644
--- a/deploy/compose/compose.yaml
+++ b/deploy/compose/compose.yaml
@@ -9,7 +9,7 @@ services:
     volumes:
       - clickhouse-data:/var/lib/clickhouse
       - ./telemetry-schema/migrations:/docker-entrypoint-initdb.d:ro
-    ports: ["${CLICKHOUSE_HTTP_PORT:-8123}:8123", "${CLICKHOUSE_NATIVE_PORT:-9000}:9000"]
+    ports: ["${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${CLICKHOUSE_HTTP_PORT:-8123}:8123", "${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${CLICKHOUSE_NATIVE_PORT:-9000}:9000"]
     healthcheck:
       test:
         - CMD-SHELL
@@ -22,6 +22,8 @@ services:
     build:
       context: .
       dockerfile: telemetry-processor/Dockerfile
+      args:
+        VCS_REF: ${TELEMETRY_IMAGE_REVISION:-development}
     restart: unless-stopped
     depends_on:
       clickhouse: { condition: service_healthy }
@@ -55,11 +57,16 @@ services:
       SMPP_DEPLOYMENT_ID: ${SMPP_DEPLOYMENT_ID:-development}
       COLLECTOR_ID: ${COLLECTOR_ID:-smpp-gateway-1}
       TRUST_DOMAIN: ${TRUST_DOMAIN:-local-compose}
+      SMPP_PROVIDER_ID: ${SMPP_PROVIDER_ID:-isr.vehicle.ugv.ugv1}
+      SMPP_RUNTIME_INSTANCE_ID: ${SMPP_RUNTIME_INSTANCE_ID:-production-ugv-direct-1}
+      SMPP_METRICS_TARGET: ${SMPP_METRICS_TARGET:-192.168.1.7:19100}
+      SMPP_METRICS_PATH: ${SMPP_METRICS_PATH:-/metrics}
+      SMPP_METRICS_SCRAPE_INTERVAL: ${SMPP_METRICS_SCRAPE_INTERVAL:-15s}
     depends_on:
       telemetry-processor: { condition: service_healthy }
     volumes:
       - ./telemetry-collector/config/gateway.yaml:/etc/otelcol-contrib/config.yaml:ro
-    ports: ["${OTLP_GRPC_PORT:-4317}:4317", "${OTLP_HTTP_PORT:-4318}:4318", "13133:13133", "8888:8888"]
+    ports: ["${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${OTLP_GRPC_PORT:-4317}:4317", "${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${OTLP_HTTP_PORT:-4318}:4318", "127.0.0.1:13133:13133", "127.0.0.1:8888:8888", "127.0.0.1:9464:9464"]
     healthcheck:
       test: ["CMD", "/otelcol-contrib", "validate", "--config=/etc/otelcol-contrib/config.yaml"]
       interval: 10s
@@ -70,6 +77,8 @@ services:
     build:
       context: .
       dockerfile: telemetry-dashboard/query-api/Dockerfile
+      args:
+        VCS_REF: ${TELEMETRY_IMAGE_REVISION:-development}
     restart: unless-stopped
     depends_on:
       clickhouse: { condition: service_healthy }
@@ -80,7 +89,7 @@ services:
       CLICKHOUSE_USER: default
       CLICKHOUSE_PASSWORD_FILE: /run/secrets/clickhouse_password
     secrets: [clickhouse_password]
-    ports: ["${QUERY_API_PORT:-8088}:8088"]
+    ports: ["${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${QUERY_API_PORT:-8088}:8088"]
 
   grafana:
     image: grafana/grafana:12.1.0
@@ -90,11 +99,16 @@ services:
       GF_INSTALL_PLUGINS: grafana-clickhouse-datasource
       GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
       GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
+    entrypoint:
+      - /bin/sh
+      - -euc
+      - 'export CLICKHOUSE_PASSWORD="$$(cat /run/secrets/clickhouse_password)"; exec /run.sh'
+    secrets: [clickhouse_password]
     volumes:
       - ./telemetry-dashboard/grafana/provisioning:/etc/grafana/provisioning:ro
       - ./telemetry-dashboard/grafana/dashboards:/var/lib/grafana/dashboards:ro
       - grafana-data:/var/lib/grafana
-    ports: ["${GRAFANA_PORT:-3000}:3000"]
+    ports: ["${TELEMETRY_BIND_ADDRESS:-127.0.0.1}:${GRAFANA_PORT:-3000}:3000"]
 
 secrets:
   clickhouse_password:
diff --git a/deploy/stable-smpp-integration/.env.example b/deploy/stable-smpp-integration/.env.example
new file mode 100644
index 0000000..82f90af
--- /dev/null
+++ b/deploy/stable-smpp-integration/.env.example
@@ -0,0 +1,26 @@
+COMPOSE_PROJECT_NAME=smpp-telemetry-goal01-qualification
+TELEMETRY_TRANSPORT_MODE=qualification
+TELEMETRY_PUBLIC_HOST=192.168.1.20
+TELEMETRY_BIND_ADDRESS=192.168.1.20
+TELEMETRY_IMAGE_REVISION=REPLACE_WITH_GOAL_HEAD_SHA
+OTLP_GRPC_PORT=4317
+OTLP_HTTP_PORT=4318
+QUERY_API_PORT=8088
+SMPP_DEPLOYMENT_ID=production-ugv-direct
+COLLECTOR_ID=smpp-gateway-ugv-1
+TRUST_DOMAIN=sdar-ugv-runtime-mtls
+SMPP_PROVIDER_ID=isr.vehicle.ugv.ugv1
+SMPP_RUNTIME_INSTANCE_ID=production-ugv-direct-1
+SMPP_SERVICES='sdar-ugv-runtime|http://192.168.1.7:19100'
+SMPP_METRICS_TARGET=192.168.1.7:19100
+SMPP_METRICS_PATH=/metrics
+SMPP_METRICS_SCRAPE_INTERVAL=15s
+TELEMETRY_TENANT_ID=tenant-local
+TELEMETRY_PROJECT_ID=smpp-local
+TELEMETRY_ENVIRONMENT=qualification
+GRAFANA_ADMIN_USER=admin
+GRAFANA_ADMIN_PASSWORD=REPLACE_WITH_SECRET
+QUALIFICATION_SECRET_DIR=/absolute/path/to/qualification/secrets
+TELEMETRY_SECRET_UID=1000
+TELEMETRY_SECRET_GID=1000
+ALLOW_SMPP_TELEMETRY_SIDE_EFFECT_TESTS=false
diff --git a/deploy/stable-smpp-integration/README.md b/deploy/stable-smpp-integration/README.md
new file mode 100644
index 0000000..8d643bc
--- /dev/null
+++ b/deploy/stable-smpp-integration/README.md
@@ -0,0 +1,18 @@
+# Stable SMPP integration deployment
+
+This profile keeps SMPP external and runs ClickHouse, Processor, Collector, Query API, and Grafana.
+The root Compose file is the development/plaintext mode. This override is the qualification and
+production transport mode: Runtime→Collector and Collector→Processor both require mutual TLS.
+
+Copy `.env.example` to `.env`, set exact identities and an absolute `QUALIFICATION_SECRET_DIR`, then
+run `./preflight.sh`, `./up.sh`, `./smoke.sh`, or the restart-inclusive `./qualify.sh`.
+`TELEMETRY_BIND_ADDRESS` must be the exact loopback or private interface reachable by the Runtime;
+the preflight rejects wildcard host binding so OTLP is not exposed on every host interface.
+
+Use a qualification-specific `COMPOSE_PROJECT_NAME`. Compose will then create isolated ClickHouse,
+WAL, and Grafana volumes without touching operator production volumes. `down.sh` deliberately keeps
+those volumes; an operator may remove only that exact project with an explicit Compose `down -v`.
+
+The live smoke is read-only: it probes health, Query API authentication, and the supplied Runtime
+Prometheus endpoint. `ALLOW_SMPP_TELEMETRY_SIDE_EFFECT_TESTS` must remain `false`; this lifecycle
+does not invoke movement, recon, gimbal, fire, or any Runtime operation.
diff --git a/deploy/stable-smpp-integration/compose.override.yaml b/deploy/stable-smpp-integration/compose.override.yaml
new file mode 100644
index 0000000..abc80f4
--- /dev/null
+++ b/deploy/stable-smpp-integration/compose.override.yaml
@@ -0,0 +1,66 @@
+services:
+  clickhouse:
+    ports: !reset []
+  telemetry-processor:
+    environment:
+      PROCESSOR_TLS_MODE: required
+      PROCESSOR_TLS_CA_FILE: /run/secrets/collector_client_ca
+      PROCESSOR_TLS_CERT_FILE: /run/secrets/processor_server_cert
+      PROCESSOR_TLS_KEY_FILE: /run/secrets/processor_server_key
+    secrets:
+      - clickhouse_password
+      - processor_admin_key
+      - collector_client_ca
+      - processor_server_cert
+      - processor_server_key
+      - processor_server_ca
+      - collector_client_cert
+      - collector_client_key
+    healthcheck:
+      test:
+        - CMD
+        - node
+        - -e
+        - "const https=require('https'),fs=require('fs');const request=https.get({hostname:'telemetry-processor',port:8443,path:'/health/live',servername:'telemetry-processor',ca:fs.readFileSync('/run/secrets/processor_server_ca'),cert:fs.readFileSync('/run/secrets/collector_client_cert'),key:fs.readFileSync('/run/secrets/collector_client_key')},response=>process.exit(response.statusCode===200?0:1));request.on('error',()=>process.exit(1));"
+  otel-collector:
+    user: "${TELEMETRY_SECRET_UID:-1000}:${TELEMETRY_SECRET_GID:-1000}"
+    command: ["--config=/etc/otelcol-contrib/config.yaml"]
+    volumes:
+      - ./telemetry-collector/config/gateway-mtls.yaml:/etc/otelcol-contrib/config.yaml:ro
+    secrets:
+      - runtime_client_ca
+      - collector_server_cert
+      - collector_server_key
+      - processor_server_ca
+      - collector_client_cert
+      - collector_client_key
+  query-api:
+    environment:
+      QUERY_API_KEY_FILE: /run/secrets/query_api_key
+    secrets:
+      - clickhouse_password
+      - query_api_key
+  grafana:
+    ports: !reset []
+
+secrets:
+  runtime_client_ca:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/runtime-client-ca.pem
+  collector_server_cert:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/collector-server.pem
+  collector_server_key:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/collector-server-key.pem
+  processor_server_ca:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/processor-server-ca.pem
+  collector_client_cert:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/collector-client.pem
+  collector_client_key:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/collector-client-key.pem
+  collector_client_ca:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/collector-client-ca.pem
+  processor_server_cert:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/processor-server.pem
+  processor_server_key:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/processor-server-key.pem
+  query_api_key:
+    file: ${QUALIFICATION_SECRET_DIR:?QUALIFICATION_SECRET_DIR is required}/query-api-key.txt
diff --git a/deploy/stable-smpp-integration/down.sh b/deploy/stable-smpp-integration/down.sh
new file mode 100755
index 0000000..5c50753
--- /dev/null
+++ b/deploy/stable-smpp-integration/down.sh
@@ -0,0 +1,6 @@
+#!/usr/bin/env sh
+set -eu
+ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
+cd "$ROOT"
+ENV_FILE=${1:-deploy/stable-smpp-integration/.env}
+docker compose --env-file "$ENV_FILE" -f compose.yaml -f deploy/stable-smpp-integration/compose.override.yaml down --remove-orphans
diff --git a/deploy/stable-smpp-integration/preflight.sh b/deploy/stable-smpp-integration/preflight.sh
new file mode 100755
index 0000000..00dce73
--- /dev/null
+++ b/deploy/stable-smpp-integration/preflight.sh
@@ -0,0 +1,22 @@
+#!/usr/bin/env sh
+set -eu
+ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
+cd "$ROOT"
+ENV_FILE=${1:-deploy/stable-smpp-integration/.env}
+[ -f "$ENV_FILE" ] || { echo "missing qualification env: $ENV_FILE" >&2; exit 1; }
+set -a
+. "$ENV_FILE"
+set +a
+[ "${ALLOW_SMPP_TELEMETRY_SIDE_EFFECT_TESTS:-false}" = false ] || { echo 'side-effect qualification must remain false' >&2; exit 1; }
+case "${TELEMETRY_BIND_ADDRESS:-}" in ''|0.0.0.0|::|'[::]') echo 'TELEMETRY_BIND_ADDRESS must be an exact loopback or private interface address' >&2; exit 1;; esac
+for value in "${COLLECTOR_ID:-}" "${TRUST_DOMAIN:-}" "${SMPP_DEPLOYMENT_ID:-}" "${SMPP_PROVIDER_ID:-}" "${SMPP_RUNTIME_INSTANCE_ID:-}"; do
+  [ -n "$value" ] && [ "$value" != '*' ] || { echo 'trusted identities must be exact and non-empty' >&2; exit 1; }
+done
+[ -n "${QUALIFICATION_SECRET_DIR:-}" ] && [ "${QUALIFICATION_SECRET_DIR#/}" != "$QUALIFICATION_SECRET_DIR" ] || { echo 'QUALIFICATION_SECRET_DIR must be absolute' >&2; exit 1; }
+[ -d "$QUALIFICATION_SECRET_DIR" ] || { echo 'qualification secret directory missing' >&2; exit 1; }
+for file in runtime-client-ca.pem collector-server.pem collector-server-key.pem processor-server-ca.pem collector-client.pem collector-client-key.pem collector-client-ca.pem processor-server.pem processor-server-key.pem query-api-key.txt; do
+  [ -s "$QUALIFICATION_SECRET_DIR/$file" ] || { echo "missing secret file: $file" >&2; exit 1; }
+done
+curl --fail --silent --show-error --max-time 10 "http://${SMPP_METRICS_TARGET}${SMPP_METRICS_PATH}" >/dev/null
+docker compose --env-file "$ENV_FILE" -f compose.yaml -f deploy/stable-smpp-integration/compose.override.yaml config --quiet
+echo 'stable SMPP qualification preflight: PASS'
diff --git a/deploy/stable-smpp-integration/qualify.sh b/deploy/stable-smpp-integration/qualify.sh
new file mode 100755
index 0000000..67cd43f
--- /dev/null
+++ b/deploy/stable-smpp-integration/qualify.sh
@@ -0,0 +1,12 @@
+#!/usr/bin/env sh
+set -eu
+ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
+cd "$ROOT"
+ENV_FILE=${1:-deploy/stable-smpp-integration/.env}
+deploy/stable-smpp-integration/up.sh "$ENV_FILE"
+deploy/stable-smpp-integration/smoke.sh "$ENV_FILE"
+docker compose --env-file "$ENV_FILE" -f compose.yaml -f deploy/stable-smpp-integration/compose.override.yaml restart telemetry-processor
+deploy/stable-smpp-integration/smoke.sh "$ENV_FILE"
+docker compose --env-file "$ENV_FILE" -f compose.yaml -f deploy/stable-smpp-integration/compose.override.yaml restart otel-collector
+deploy/stable-smpp-integration/smoke.sh "$ENV_FILE"
+echo 'stable SMPP deployment qualification: PASS'
diff --git a/deploy/stable-smpp-integration/secrets/README.md b/deploy/stable-smpp-integration/secrets/README.md
new file mode 100644
index 0000000..4bac9db
--- /dev/null
+++ b/deploy/stable-smpp-integration/secrets/README.md
@@ -0,0 +1,21 @@
+# Qualification secrets
+
+Do not place secrets in this repository. Set `QUALIFICATION_SECRET_DIR` to an absolute directory
+owned by the operator with mode `0700`; private keys and tokens should be mode `0600`.
+Set `TELEMETRY_SECRET_UID`/`TELEMETRY_SECRET_GID` to that owner so the non-root Collector can read
+Compose file-backed secrets without making private keys world-readable.
+
+Required files:
+
+- `runtime-client-ca.pem`: CA dedicated to the authorized SMPP Runtime client certificate.
+- `collector-server.pem` / `collector-server-key.pem`: Collector server identity. The certificate
+  must cover the hostname or IP configured as the Runtime OTLP endpoint.
+- `processor-server-ca.pem`, `processor-server.pem`, `processor-server-key.pem`: Processor server
+  CA and identity; the certificate must contain DNS SAN `telemetry-processor`.
+- `collector-client-ca.pem`, `collector-client.pem`, `collector-client-key.pem`: Collector client
+  CA and identity accepted by Processor.
+- `query-api-key.txt`: Query API bearer secret.
+
+Runtime itself receives a client certificate issued by `runtime-client-ca.pem`; bind that CA to this
+one Runtime deployment only. Never use `insecure_skip_verify`, a shared public CA, or a wildcard
+source mapping for qualification.
diff --git a/deploy/stable-smpp-integration/smoke.sh b/deploy/stable-smpp-integration/smoke.sh
new file mode 100755
index 0000000..a341d02
--- /dev/null
+++ b/deploy/stable-smpp-integration/smoke.sh
@@ -0,0 +1,25 @@
+#!/usr/bin/env sh
+set -eu
+ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
+cd "$ROOT"
+ENV_FILE=${1:-deploy/stable-smpp-integration/.env}
+set -a
+. "$ENV_FILE"
+set +a
+retry() {
+  attempts=0
+  until "$@"; do
+    attempts=$((attempts + 1))
+    [ "$attempts" -lt 30 ] || return 1
+    sleep 2
+  done
+}
+collector_metrics_ready() {
+  curl --fail --silent --show-error --max-time 10 http://127.0.0.1:9464/metrics | grep 'sdar_runtime_info' >/dev/null
+}
+retry curl --fail --silent --show-error --max-time 10 http://127.0.0.1:13133/ >/dev/null
+retry collector_metrics_ready
+QUERY_API_KEY=$(tr -d '\r\n' < "$QUALIFICATION_SECRET_DIR/query-api-key.txt")
+retry curl --fail --silent --show-error --max-time 10 -H "Authorization: Bearer $QUERY_API_KEY" "http://127.0.0.1:${QUERY_API_PORT:-8088}/health" >/dev/null
+retry curl --fail --silent --show-error --max-time 10 "http://${SMPP_METRICS_TARGET}${SMPP_METRICS_PATH}" >/dev/null
+echo 'stable SMPP read-only smoke: PASS'
diff --git a/deploy/stable-smpp-integration/up.sh b/deploy/stable-smpp-integration/up.sh
new file mode 100755
index 0000000..b09b6fb
--- /dev/null
+++ b/deploy/stable-smpp-integration/up.sh
@@ -0,0 +1,11 @@
+#!/usr/bin/env sh
+set -eu
+ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
+cd "$ROOT"
+ENV_FILE=${1:-deploy/stable-smpp-integration/.env}
+deploy/stable-smpp-integration/preflight.sh "$ENV_FILE"
+set -a
+. "$ENV_FILE"
+set +a
+node deploy/bin/generate-config.ts "$ROOT" "$ENV_FILE"
+docker compose --env-file "$ENV_FILE" -f compose.yaml -f deploy/stable-smpp-integration/compose.override.yaml up -d --build --remove-orphans --wait --wait-timeout 180
diff --git a/package.json b/package.json
index 33f74fc..53f8d5f 100644
--- a/package.json
+++ b/package.json
@@ -21,12 +21,14 @@
     "start:query-api": "node dist/telemetry-dashboard/query-api/src/index.js",
     "migrate": "node dist/telemetry-schema/tools/migrate.js",
     "send:sample": "node dist/tools/send-sample.js",
+    "render:source-vector": "node dist/tools/render-source-vector-otlp.js",
     "backfill:v01": "node dist/telemetry-schema/tools/backfill-v01.js",
     "test": "npm run build && node --test dist/telemetry-processor/test/*.test.js dist/telemetry-collector/tests/*.test.js dist/telemetry-dashboard/query-api/test/*.test.js dist/telemetry-schema/tools/*.test.js",
     "check": "npm run test",
     "manifest": "npm run build && node dist/tools/create-manifest.js"
   },
   "devDependencies": {
+    "@types/node": "22.20.1",
     "typescript": "5.8.3"
   },
   "license": "Apache-2.0"
diff --git a/packages/telemetry-contracts/src/index.ts b/packages/telemetry-contracts/src/index.ts
index 458b296..75e9067 100644
--- a/packages/telemetry-contracts/src/index.ts
+++ b/packages/telemetry-contracts/src/index.ts
@@ -1,3 +1,4 @@
+export const PROVIDER_OPS_SCHEMA_NAME = 'sdar.provider.ops.event' as const;
 export const PROVIDER_OPS_SCHEMA_VERSION = '1.1.0' as const;
 export const CANONICAL_FACT_VERSION = '1.0.0' as const;
 export const ENTITY_RELATION_FACT_VERSION = '1.0.0' as const;
diff --git a/packages/telemetry-types/src/index.ts b/packages/telemetry-types/src/index.ts
index 2f57746..0ad0493 100644
--- a/packages/telemetry-types/src/index.ts
+++ b/packages/telemetry-types/src/index.ts
@@ -6,17 +6,50 @@ export interface OtlpLogRecord {
 }
 
 export interface ProviderOpsEnvelope {
+  schemaName: 'sdar.provider.ops.event';
   schemaVersion: '1.1.0';
   recordId: string;
   recordHash: string;
   recordType: string;
+  eventCategory: string;
+  deliveryClass: 'audit' | 'operational';
   occurredAt: string;
+  emittedAt: string;
   providerId: string;
+  runtimeVersion: string;
   instanceId: string;
-  payload?: Record<string, unknown>;
+  taskId?: string;
+  resourceId?: string;
+  resourceType?: string;
+  externalExecutionId?: string;
+  operationName?: string;
+  correlationId?: string;
+  traceId?: string;
+  spanId?: string;
+  providerEventId?: string;
+  providerEventSequence?: number;
+  eventType?: string;
+  executionMode?: string;
+  simulationId?: string;
+  argumentHash?: string;
+  authorizationContextHash?: string;
+  adapterRevision?: string | number;
+  observationRevision?: number;
+  commandSequence?: number;
+  attributes: Record<string, unknown>;
+  payload: unknown;
   [key: string]: unknown;
 }
 
+export type ProviderOpsDeliveryClass = 'audit' | 'operational';
+
+export interface ProviderOpsSourceIdentity {
+  providerId: string;
+  runtimeVersion: string;
+  runtimeInstanceId: string;
+  deploymentId: string;
+}
+
 export interface CollectResult {
   status: 'accepted' | 'duplicate' | 'conflict' | 'rejected_retryable' | 'rejected_permanent';
   recordId?: string;
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
new file mode 100644
index 0000000..4a3638a
--- /dev/null
+++ b/pnpm-lock.yaml
@@ -0,0 +1,47 @@
+lockfileVersion: '9.0'
+
+settings:
+  autoInstallPeers: true
+  excludeLinksFromLockfile: false
+
+importers:
+
+  .:
+    devDependencies:
+      '@types/node':
+        specifier: 22.20.1
+        version: 22.20.1
+      typescript:
+        specifier: 5.8.3
+        version: 5.8.3
+
+  packages/telemetry-contracts: {}
+
+  packages/telemetry-types: {}
+
+  telemetry-dashboard/query-api: {}
+
+  telemetry-processor: {}
+
+packages:
+
+  '@types/node@22.20.1':
+    resolution: {integrity: sha512-EANqOCF9QFyra+4pfxUcX9STKJpCLjMbObVzljIJomAWSnuSIEAvyzEU53GaajbXJEgdh0iEcPL+DGvpUd4k1Q==}
+
+  typescript@5.8.3:
+    resolution: {integrity: sha512-p1diW6TqL9L07nNxvRMM7hMMw4c5XOo/1ibL4aAIGmSAt9slTE1Xgw5KWuof2uTOvCg9BY7ZRi+GaF+7sfgPeQ==}
+    engines: {node: '>=14.17'}
+    hasBin: true
+
+  undici-types@6.21.0:
+    resolution: {integrity: sha512-iwDZqg0QAGrg9Rav5H4n0M64c3mkR59cJ6wQp+7C4nI0gsmExaedaYLNO44eT4AtBBwjbTiGPMlt2Md0T9H9JQ==}
+
+snapshots:
+
+  '@types/node@22.20.1':
+    dependencies:
+      undici-types: 6.21.0
+
+  typescript@5.8.3: {}
+
+  undici-types@6.21.0: {}
diff --git a/reports/smpp-stable-integration/BASELINE.json b/reports/smpp-stable-integration/BASELINE.json
new file mode 100644
index 0000000..0da1434
--- /dev/null
+++ b/reports/smpp-stable-integration/BASELINE.json
@@ -0,0 +1,27 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T10:29:42.675Z",
+  "telemetry": {
+    "repository": "zhouwen-giser/smpp-telemetry-platform",
+    "reviewedSha": "53a799d4c0166669411e61b816c6ed8ef63cc70f",
+    "executionOriginMainSha": "53a799d4c0166669411e61b816c6ed8ef63cc70f",
+    "baseSha": "53a799d4c0166669411e61b816c6ed8ef63cc70f",
+    "branch": "main",
+    "branchPolicy": "user-overridden: work on the existing local branch; no isolated telemetry branch",
+    "driftFromReviewedBaseline": false
+  },
+  "smpp": {
+    "repository": "zhouwen-giser/sdar-mcp-provider-platform",
+    "reviewedSha": "cce7ea0cb0fe1b13328a48189a76d9b1688f49f4",
+    "executionOriginMainSha": "cce7ea0cb0fe1b13328a48189a76d9b1688f49f4",
+    "auditSha": "678521e35c83793a59e045ef9bf59b4df842962e",
+    "auditBranch": "codex/goal-11-npc-tank-simulation-real-interface",
+    "sourceAuthorityPolicy": "user-overridden: current local SMPP branch is primary; origin/main remains the stable comparison baseline",
+    "driftFromReviewedBaseline": true,
+    "mutationAllowed": false,
+    "localWorkingBranchLeftUnchanged": "codex/goal-11-npc-tank-simulation-real-interface"
+  },
+  "integrationBoundary": "SMPP Runtime OTLP -> OpenTelemetry Collector",
+  "providedReadOnlyMetricsEndpoint": "http://192.168.1.7:19100/metrics",
+  "baselineGate": "PASS"
+}
diff --git a/reports/smpp-stable-integration/COLLECTOR_CONFORMANCE.json b/reports/smpp-stable-integration/COLLECTOR_CONFORMANCE.json
new file mode 100644
index 0000000..9497853
--- /dev/null
+++ b/reports/smpp-stable-integration/COLLECTOR_CONFORMANCE.json
@@ -0,0 +1,35 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T11:32:00.000Z",
+  "collector": {
+    "image": "otel/opentelemetry-collector-contrib:0.157.0",
+    "digest": "sha256:f2f01157055a9b2aab9df7118e1f1c9abf345e99b23bc7a2bc791db374a7d0f6",
+    "developmentConfigValidated": true,
+    "qualificationMtlsConfigValidated": true,
+    "providerOpsSelection": [
+      "sdar.schema.name",
+      "sdar.schema.version",
+      "sdar.record.id",
+      "sdar.record.hash",
+      "body.schemaName"
+    ],
+    "nonProviderOpsExcluded": true,
+    "strictProcessorAck": true,
+    "prometheusReceiverConfigured": true
+  },
+  "realRuntimeMetrics": {
+    "target": "192.168.1.7:19100",
+    "path": "/metrics",
+    "up": 1,
+    "samplesScraped": 43,
+    "runtimeVersion": "2.0.0-rc.1"
+  },
+  "providerOpsQualification": {
+    "sourceDerivedVectorReachedProcessor": true,
+    "unselectedLogDurablyStored": false,
+    "realExternalRuntimeProviderOpsObserved": false,
+    "externalRuntimeConfigurationChanged": false
+  },
+  "status": "COLLECTOR_READY",
+  "liveStatus": "PARTIAL_REAL_METRICS_ONLY"
+}
diff --git a/reports/smpp-stable-integration/COMPOSE_EVIDENCE.json b/reports/smpp-stable-integration/COMPOSE_EVIDENCE.json
new file mode 100644
index 0000000..644a8b5
--- /dev/null
+++ b/reports/smpp-stable-integration/COMPOSE_EVIDENCE.json
@@ -0,0 +1,38 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T11:32:00.000Z",
+  "project": "smpp-telemetry-goal01-qualification",
+  "telemetrySha": "5ff2a4e5b2fd8066fdbb22f98bb9c68b0788a926",
+  "services": {
+    "clickhouse": "healthy",
+    "telemetryMigrate": "exited_0",
+    "telemetryProcessor": "healthy",
+    "otelCollector": "healthy",
+    "queryApi": "running_and_smoke_passed",
+    "grafana": "running"
+  },
+  "images": {
+    "processor": "sha256:e90e9b04b41c6acc18e4ce2af8bdc1c649ef9e3794b01e62d65d7a5462c5f0c6",
+    "queryApi": "sha256:949d33852745c7017072ec2b53c2cd889ba81e6e76bf09e449b17541b8266137",
+    "collector": "otel/opentelemetry-collector-contrib@sha256:f2f01157055a9b2aab9df7118e1f1c9abf345e99b23bc7a2bc791db374a7d0f6"
+  },
+  "ociRevision": {
+    "processor": "5ff2a4e5b2fd8066fdbb22f98bb9c68b0788a926",
+    "queryApi": "5ff2a4e5b2fd8066fdbb22f98bb9c68b0788a926"
+  },
+  "publishedPorts": {
+    "otlpGrpc": "127.0.0.1:14317",
+    "otlpHttp": "127.0.0.1:14318",
+    "collectorHealth": "127.0.0.1:13133",
+    "runtimeMetricsProjection": "127.0.0.1:9464",
+    "queryApi": "127.0.0.1:18088"
+  },
+  "qualification": {
+    "preflight": "PASS",
+    "initialSmoke": "PASS",
+    "processorRestartSmoke": "PASS",
+    "collectorRestartSmoke": "PASS",
+    "sideEffectsEnabled": false
+  },
+  "status": "DEPLOYMENT_READY"
+}
diff --git a/reports/smpp-stable-integration/CONTRACT_CONFORMANCE.json b/reports/smpp-stable-integration/CONTRACT_CONFORMANCE.json
new file mode 100644
index 0000000..1a602ee
--- /dev/null
+++ b/reports/smpp-stable-integration/CONTRACT_CONFORMANCE.json
@@ -0,0 +1,32 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T11:32:00.000Z",
+  "telemetrySha": "5ff2a4e5b2fd8066fdbb22f98bb9c68b0788a926",
+  "smppSourceSha": "678521e35c83793a59e045ef9bf59b4df842962e",
+  "contract": {
+    "schemaName": "sdar.provider.ops.event",
+    "schemaVersion": "1.1.0",
+    "recordTypeCount": 16,
+    "eventCategoryCount": 11,
+    "deliveryClasses": ["audit", "operational"],
+    "allowlistsEqualSourceCapture": true,
+    "schemaCopiesByteIdentical": true,
+    "schemaSha256": "d8266956622b7c6eb1e970c0f8979fe58a7709af8619d94448f97d49f1f259f7"
+  },
+  "sourceVector": {
+    "recordId": "6eb4e972-be6e-5708-98ef-95dd5f263b96",
+    "expectedRecordHash": "57afebfcb2fcd7b2eb7a7ea2b79f7348d57b279ebf06cb87fe0a03621440dfee",
+    "platformRecomputedRecordHash": "57afebfcb2fcd7b2eb7a7ea2b79f7348d57b279ebf06cb87fe0a03621440dfee",
+    "hashParity": true,
+    "accepted": true
+  },
+  "failClosedChecks": {
+    "unsupportedSchemaVersionRejected": true,
+    "recordHashMismatchRejected": true,
+    "otlpBodyAttributeMismatchRejected": true,
+    "missingContractAttributeRejected": true,
+    "unsupportedAdapterTraceNameExcludedFromProviderOps": true
+  },
+  "sourceDefect": null,
+  "status": "SOURCE_CONTRACT_READY"
+}
diff --git a/reports/smpp-stable-integration/CONTRACT_MATRIX.json b/reports/smpp-stable-integration/CONTRACT_MATRIX.json
new file mode 100644
index 0000000..917aabb
--- /dev/null
+++ b/reports/smpp-stable-integration/CONTRACT_MATRIX.json
@@ -0,0 +1,120 @@
+{
+  "schemaVersion": "1.0",
+  "createdAt": "2026-08-12T10:36:25.366Z",
+  "sourceSha": "678521e35c83793a59e045ef9bf59b4df842962e",
+  "telemetryBaseSha": "53a799d4c0166669411e61b816c6ed8ef63cc70f",
+  "entries": [
+    {
+      "area": "schema identity",
+      "source": "sdar.provider.ops.event / 1.1.0",
+      "platform": "exact name, exact version in body; Collector injects contract version",
+      "classification": "CONFIG_MISMATCH",
+      "remediation": "Require and cross-check sdar.schema.name/version/id/hash OTLP attributes without overwriting source contract identity."
+    },
+    {
+      "area": "eventCategory",
+      "source": "dotted semantic categories including resource.state and business_event.lifecycle",
+      "platform": "audit|diagnostic",
+      "classification": "SCHEMA_MISMATCH",
+      "remediation": "Align both schema copies and bounded runtime validation to current source categories."
+    },
+    {
+      "area": "deliveryClass",
+      "source": "audit|operational",
+      "platform": "durable|best_effort schema; validator requires durable",
+      "classification": "SCHEMA_MISMATCH",
+      "remediation": "Accept only current source audit|operational values; keep fail-closed validation."
+    },
+    {
+      "area": "recordType",
+      "source": "provider.* record types including Provider ingress and Business Events",
+      "platform": "mixed legacy/unprefixed allowlist",
+      "classification": "SCHEMA_MISMATCH",
+      "remediation": "Use a bounded current-source allowlist and update core projection aliases."
+    },
+    {
+      "area": "canonical hash",
+      "source": "SHA-256 over canonical JSON excluding six delivery fields",
+      "platform": "same algorithm and exclusions",
+      "classification": "MATCH",
+      "remediation": "Add pinned source-derived parity vector to prevent drift."
+    },
+    {
+      "area": "top-level trace/span",
+      "source": "traceId/spanId are first-class optional fields",
+      "platform": "normalizer and landing only inspect nested attributes",
+      "classification": "PLATFORM_DEFECT",
+      "remediation": "Give top-level values first priority and preserve them through landing, canonical payload, core, and query."
+    },
+    {
+      "area": "source lineage",
+      "source": "provider/runtime/task/resource/execution/operation/provider-event/revisions/timestamps",
+      "platform": "subset retained",
+      "classification": "PLATFORM_DEFECT",
+      "remediation": "Preserve emitted first-class values verbatim; absent values remain null/absent."
+    },
+    {
+      "area": "OTLP ProviderOps selection",
+      "source": "LogRecord body plus sdar.schema.name/version and sdar.record.id/hash",
+      "platform": "Collector only tests sdar.record.id and then injects ProviderOps channel",
+      "classification": "TRANSPORT_MISMATCH",
+      "remediation": "Filter on the full canonical attribute tuple and require body/attribute cross-checks."
+    },
+    {
+      "area": "Runtime metrics pull",
+      "source": "Prometheus text at 192.168.1.7:19100/metrics without credentials",
+      "platform": "no Prometheus receiver",
+      "classification": "CONFIG_MISMATCH",
+      "remediation": "Add an explicitly identified Prometheus scrape pipeline and live read-only smoke."
+    },
+    {
+      "area": "Runtime to Collector security",
+      "source": "production HTTPS/mTLS supported and required",
+      "platform": "default plaintext only; docs mention unsupported env knobs",
+      "classification": "TRANSPORT_MISMATCH",
+      "remediation": "Separate development from qualification/production Collector configs and mount exact CA/server certificate/key."
+    },
+    {
+      "area": "Collector to Processor security",
+      "source": "internal trusted boundary must be bounded",
+      "platform": "plaintext Compose exporter; Processor mTLS code unused",
+      "classification": "TRANSPORT_MISMATCH",
+      "remediation": "Qualification profile uses Collector client mTLS and Processor server mTLS with no skip-verify."
+    },
+    {
+      "area": "source mapping",
+      "source": "isr.vehicle.ugv.ugv1 / production-ugv-direct-1",
+      "platform": "generated providerId=* / instanceId=*",
+      "classification": "CONFIG_MISMATCH",
+      "remediation": "Require exact provider/runtime instance values in qualification; reject wildcard generation there."
+    },
+    {
+      "area": "WAL ACK boundary",
+      "source": "audit exporter retries until Collector success",
+      "platform": "file fsync precedes success response and exact duplicate is idempotent",
+      "classification": "MATCH",
+      "remediation": "Add restart/outage/conflict qualification evidence."
+    },
+    {
+      "area": "invalid/conflict observability",
+      "source": "bounded rejects and duplicate conflicts are explicit",
+      "platform": "conflicts persist; invalid/sensitive rejects are metrics-only",
+      "classification": "PLATFORM_DEFECT",
+      "remediation": "Persist a safe metadata-only reject summary without raw secret-bearing input."
+    },
+    {
+      "area": "storage/query",
+      "source": "current first-class dimensions",
+      "platform": "four storage layers exist; broad dimension query absent",
+      "classification": "PLATFORM_DEFECT",
+      "remediation": "Extend compatible schema/projections and add bounded dimension search plus Grafana coverage."
+    },
+    {
+      "area": "live ProviderOps",
+      "source": "Runtime shows audit backlog 143399 and retry total 2163655",
+      "platform": "not deployed at capture time",
+      "classification": "SOURCE_UNAVAILABLE",
+      "remediation": "Deploy Collector first, observe backlog/read-only flow, and mark live OTLP partial if external Runtime remains pointed elsewhere."
+    }
+  ]
+}
diff --git a/reports/smpp-stable-integration/CONTRACT_MATRIX.md b/reports/smpp-stable-integration/CONTRACT_MATRIX.md
new file mode 100644
index 0000000..c69c534
--- /dev/null
+++ b/reports/smpp-stable-integration/CONTRACT_MATRIX.md
@@ -0,0 +1,27 @@
+# Contract and integration Gap Matrix
+
+Written before broad product coding against telemetry base `53a799d4c0166669411e61b816c6ed8ef63cc70f`
+and the user-selected local SMPP source `678521e35c83793a59e045ef9bf59b4df842962e`.
+
+| Area | Source authority | Current platform | Classification | Required correction |
+| --- | --- | --- | --- | --- |
+| Schema identity | `sdar.provider.ops.event/1.1.0` in body and OTLP attributes | Body checked, source attributes not fully required/cross-checked | CONFIG_MISMATCH | Require and cross-check name/version/id/hash |
+| `eventCategory` | Dotted semantic values such as `resource.state` | `audit\|diagnostic` | SCHEMA_MISMATCH | Align both schema copies and validator |
+| `deliveryClass` | `audit\|operational` | `durable\|best_effort`; validator requires `durable` | SCHEMA_MISMATCH | Accept only current bounded values |
+| `recordType` | Current `provider.*` classes, including Provider ingress and Business Events | Mixed legacy/unprefixed allowlist | SCHEMA_MISMATCH | Replace with bounded source-derived allowlist |
+| Canonical hash | Canonical SHA-256 with six delivery fields excluded | Same implementation | MATCH | Lock with source-derived vector |
+| Trace/span | Canonical top-level fields | Nested attributes only | PLATFORM_DEFECT | Top-level first, nested fallback |
+| Lineage | Provider/runtime/task/resource/execution/operation/event/revision/timestamps | Partial | PLATFORM_DEFECT | Preserve every emitted first-class value |
+| Collector selection | Full `sdar.schema.*` and `sdar.record.*` tuple | Presence of record ID only | TRANSPORT_MISMATCH | Fail closed on canonical tuple; exclude non-ProviderOps |
+| Prometheus pull | Reachable `/metrics` at `192.168.1.7:19100` | No receiver/pipeline | CONFIG_MISMATCH | Add identified scrape pipeline and smoke |
+| Runtime→Collector | Production HTTPS/mTLS | Plaintext Compose | TRANSPORT_MISMATCH | Explicit development and mTLS qualification profiles |
+| Collector→Processor | Bounded trusted internal identity | Plaintext Compose; dormant Processor mTLS support | TRANSPORT_MISMATCH | Enable mutual TLS in qualification profile |
+| Source mapping | Exact `isr.vehicle.ugv.ugv1` / `production-ugv-direct-1` | Wildcards generated | CONFIG_MISMATCH | Exact identity generation; reject qualification wildcards |
+| WAL/duplicate | Retry until ACK, stable ID/hash | fsync-before-ACK and idempotent duplicate | MATCH | Add restart/outage/replay evidence |
+| Reject observability | Explicit bounded failures | Conflict persisted, invalid/sensitive metrics-only | PLATFORM_DEFECT | Safe metadata-only rejection records |
+| Storage/query | Current first-class dimensions | Four layers exist, broad dimension query absent | PLATFORM_DEFECT | Compatible schema additions and bounded search |
+| Live ProviderOps | Runtime audit backlog is non-zero and growing | Platform not deployed at capture time | SOURCE_UNAVAILABLE | Deploy and observe; do not fabricate live ProviderOps success |
+
+The four required P0 hypotheses are confirmed: contract enum drift, top-level trace/span loss,
+production mTLS mismatch, and wildcard source identity. No SMPP contract defect was found. The local
+SMPP branch only changes deployment OTLP wiring; it does not change the current envelope contract.
diff --git a/reports/smpp-stable-integration/FAULT_RECOVERY.json b/reports/smpp-stable-integration/FAULT_RECOVERY.json
new file mode 100644
index 0000000..82b6622
--- /dev/null
+++ b/reports/smpp-stable-integration/FAULT_RECOVERY.json
@@ -0,0 +1,17 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T11:32:00.000Z",
+  "tests": [
+    {"name": "exact duplicate replay", "result": "PASS", "evidence": "one canonical row after two identical sends"},
+    {"name": "record hash conflict", "result": "PASS", "evidence": "HTTP exporter failure and one isolated conflict row"},
+    {"name": "Processor restart", "result": "PASS", "evidence": "read-only smoke passed after restart"},
+    {"name": "Collector restart", "result": "PASS", "evidence": "read-only smoke passed after restart"},
+    {"name": "ClickHouse outage", "result": "PASS", "evidence": "pending=1 during outage; landing/normalized/core each recovered to one row"},
+    {"name": "malformed envelope", "result": "PASS", "evidence": "REQUIRED_FIELD_MISSING summary"},
+    {"name": "oversized field", "result": "PASS", "evidence": "STRING_TOO_LONG summary"},
+    {"name": "sensitive input", "result": "PASS", "evidence": "SENSITIVE_KEY_DETECTED summary with no raw value"},
+    {"name": "partial WAL tail", "result": "PASS", "evidence": "unit recovery truncates partial tail without losing accepted entry"}
+  ],
+  "canonicalCorruptionObserved": false,
+  "status": "RESILIENCE_READY"
+}
diff --git a/reports/smpp-stable-integration/FINAL_REPORT.md b/reports/smpp-stable-integration/FINAL_REPORT.md
new file mode 100644
index 0000000..879611d
--- /dev/null
+++ b/reports/smpp-stable-integration/FINAL_REPORT.md
@@ -0,0 +1,53 @@
+# Stable SMPP × Telemetry Platform — Goal 01 final report
+
+Final qualification: **SMPP_TELEMETRY_INTEGRATION_PARTIAL**.
+
+Product correctness, source-contract parity, Collector filtering, dual-hop mTLS, normalization, WAL durability, ClickHouse storage, bounded Query API, redaction, resilience, and repeatable deployment are ready. The remaining qualification gap is external: the provided real SMPP endpoint exposed and was successfully scraped as Prometheus metrics, but no real ProviderOps OTLP log from that Runtime entered the isolated qualification Collector. The fixed current-source vector passed end to end and is not mislabeled as a live emission.
+
+## Revisions
+
+- Telemetry branch: local `main` (user override; no isolated branch)
+- Telemetry base: `53a799d4c0166669411e61b816c6ed8ef63cc70f`
+- Telemetry final HEAD: `5ff2a4e5b2fd8066fdbb22f98bb9c68b0788a926`
+- SMPP primary source: `codex/goal-11-npc-tank-simulation-real-interface` at `678521e35c83793a59e045ef9bf59b4df842962e`
+- SMPP stable comparison: `origin/main` at `cce7ea0cb0fe1b13328a48189a76d9b1688f49f4`
+- SMPP source was read-only and unchanged.
+
+## Contract and hash
+
+- Schema: `sdar.provider.ops.event` / `1.1.0`
+- Current source allowlists: 16 record types, 11 event categories, delivery classes `audit|operational`
+- Schema copies are byte-identical: SHA-256 `d8266956622b7c6eb1e970c0f8979fe58a7709af8619d94448f97d49f1f259f7`
+- Source vector record ID: `6eb4e972-be6e-5708-98ef-95dd5f263b96`
+- Exact source/platform record hash: `57afebfcb2fcd7b2eb7a7ea2b79f7348d57b279ebf06cb87fe0a03621440dfee`
+- Hash parity: PASS; no `SMPP_TELEMETRY_SOURCE_DEFECT` found.
+
+## Layered readiness
+
+| Layer | Status | Evidence |
+|---|---|---|
+| SOURCE_CONTRACT_READY | READY | Current local source capture, exact allowlists, vector parity |
+| COLLECTOR_READY | READY | Exact 0.157.0 image/config, strict tuple filter, Prometheus `up=1`, mTLS |
+| NORMALIZATION_READY | READY | Top-level trace/span and full source lineage preserved; no fabricated identity/time |
+| STORAGE_READY | READY | WAL, idempotency, conflict isolation, explicit migrations, all four data layers |
+| QUERY_READY | READY | Authenticated bounded multi-dimension query returned the source record and lineage |
+| LIVE_SMPP_READY | PARTIAL | Real metrics qualified; real Runtime ProviderOps OTLP not observed |
+| RESILIENCE_READY | READY | Processor/Collector restart, ClickHouse outage/recovery, malformed/oversize tests |
+| DEPLOYMENT_READY | READY | Final-SHA OCI labels, isolated volumes, private bind, migration service, qualification PASS |
+
+## Key qualification results
+
+- Source vector traversed mTLS Collector → mTLS Processor → WAL → landing → normalized → core → serving/query.
+- Exact duplicate produced one canonical record; same-ID/different-hash produced one isolated conflict.
+- ClickHouse outage left `pending=1`; recovery projected the record once into landing, normalized, and core.
+- Rejections persisted metadata only: `REQUIRED_FIELD_MISSING`, `STRING_TOO_LONG`, and `SENSITIVE_KEY_DETECTED`; leaked sensitive rows: 0.
+- Missing client certificate was rejected with `certificate required`; a client signed by an untrusted CA was rejected with `unknown ca`.
+- Final qualification stack bound published endpoints to `127.0.0.1`; ClickHouse, Grafana, and Processor were not published.
+- Final Runtime metrics capture: 43 samples/lines, Runtime `2.0.0-rc.1`, `telemetry_audit_backlog=0`, Collector scrape `up=1`.
+- Regression: build PASS, test 28/28 PASS, check PASS, exact Collector validation PASS. Strict typecheck remains baseline debt and is recorded as a limitation.
+
+## Acceptance gates
+
+G0, G1, G2, G4, G5, G6, G8, G9 and G10 pass. G3/G7 are partial because real Runtime ProviderOps OTLP was not observed. G11 is partial only because the pre-existing strict TypeScript debt still fails `pnpm typecheck`; all executable regression and deployment checks pass.
+
+No UGV movement, recon, gimbal, fire, or other side-effecting operation was invoked. No push, PR, tag, merge, release, or SMPP source change was performed.
diff --git a/reports/smpp-stable-integration/KNOWN_LIMITATIONS.md b/reports/smpp-stable-integration/KNOWN_LIMITATIONS.md
new file mode 100644
index 0000000..4d06bc3
--- /dev/null
+++ b/reports/smpp-stable-integration/KNOWN_LIMITATIONS.md
@@ -0,0 +1,7 @@
+# Known limitations
+
+1. The real Prometheus plane is qualified, but no real ProviderOps OTLP record from the external SMPP Runtime reached this isolated qualification Collector. The final Runtime snapshot reported 24,689 audit export failures, and the qualification deliberately did not mutate external Runtime configuration. A source-derived envelope generated by current SMPP code passed the complete Collector → Processor → WAL → ClickHouse → Query path, but it is not presented as a live Runtime emission.
+2. `pnpm typecheck` still fails on broad baseline TypeScript debt: existing JavaScript-style `.ts` files omit class property declarations and parameter types. Strict compiler settings were preserved. Build, `pnpm test` (28/28), and deployment qualification pass.
+3. No real SDAR-origin task correlation record was available from the Runtime. Explicit N:N relation projection is covered by a 2×2 test, and the source-derived record proves trace/span and source lineage preservation, but no synthetic relation is claimed as live evidence.
+4. Qualification certificates and secrets are operator-supplied and were generated only in a disposable `/tmp` directory. They are not part of the delivery bundle. Production must issue certificates from its approved PKI and bind OTLP only to the required private interface.
+5. The user-directed branch policy overrides the work package's isolated-branch instruction: work was committed directly to local `main`. No remote push, PR, tag, merge, release, or SMPP mutation was performed.
diff --git a/reports/smpp-stable-integration/PERSISTENCE_INTEGRITY.json b/reports/smpp-stable-integration/PERSISTENCE_INTEGRITY.json
new file mode 100644
index 0000000..19c3808
--- /dev/null
+++ b/reports/smpp-stable-integration/PERSISTENCE_INTEGRITY.json
@@ -0,0 +1,37 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T11:32:00.000Z",
+  "qualificationProject": "smpp-telemetry-goal01-qualification",
+  "isolatedVolumes": true,
+  "wal": {
+    "fsyncBeforeSuccess": true,
+    "recoveredAfterProcessorRestart": true,
+    "clickhouseOutagePendingRecords": 1,
+    "clickhouseRecoveryProjectedRecords": 1
+  },
+  "idempotency": {
+    "sameRecordIdSameHashInputs": 2,
+    "landingRows": 1,
+    "normalizedRows": 1,
+    "coreRows": 1
+  },
+  "conflictIsolation": {
+    "sameRecordIdDifferentHashInputs": 1,
+    "conflictRows": 1,
+    "canonicalRowsCorrupted": false
+  },
+  "finalQualificationCounts": {
+    "landing": 2,
+    "normalized": 2,
+    "coreResourceState": 2,
+    "conflict": 1,
+    "rejectedSummary": 3
+  },
+  "migration": {
+    "explicitOneShotServiceExitCode": 0,
+    "pinnedClickHouseVersion": "25.3.14.14",
+    "dateTime64TtlCompatible": true,
+    "rfc3339JsonEachRowCompatible": true
+  },
+  "status": "STORAGE_READY"
+}
diff --git a/reports/smpp-stable-integration/PROCESSOR_NORMALIZATION.json b/reports/smpp-stable-integration/PROCESSOR_NORMALIZATION.json
new file mode 100644
index 0000000..25bef1b
--- /dev/null
+++ b/reports/smpp-stable-integration/PROCESSOR_NORMALIZATION.json
@@ -0,0 +1,34 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T11:32:00.000Z",
+  "normalizer": {
+    "id": "smpp-provider-ops-v1",
+    "version": 2,
+    "sourceSystem": "smpp",
+    "canonicalTraceSpanPriority": "top-level traceId/spanId before nested fallback",
+    "sourceTimestamps": {
+      "occurredAtRewritten": false,
+      "emittedAtRewritten": false,
+      "receivedAtSeparate": true,
+      "normalizedAtSeparate": true
+    },
+    "firstClassLineagePreserved": [
+      "providerId", "runtimeVersion", "instanceId", "taskId", "resourceId",
+      "resourceType", "externalExecutionId", "operationName", "providerEventId",
+      "providerEventSequence", "eventType", "executionMode", "simulationId",
+      "adapterRevision", "observationRevision", "commandSequence", "traceId", "spanId"
+    ],
+    "missingSourceValuesFabricated": false,
+    "exactDeploymentIdentityRequired": true,
+    "stableFactHashExcludesProcessingTime": true
+  },
+  "sourceVector": {
+    "recordId": "6eb4e972-be6e-5708-98ef-95dd5f263b96",
+    "landingTraceId": "0123456789abcdef0123456789abcdef",
+    "landingSpanId": "0123456789abcdef",
+    "runtimeInstanceId": "production-ugv-direct-1",
+    "deploymentId": "production-ugv-direct",
+    "providerEventId": "source-vector-001"
+  },
+  "status": "NORMALIZATION_READY"
+}
diff --git a/reports/smpp-stable-integration/REAL_SMOKE.json b/reports/smpp-stable-integration/REAL_SMOKE.json
new file mode 100644
index 0000000..dbc3f24
--- /dev/null
+++ b/reports/smpp-stable-integration/REAL_SMOKE.json
@@ -0,0 +1,32 @@
+{
+  "schemaVersion": "1.0",
+  "capturedAt": "2026-08-12T11:20:00.791Z",
+  "endpoint": "http://192.168.1.7:19100/metrics",
+  "credentialsRequired": false,
+  "reachable": true,
+  "format": "Prometheus text exposition",
+  "bytes": 3062,
+  "lines": 43,
+  "sha256": "7975524438059cabc49734c42dfcec4b6aad8163fdaccff00b8476d3f8573f18",
+  "runtimeVersion": "2.0.0-rc.1",
+  "observations": {
+    "telemetryAuditBacklog": 0,
+    "telemetryEventsEmittedAudit": 24689,
+    "telemetryExportAttemptsAudit": 24689,
+    "telemetryExportFailuresAudit": 24689,
+    "collectorScrapeUp": 1,
+    "collectorSamplesScraped": 43
+  },
+  "safety": {
+    "allowSideEffects": false,
+    "operationTriggeredByQualification": null,
+    "movementReconGimbalFireInvoked": false
+  },
+  "providerOps": {
+    "sourceDerivedCurrentCodeVectorEndToEnd": true,
+    "realRuntimeProviderOpsEndToEnd": false,
+    "landingRowsNotFromQualificationInjection": 0,
+    "interpretation": "The live metrics plane is qualified. The external Runtime did not deliver a real ProviderOps OTLP record to this isolated Collector, and the qualification did not modify external SMPP configuration."
+  },
+  "status": "LIVE_SMPP_PARTIAL_METRICS_ONLY"
+}
diff --git a/reports/smpp-stable-integration/REGRESSION.json b/reports/smpp-stable-integration/REGRESSION.json
new file mode 100644
index 0000000..f05a617
--- /dev/null
+++ b/reports/smpp-stable-integration/REGRESSION.json
@@ -0,0 +1,24 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T11:32:00.000Z",
+  "telemetrySha": "5ff2a4e5b2fd8066fdbb22f98bb9c68b0788a926",
+  "commands": {
+    "pnpmBuild": "PASS",
+    "pnpmTest": {"status": "PASS", "tests": 28, "passed": 28, "failed": 0},
+    "pnpmCheck": {"status": "PASS", "tests": 28, "passed": 28, "failed": 0},
+    "pnpmTypecheck": {
+      "status": "FAIL_BASELINE_DEBT",
+      "exitCode": 2,
+      "strictOptionsPreserved": true,
+      "summary": "The baseline repository uses JavaScript-style TypeScript with undeclared class properties and implicit any across existing Processor, WAL, Query and test files. The Goal did not weaken strict compiler options."
+    },
+    "gitDiffCheck": "PASS",
+    "collectorExactImageConfigValidation": "PASS",
+    "schemaCopyParity": "PASS",
+    "migrationDeploymentTests": "PASS",
+    "queryApiTests": "PASS",
+    "realMetricsSmoke": "PASS",
+    "deploymentQualification": "PASS"
+  },
+  "status": "REGRESSION_PARTIAL_TYPECHECK_BASELINE_DEBT"
+}
diff --git a/reports/smpp-stable-integration/SECURITY_REDACTION.json b/reports/smpp-stable-integration/SECURITY_REDACTION.json
new file mode 100644
index 0000000..16df6e1
--- /dev/null
+++ b/reports/smpp-stable-integration/SECURITY_REDACTION.json
@@ -0,0 +1,36 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T11:32:00.000Z",
+  "mtls": {
+    "runtimeToCollectorRequired": true,
+    "collectorToProcessorRequired": true,
+    "skipVerifyPresent": false,
+    "missingClientCertificateExitCode": 56,
+    "missingClientCertificateAlert": "certificate required",
+    "untrustedClientIdentityExitCode": 56,
+    "untrustedClientIdentityAlert": "unknown ca"
+  },
+  "identity": {
+    "collectorId": "smpp-gateway-ugv-1",
+    "trustDomain": "sdar-ugv-runtime-mtls",
+    "deploymentId": "production-ugv-direct",
+    "providerId": "isr.vehicle.ugv.ugv1",
+    "instanceId": "production-ugv-direct-1",
+    "wildcards": false,
+    "forgedCollectorRejected": true
+  },
+  "network": {
+    "qualificationPublishedAddress": "127.0.0.1",
+    "wildcardBindRejectedByPreflight": true,
+    "clickhousePublished": false,
+    "grafanaPublished": false,
+    "processorPublished": false
+  },
+  "rejectionRedaction": {
+    "safeSummaryRows": 3,
+    "rawSensitiveValueRows": 0,
+    "rawRejectedEnvelopePersisted": false
+  },
+  "secretValuesPresentInReports": false,
+  "status": "SECURITY_READY"
+}
diff --git a/reports/smpp-stable-integration/SMPP_TELEMETRY_SOURCE_CAPTURE.json b/reports/smpp-stable-integration/SMPP_TELEMETRY_SOURCE_CAPTURE.json
new file mode 100644
index 0000000..cce3e2f
--- /dev/null
+++ b/reports/smpp-stable-integration/SMPP_TELEMETRY_SOURCE_CAPTURE.json
@@ -0,0 +1,202 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T10:33:11.522Z",
+  "source": {
+    "repository": "zhouwen-giser/sdar-mcp-provider-platform",
+    "primaryBranch": "codex/goal-11-npc-tank-simulation-real-interface",
+    "primarySha": "678521e35c83793a59e045ef9bf59b4df842962e",
+    "stableComparisonSha": "cce7ea0cb0fe1b13328a48189a76d9b1688f49f4",
+    "contractDriftFromStableComparison": false,
+    "deploymentDriftFromStableComparison": true,
+    "deploymentDriftSummary": "The local branch wires configurable plaintext intranet OTLP/HTTP export into UGV/NPC production bundles; ProviderOps envelope, hashing, ingress, and exporter code are unchanged."
+  },
+  "runtime": {
+    "runtimeVersion": "2.0.0-rc.1",
+    "schemaName": "sdar.provider.ops.event",
+    "schemaVersion": "1.1.0",
+    "integrationBoundary": "SMPP Runtime OTLP/HTTP -> OpenTelemetry Collector",
+    "prometheusBoundary": "OpenTelemetry Collector Prometheus receiver -> SMPP Runtime /metrics"
+  },
+  "recordIdentity": {
+    "recordIdAlgorithm": "UUIDv5(SHA-1 standardized name mapping)",
+    "recordNamespace": "670c4482-8e75-58ae-a0d9-60c923de6432",
+    "recordNameMaterial": "RFC8785-style canonical JSON array [recordType, stableAggregateIdentity, eventIdentity, revision ?? null]",
+    "recordHashAlgorithm": "lowercase hex SHA-256 over canonical JSON object",
+    "recordHashExcludedTopLevelFields": [
+      "recordHash",
+      "emittedAt",
+      "instanceId",
+      "exportRetryCount",
+      "collectorTimestamp",
+      "exporterHost"
+    ]
+  },
+  "signals": {
+    "protocol": "OTLP/HTTP",
+    "paths": {
+      "logs": "/v1/logs",
+      "traces": "/v1/traces",
+      "metrics": "/v1/metrics"
+    },
+    "resourceAttributes": [
+      "service.name",
+      "service.version",
+      "service.instance.id",
+      "deployment.environment.name",
+      "sdar.provider.id",
+      "sdar.provider.version"
+    ],
+    "providerOpsLogAttributes": [
+      "sdar.schema.name",
+      "sdar.schema.version",
+      "sdar.record.id",
+      "sdar.record.hash"
+    ],
+    "auditOnlyLogAttribute": "sdar.delivery.class",
+    "body": "canonical ProviderOpsEnvelope JSON object"
+  },
+  "recordTypes": [
+    "provider.task.lifecycle",
+    "provider.command.lifecycle",
+    "provider.scheduler.decision",
+    "provider.recovery.lifecycle",
+    "provider.ttl.lifecycle",
+    "provider.resource.state",
+    "provider.resource.metric",
+    "provider.resource.health",
+    "provider.execution.progress",
+    "provider.business_event.source.lifecycle",
+    "provider.business_event.ingest.lifecycle",
+    "provider.business_event.publication.lifecycle",
+    "provider.business_event.stream.lifecycle",
+    "provider.business_event.continuity",
+    "provider.business_event.delivery.lifecycle",
+    "provider.business_event.relation.lifecycle"
+  ],
+  "eventCategories": [
+    "task.lifecycle",
+    "command.lifecycle",
+    "command.dispatch",
+    "scheduler.decision",
+    "recovery.lifecycle",
+    "ttl.lifecycle",
+    "resource.state",
+    "resource.metric",
+    "resource.health",
+    "execution.progress",
+    "business_event.lifecycle"
+  ],
+  "deliveryClasses": [
+    "audit",
+    "operational"
+  ],
+  "providerIngressEventTypes": [
+    "RESOURCE_STATE",
+    "RESOURCE_METRIC",
+    "RESOURCE_HEALTH",
+    "EXECUTION_PROGRESS"
+  ],
+  "lineage": {
+    "topLevelOptionalFields": [
+      "taskId",
+      "resourceId",
+      "resourceType",
+      "externalExecutionId",
+      "operationName",
+      "correlationId",
+      "traceId",
+      "spanId",
+      "providerEventId",
+      "providerEventSequence",
+      "eventType",
+      "executionMode",
+      "simulationId",
+      "argumentHash",
+      "authorizationContextHash",
+      "adapterRevision",
+      "observationRevision",
+      "commandSequence"
+    ],
+    "traceSemantics": "A valid provider traceparent supplies top-level traceId/spanId; the persisted task traceId is fallback for traceId and is retained as attributes.linkedTaskTraceId when present.",
+    "timestampSemantics": "occurredAt is source-authoritative and normalized to ISO-8601; emittedAt is delivery time. Audit OTLP LogRecord timestamp is occurredAt. Consumers must keep receive/normalization times separate."
+  },
+  "validationAndRedaction": {
+    "maxBatch": 100,
+    "maxEventBytes": 65536,
+    "maxDepth": 16,
+    "maxNodes": 4096,
+    "maxAttributeKeyLength": 128,
+    "maxResourceIdLength": 512,
+    "maxTimestampSkewMs": 86400000,
+    "defaultRateLimitPerMinute": 600,
+    "payloadPolicy": "event-type allowlist followed by recursive sanitizer",
+    "sensitiveKeyClasses": [
+      "password",
+      "secret",
+      "api key",
+      "cookie",
+      "authorization",
+      "token",
+      "jwt",
+      "raw argument/input/answer/adapter payload"
+    ],
+    "allowedSensitiveHashes": [
+      "argumentHash",
+      "authorizationContextHash"
+    ]
+  },
+  "runtimeOtlpConfigKeys": [
+    "OTEL_ENABLED",
+    "OTEL_EXPORTER_OTLP_ENDPOINT",
+    "OTEL_EXPORTER_OTLP_TLS_MODE",
+    "OTEL_EXPORTER_OTLP_CA_PATH",
+    "OTEL_EXPORTER_OTLP_CERT_PATH",
+    "OTEL_EXPORTER_OTLP_KEY_PATH",
+    "OTEL_EXPORTER_OTLP_HEADERS_FILE",
+    "OTEL_EXPORTER_OTLP_TIMEOUT_MS",
+    "OTEL_SERVICE_INSTANCE_ID"
+  ],
+  "sourceDerivedVector": {
+    "path": "telemetry-schema/contracts/test-vectors/smpp-local-source-provider-resource-state-1.1.0.json",
+    "recordId": "6eb4e972-be6e-5708-98ef-95dd5f263b96",
+    "recordHash": "57afebfcb2fcd7b2eb7a7ea2b79f7348d57b279ebf06cb87fe0a03621440dfee",
+    "sourceRecomputedHash": "57afebfcb2fcd7b2eb7a7ea2b79f7348d57b279ebf06cb87fe0a03621440dfee",
+    "hashParity": true
+  },
+  "livePrometheusCapture": {
+    "endpoint": "http://192.168.1.7:19100/metrics",
+    "credentialsRequired": false,
+    "reachable": true,
+    "format": "Prometheus text exposition",
+    "bytes": 3234,
+    "lines": 41,
+    "sha256": "8a29b376c31d0c52fe1236f748e158a47c4a1cadf0aa9c27da961bf6ad9310ca",
+    "runtimeVersionObserved": "2.0.0-rc.1",
+    "metricFamiliesObserved": [
+      "sdar_adapter_rpc_total",
+      "sdar_business_event_active_relation_tokens",
+      "sdar_business_event_finalized_total",
+      "sdar_business_event_inbox_backlog",
+      "sdar_business_event_oldest_pending_mapping_age_seconds",
+      "sdar_business_event_publication_barrier_waiting",
+      "sdar_business_event_source_blocked_total",
+      "sdar_business_event_source_received_total",
+      "sdar_business_event_source_rejected_total",
+      "sdar_outbox_pending",
+      "sdar_recovery_total",
+      "sdar_runtime_info",
+      "sdar_tool_call_duration_seconds_count",
+      "sdar_tool_call_duration_seconds_sum",
+      "sdar_tool_calls_total",
+      "telemetry_audit_backlog",
+      "telemetry_audit_oldest_age_seconds",
+      "telemetry_audit_retry_total"
+    ],
+    "operationalObservation": {
+      "telemetryAuditBacklog": 143399,
+      "telemetryAuditRetryTotal": 2163655,
+      "interpretation": "The Runtime has a large audit export backlog at capture time; this is environment evidence that OTLP delivery is not currently succeeding, not a schema defect."
+    }
+  },
+  "sourceContractStatus": "SOURCE_CONTRACT_READY"
+}
diff --git a/reports/smpp-stable-integration/SMPP_TELEMETRY_SOURCE_CAPTURE.md b/reports/smpp-stable-integration/SMPP_TELEMETRY_SOURCE_CAPTURE.md
new file mode 100644
index 0000000..22330de
--- /dev/null
+++ b/reports/smpp-stable-integration/SMPP_TELEMETRY_SOURCE_CAPTURE.md
@@ -0,0 +1,58 @@
+# SMPP telemetry source capture
+
+Captured at `2026-08-12T10:33:11.522Z` from the user-selected local SMPP branch
+`codex/goal-11-npc-tank-simulation-real-interface` at
+`678521e35c83793a59e045ef9bf59b4df842962e`. The stable comparison remains
+`origin/main` at `cce7ea0cb0fe1b13328a48189a76d9b1688f49f4`.
+
+The local branch changes production-bundle OTLP wiring and validation only. It does not change the
+ProviderOps envelope, hash implementation, Provider telemetry ingress, or OTLP exporter mapping.
+
+## Canonical contract
+
+- Schema: `sdar.provider.ops.event` / `1.1.0`.
+- Runtime product version: `2.0.0-rc.1`.
+- `recordId`: UUIDv5 in namespace `670c4482-8e75-58ae-a0d9-60c923de6432`, using canonical JSON
+  `[recordType, stableAggregateIdentity, eventIdentity, revision ?? null]` as the name.
+- `recordHash`: lowercase SHA-256 of canonical JSON after excluding `recordHash`, `emittedAt`,
+  `instanceId`, `exportRetryCount`, `collectorTimestamp`, and `exporterHost`.
+- Delivery classes found in source: `audit` and `operational`.
+- Provider ingress categories: `resource.state`, `resource.metric`, `resource.health`, and
+  `execution.progress`; all use delivery class `audit`.
+- Other current categories include task/command/scheduler/recovery/TTL and Business Event lifecycle
+  records. The machine-readable capture contains the full enumerated set found in source.
+
+The source-derived resource-state vector has record ID
+`6eb4e972-be6e-5708-98ef-95dd5f263b96` and record hash
+`57afebfcb2fcd7b2eb7a7ea2b79f7348d57b279ebf06cb87fe0a03621440dfee`. Recomputing with the SMPP
+source implementation produced the same hash.
+
+## OTLP mapping
+
+Runtime exports OTLP/HTTP to `/v1/logs`, `/v1/traces`, and `/v1/metrics`. The ProviderOps envelope is
+the LogRecord body. Canonical log attributes are `sdar.schema.name`, `sdar.schema.version`,
+`sdar.record.id`, and `sdar.record.hash`; audit delivery also includes `sdar.delivery.class`.
+Resource identity is carried by `service.name`, `service.version`, `service.instance.id`,
+`deployment.environment.name`, `sdar.provider.id`, and `sdar.provider.version`.
+
+Top-level ProviderOps `traceId`/`spanId` are authoritative when supplied by a valid Provider
+`traceparent`. `occurredAt` remains the source timestamp; `emittedAt` is delivery time. Neither may be
+rewritten as receive or normalization time.
+
+Production OTLP uses HTTPS plus `OTEL_EXPORTER_OTLP_TLS_MODE=required` and CA/client certificate/key
+files. Export headers, if any, are file-backed. The local UGV bundle branch intentionally adds an
+explicit plaintext intranet mode; that mode is development/intranet evidence, not production mTLS
+qualification.
+
+## Live read-only Prometheus capture
+
+`http://192.168.1.7:19100/metrics` was reachable without credentials and returned 41 lines / 3234
+bytes of Prometheus text (`sha256:8a29b376c31d0c52fe1236f748e158a47c4a1cadf0aa9c27da961bf6ad9310ca`).
+It identifies Runtime `2.0.0-rc.1` and includes Runtime, adapter RPC, Business Event, tool-call,
+outbox, recovery, and telemetry audit health families.
+
+At capture time `telemetry_audit_backlog` was `143399` and `telemetry_audit_retry_total` was
+`2163655`. This is strong environment evidence that Runtime OTLP audit delivery is not currently
+succeeding; it is not a ProviderOps schema defect. No operation or side effect was invoked.
+
+Status: `SOURCE_CONTRACT_READY`.
diff --git a/reports/smpp-stable-integration/TASK_CORRELATION.json b/reports/smpp-stable-integration/TASK_CORRELATION.json
new file mode 100644
index 0000000..0d938da
--- /dev/null
+++ b/reports/smpp-stable-integration/TASK_CORRELATION.json
@@ -0,0 +1,23 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T11:32:00.000Z",
+  "canonicalLineage": {
+    "topLevelTraceAndSpanPreserved": true,
+    "taskResourceExecutionProviderDimensionsQueryable": true,
+    "sourceDeploymentPartOfEntityUrn": true,
+    "unknownDeploymentFabricated": false
+  },
+  "relationProjection": {
+    "explicitSdarOriginRequired": true,
+    "originDeploymentRequired": true,
+    "twoByTwoManyToManyTestExpectedRelations": 4,
+    "twoByTwoManyToManyTestActualRelations": 4,
+    "uniqueRelationIds": 4
+  },
+  "liveObservation": {
+    "sourceVectorContainsSdarOrigin": false,
+    "realRuntimeCorrelatedTaskObserved": false,
+    "syntheticRelationClaimedAsLive": false
+  },
+  "status": "CORRELATION_LOGIC_READY_LIVE_SAMPLE_NOT_OBSERVED"
+}
diff --git a/reports/smpp-stable-integration/TELEMETRY_PLATFORM_BASELINE.json b/reports/smpp-stable-integration/TELEMETRY_PLATFORM_BASELINE.json
new file mode 100644
index 0000000..0d39c5b
--- /dev/null
+++ b/reports/smpp-stable-integration/TELEMETRY_PLATFORM_BASELINE.json
@@ -0,0 +1,155 @@
+{
+  "schemaVersion": "1.0",
+  "observedAt": "2026-08-12T10:36:25.366Z",
+  "repositorySha": "53a799d4c0166669411e61b816c6ed8ef63cc70f",
+  "workingBranch": "main",
+  "platformVersion": "0.3.0",
+  "architecture": [
+    "otel-collector",
+    "telemetry-processor",
+    "WAL",
+    "clickhouse",
+    "query-api",
+    "grafana"
+  ],
+  "collector": {
+    "image": "otel/opentelemetry-collector-contrib:0.157.0",
+    "receivers": [
+      "otlp/http",
+      "otlp/grpc"
+    ],
+    "providerOpsFilter": "sdar.record.id attribute is present",
+    "processorExporter": "OTLP/HTTP to http://telemetry-processor:8443/internal/otlp",
+    "queueEnabled": false,
+    "retryEnabled": false,
+    "prometheusRuntimeScrapeConfigured": false,
+    "externalTransport": "plaintext",
+    "internalTransport": "plaintext"
+  },
+  "processor": {
+    "otlpLogJson": true,
+    "otlpLogProtobuf": true,
+    "trustedCollectorAllowlist": true,
+    "schemaValidation": "hard-coded ProviderOps 1.1.0 rules",
+    "hashRecomputation": true,
+    "walAckBoundary": "append + file fsync before accepted response",
+    "duplicateClassification": "sourceSystem + recordId with recordHash equality",
+    "conflictClassification": "same sourceSystem + recordId with different recordHash",
+    "rejectedSummaryPersistence": false,
+    "normalizer": "smpp-provider-ops-v1",
+    "normalizerVersion": 1
+  },
+  "storage": {
+    "databases": [
+      "telemetry_meta",
+      "telemetry_landing",
+      "telemetry_normalized",
+      "telemetry_core",
+      "telemetry_serving"
+    ],
+    "landing": true,
+    "normalized": true,
+    "core": true,
+    "relation": true,
+    "serving": true,
+    "conflictTable": true,
+    "rejectedSummaryTable": true,
+    "normalizationDeadLetterTable": true,
+    "processorWritesRejectedSummary": false
+  },
+  "query": {
+    "recordLookup": true,
+    "taskTimeline": true,
+    "taskRelations": true,
+    "topology": true,
+    "providerHealth": true,
+    "resourceState": true,
+    "broadDimensionSearch": false,
+    "grafanaClickHouseDatasourceUsesExamplePassword": true
+  },
+  "deployment": {
+    "composeServicesPresent": [
+      "clickhouse",
+      "telemetry-processor",
+      "otel-collector",
+      "query-api",
+      "grafana"
+    ],
+    "processorImageRevisionLabel": false,
+    "queryApiImageRevisionLabel": false,
+    "qualificationProductionMtlsProfile": false,
+    "sourceMappingGeneratorUsesWildcards": true,
+    "queryHealthScriptMatchesImplementation": false,
+    "isolatedQualificationVolumes": false
+  },
+  "confirmedFindings": [
+    {
+      "id": "P0-CONTRACT-ENUM",
+      "classification": "SCHEMA_MISMATCH",
+      "evidence": "Both schema copies allow eventCategory audit|diagnostic and deliveryClass durable|best_effort, while local SMPP emits dotted event categories and audit|operational delivery classes."
+    },
+    {
+      "id": "P0-RUNTIME-VALIDATOR",
+      "classification": "PLATFORM_DEFECT",
+      "evidence": "validateEnvelope requires deliveryClass=durable and eventCategory=audit; the source-derived vector is rejected."
+    },
+    {
+      "id": "P0-RECORD-TYPES",
+      "classification": "SCHEMA_MISMATCH",
+      "evidence": "The allowlist contains resource.state but SMPP emits provider.resource.state; current Business Event record types are absent."
+    },
+    {
+      "id": "P0-TRACE-SPAN",
+      "classification": "PLATFORM_DEFECT",
+      "evidence": "Normalizer and landing mapper ignore canonical top-level traceId/spanId and read only nested attributes."
+    },
+    {
+      "id": "P0-MTLS",
+      "classification": "TRANSPORT_MISMATCH",
+      "evidence": "Documentation requires Runtime->Collector and Collector->Processor mTLS, but Compose exposes plaintext OTLP and configures a plaintext Processor exporter."
+    },
+    {
+      "id": "P0-WILDCARD-IDENTITY",
+      "classification": "CONFIG_MISMATCH",
+      "evidence": "generate-config.ts emits providerId=* and instanceId=* and the resolver accepts wildcards."
+    },
+    {
+      "id": "P0-PROMETHEUS-SCRAPE",
+      "classification": "CONFIG_MISMATCH",
+      "evidence": "The user-provided Runtime /metrics endpoint is reachable, but Collector has no Prometheus receiver or runtime metrics pipeline."
+    },
+    {
+      "id": "P1-OTLP-CROSS-CHECK",
+      "classification": "PLATFORM_DEFECT",
+      "evidence": "Processor does not require/cross-check sdar.schema.name and sdar.schema.version, and Collector marks any log with sdar.record.id as ProviderOps."
+    },
+    {
+      "id": "P1-LINEAGE",
+      "classification": "PLATFORM_DEFECT",
+      "evidence": "Canonical payload omits providerEventId/Sequence, eventType, execution mode/simulation, revisions, hashes, and top-level trace/span; landing omits several first-class fields."
+    },
+    {
+      "id": "P1-REJECTION-OBSERVABILITY",
+      "classification": "PLATFORM_DEFECT",
+      "evidence": "Invalid/hash/sensitive events are counted but no safe rejected summary is persisted even though a ClickHouse table exists."
+    },
+    {
+      "id": "P1-QUERY-DIMENSIONS",
+      "classification": "PLATFORM_DEFECT",
+      "evidence": "Query API lacks a bounded activity search across provider/resource/task/operation/runtime/deployment/record/trace/time dimensions."
+    },
+    {
+      "id": "P1-DEPLOYMENT",
+      "classification": "PLATFORM_DEFECT",
+      "evidence": "status.sh calls /health/live on Query API while the implementation exposes /health; the deployment test references a missing docs path; no image revision labels or qualification lifecycle exist."
+    }
+  ],
+  "baselineExecution": {
+    "dependenciesInstalled": false,
+    "testCommandRun": false,
+    "reason": "Repository has no installed node_modules or committed lockfile; dependencies will be installed after the pre-code Gap Matrix is recorded.",
+    "existingPlatformContainers": false,
+    "unrelatedDockerContainersPreserved": true
+  },
+  "baselineStatus": "AUDITED_WITH_CONFIRMED_P0_GAPS"
+}
diff --git a/status.sh b/status.sh
index 0f499ca..7f4518e 100644
--- a/status.sh
+++ b/status.sh
@@ -5,4 +5,5 @@ cd "$ROOT"
 docker compose ps
 printf '\n健康检查：\n'
 curl -fsS http://127.0.0.1:13133/ >/dev/null && echo 'Collector: OK' || echo 'Collector: ERROR'
-curl -fsS "http://127.0.0.1:${QUERY_API_PORT:-8088}/health/live" >/dev/null && echo 'Query API: OK' || echo 'Query API: ERROR'
+curl -fsS "http://127.0.0.1:${QUERY_API_PORT:-8088}/health" >/dev/null && echo 'Query API: OK' || echo 'Query API: ERROR'
+curl -fsS http://127.0.0.1:9464/metrics | grep -q 'sdar_runtime_info' && echo 'SMPP Prometheus scrape: OK' || echo 'SMPP Prometheus scrape: ERROR'
diff --git a/telemetry-collector/config/gateway-mtls.yaml b/telemetry-collector/config/gateway-mtls.yaml
new file mode 100644
index 0000000..617f8bc
--- /dev/null
+++ b/telemetry-collector/config/gateway-mtls.yaml
@@ -0,0 +1,132 @@
+receivers:
+  otlp/smpp:
+    protocols:
+      grpc:
+        endpoint: 0.0.0.0:4317
+        tls:
+          cert_file: /run/secrets/collector_server_cert
+          key_file: /run/secrets/collector_server_key
+          client_ca_file: /run/secrets/runtime_client_ca
+      http:
+        endpoint: 0.0.0.0:4318
+        tls:
+          cert_file: /run/secrets/collector_server_cert
+          key_file: /run/secrets/collector_server_key
+          client_ca_file: /run/secrets/runtime_client_ca
+  prometheus/smpp_runtime:
+    config:
+      scrape_configs:
+        - job_name: sdar-ugv-runtime
+          scheme: http
+          metrics_path: ${env:SMPP_METRICS_PATH}
+          scrape_interval: ${env:SMPP_METRICS_SCRAPE_INTERVAL}
+          static_configs:
+            - targets: ["${env:SMPP_METRICS_TARGET}"]
+
+processors:
+  memory_limiter/provider_ops:
+    check_interval: 1s
+    limit_mib: 512
+    spike_limit_mib: 128
+  filter/provider_ops_only:
+    error_mode: ignore
+    logs:
+      log_record:
+        - 'attributes["sdar.record.id"] == nil'
+        - 'attributes["sdar.record.hash"] == nil'
+        - 'attributes["sdar.schema.name"] != "sdar.provider.ops.event"'
+        - 'attributes["sdar.schema.version"] != "1.1.0"'
+        - 'body == nil or body["schemaName"] != "sdar.provider.ops.event"'
+  resource/trusted_ingress:
+    attributes:
+      - key: telemetry.source.system
+        value: smpp
+        action: upsert
+      - key: telemetry.source.collector_id
+        value: ${env:COLLECTOR_ID}
+        action: upsert
+      - key: telemetry.source.trust_domain
+        value: ${env:TRUST_DOMAIN}
+        action: upsert
+      - key: telemetry.source.deployment_id
+        value: ${env:SMPP_DEPLOYMENT_ID}
+        action: upsert
+      - key: telemetry.ingress.mode
+        value: gateway-mtls
+        action: upsert
+  transform/provider_ops_contract:
+    error_mode: propagate
+    log_statements:
+      - context: log
+        statements:
+          - set(attributes["telemetry.channel"], "smpp.provider_ops")
+          - set(attributes["telemetry.contract.name"], attributes["sdar.schema.name"])
+          - set(attributes["telemetry.contract.version"], attributes["sdar.schema.version"])
+  resource/smpp_runtime_metrics:
+    attributes:
+      - key: telemetry.source.system
+        value: smpp
+        action: upsert
+      - key: telemetry.channel
+        value: smpp.runtime_metrics
+        action: upsert
+      - key: telemetry.source.deployment_id
+        value: ${env:SMPP_DEPLOYMENT_ID}
+        action: upsert
+      - key: telemetry.source.runtime_instance_id
+        value: ${env:SMPP_RUNTIME_INSTANCE_ID}
+        action: upsert
+      - key: telemetry.source.provider_id
+        value: ${env:SMPP_PROVIDER_ID}
+        action: upsert
+
+exporters:
+  otlphttp/processor_provider_ops:
+    endpoint: https://telemetry-processor:8443/internal/otlp
+    compression: none
+    timeout: 10s
+    tls:
+      ca_file: /run/secrets/processor_server_ca
+      cert_file: /run/secrets/collector_client_cert
+      key_file: /run/secrets/collector_client_key
+    sending_queue:
+      enabled: false
+    retry_on_failure:
+      enabled: false
+  debug/best_effort:
+    verbosity: basic
+  prometheus/smpp_runtime:
+    endpoint: 0.0.0.0:9464
+    resource_to_telemetry_conversion:
+      enabled: true
+
+extensions:
+  health_check:
+    endpoint: 0.0.0.0:13133
+
+service:
+  extensions: [health_check]
+  telemetry:
+    metrics:
+      level: normal
+      readers:
+        - pull:
+            exporter:
+              prometheus:
+                host: 0.0.0.0
+                port: 8888
+                without_type_suffix: true
+                without_units: true
+  pipelines:
+    logs/provider_ops:
+      receivers: [otlp/smpp]
+      processors: [memory_limiter/provider_ops, filter/provider_ops_only, resource/trusted_ingress, transform/provider_ops_contract]
+      exporters: [otlphttp/processor_provider_ops]
+    traces/best_effort:
+      receivers: [otlp/smpp]
+      processors: [memory_limiter/provider_ops]
+      exporters: [debug/best_effort]
+    metrics/smpp_runtime:
+      receivers: [otlp/smpp, prometheus/smpp_runtime]
+      processors: [memory_limiter/provider_ops, resource/smpp_runtime_metrics]
+      exporters: [prometheus/smpp_runtime, debug/best_effort]
diff --git a/telemetry-collector/config/gateway.yaml b/telemetry-collector/config/gateway.yaml
index d191780..8e05e6c 100644
--- a/telemetry-collector/config/gateway.yaml
+++ b/telemetry-collector/config/gateway.yaml
@@ -5,6 +5,15 @@ receivers:
         endpoint: 0.0.0.0:4317
       http:
         endpoint: 0.0.0.0:4318
+  prometheus/smpp_runtime:
+    config:
+      scrape_configs:
+        - job_name: sdar-ugv-runtime
+          scheme: http
+          metrics_path: ${env:SMPP_METRICS_PATH}
+          scrape_interval: ${env:SMPP_METRICS_SCRAPE_INTERVAL}
+          static_configs:
+            - targets: ["${env:SMPP_METRICS_TARGET}"]
 
 processors:
   memory_limiter/provider_ops:
@@ -16,6 +25,10 @@ processors:
     logs:
       log_record:
         - 'attributes["sdar.record.id"] == nil'
+        - 'attributes["sdar.record.hash"] == nil'
+        - 'attributes["sdar.schema.name"] != "sdar.provider.ops.event"'
+        - 'attributes["sdar.schema.version"] != "1.1.0"'
+        - 'body == nil or body["schemaName"] != "sdar.provider.ops.event"'
   resource/trusted_ingress:
     attributes:
       - key: telemetry.source.system
@@ -39,8 +52,25 @@ processors:
       - context: log
         statements:
           - set(attributes["telemetry.channel"], "smpp.provider_ops")
-          - set(attributes["telemetry.contract.name"], "ProviderOpsEnvelope")
-          - set(attributes["telemetry.contract.version"], "1.1.0")
+          - set(attributes["telemetry.contract.name"], attributes["sdar.schema.name"])
+          - set(attributes["telemetry.contract.version"], attributes["sdar.schema.version"])
+  resource/smpp_runtime_metrics:
+    attributes:
+      - key: telemetry.source.system
+        value: smpp
+        action: upsert
+      - key: telemetry.channel
+        value: smpp.runtime_metrics
+        action: upsert
+      - key: telemetry.source.deployment_id
+        value: ${env:SMPP_DEPLOYMENT_ID}
+        action: upsert
+      - key: telemetry.source.runtime_instance_id
+        value: ${env:SMPP_RUNTIME_INSTANCE_ID}
+        action: upsert
+      - key: telemetry.source.provider_id
+        value: ${env:SMPP_PROVIDER_ID}
+        action: upsert
 
 exporters:
   otlphttp/processor_provider_ops:
@@ -53,6 +83,10 @@ exporters:
       enabled: false
   debug/best_effort:
     verbosity: basic
+  prometheus/smpp_runtime:
+    endpoint: 0.0.0.0:9464
+    resource_to_telemetry_conversion:
+      enabled: true
 
 extensions:
   health_check:
@@ -80,7 +114,7 @@ service:
       receivers: [otlp/smpp]
       processors: [memory_limiter/provider_ops]
       exporters: [debug/best_effort]
-    metrics/best_effort:
-      receivers: [otlp/smpp]
-      processors: [memory_limiter/provider_ops]
-      exporters: [debug/best_effort]
+    metrics/smpp_runtime:
+      receivers: [otlp/smpp, prometheus/smpp_runtime]
+      processors: [memory_limiter/provider_ops, resource/smpp_runtime_metrics]
+      exporters: [prometheus/smpp_runtime, debug/best_effort]
diff --git a/telemetry-collector/tests/config.test.ts b/telemetry-collector/tests/config.test.ts
index 9e8377c..4d1e1c0 100644
--- a/telemetry-collector/tests/config.test.ts
+++ b/telemetry-collector/tests/config.test.ts
@@ -1 +1,3 @@
 import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';const file=new URL('telemetry-collector/config/gateway.yaml', `file://${process.cwd()}/`);test('official collector config preserves strict Processor ACK path',async()=>{const y=await readFile(file,'utf8');assert.match(y,/otlphttp\/processor_provider_ops/);assert.match(y,/endpoint: http:\/\/telemetry-processor:8443\/internal\/otlp/);assert.match(y,/sending_queue:\s*\n\s*enabled: false/);assert.match(y,/retry_on_failure:\s*\n\s*enabled: false/);assert.doesNotMatch(y,/logs\/provider_ops:[\s\S]*batch/);});
+test('ProviderOps selection checks canonical attributes and Runtime metrics are scraped',async()=>{const y=await readFile(file,'utf8');for(const value of ['sdar.schema.name','sdar.schema.version','sdar.record.id','sdar.record.hash'])assert.match(y,new RegExp(value.replaceAll('.','\\.')));assert.match(y,/prometheus\/smpp_runtime/);assert.match(y,/SMPP_METRICS_TARGET/);assert.match(y,/telemetry\.channel/);});
+test('qualification Collector uses mutual TLS on both hops without skip-verify',async()=>{const y=await readFile(new URL('telemetry-collector/config/gateway-mtls.yaml',`file://${process.cwd()}/`),'utf8');assert.match(y,/client_ca_file/);assert.match(y,/endpoint: https:\/\/telemetry-processor:8443\/internal\/otlp/);assert.match(y,/collector_client_cert/);assert.doesNotMatch(y,/insecure_skip_verify/);});
diff --git a/telemetry-dashboard/grafana/dashboards/overview.json b/telemetry-dashboard/grafana/dashboards/overview.json
index dd13377..94b85af 100644
--- a/telemetry-dashboard/grafana/dashboards/overview.json
+++ b/telemetry-dashboard/grafana/dashboards/overview.json
@@ -42,6 +42,26 @@
         "x": 0,
         "y": 10
       }
+    },
+    {
+      "type": "timeseries",
+      "title": "SMPP Runtime telemetry audit backlog",
+      "datasource": {
+        "uid": "smpp-runtime-prometheus"
+      },
+      "targets": [
+        {
+          "expr": "telemetry_audit_backlog",
+          "legendFormat": "{{telemetry_source_runtime_instance_id}}",
+          "refId": "A"
+        }
+      ],
+      "gridPos": {
+        "h": 8,
+        "w": 24,
+        "x": 0,
+        "y": 20
+      }
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/telemetry-dashboard/grafana/provisioning/datasources/clickhouse.yaml b/telemetry-dashboard/grafana/provisioning/datasources/clickhouse.yaml
index 4c95807..d415565 100644
--- a/telemetry-dashboard/grafana/provisioning/datasources/clickhouse.yaml
+++ b/telemetry-dashboard/grafana/provisioning/datasources/clickhouse.yaml
@@ -11,4 +11,10 @@ datasources:
       username: default
       defaultDatabase: telemetry_serving
     secureJsonData:
-      password: change-me
+      password: ${CLICKHOUSE_PASSWORD}
+  - name: SMPP Runtime Metrics
+    uid: smpp-runtime-prometheus
+    type: prometheus
+    access: proxy
+    url: http://otel-collector:9464
+    isDefault: false
diff --git a/telemetry-dashboard/query-api/Dockerfile b/telemetry-dashboard/query-api/Dockerfile
index 4080209..9d64e86 100644
--- a/telemetry-dashboard/query-api/Dockerfile
+++ b/telemetry-dashboard/query-api/Dockerfile
@@ -11,6 +11,8 @@ COPY deploy/bin ./deploy/bin
 RUN npm install --include=dev --ignore-scripts && npm run build
 
 FROM node:22-alpine
+ARG VCS_REF=development
+LABEL org.opencontainers.image.revision=$VCS_REF
 WORKDIR /app
 ENV NODE_ENV=production
 COPY --from=builder /app/dist ./dist
diff --git a/telemetry-dashboard/query-api/src/index.ts b/telemetry-dashboard/query-api/src/index.ts
index 90acc2d..760e565 100644
--- a/telemetry-dashboard/query-api/src/index.ts
+++ b/telemetry-dashboard/query-api/src/index.ts
@@ -1 +1 @@
-import {QueryClient} from './clickhouse.js';import {createQueryServer} from './server.js';const host=process.env.QUERY_API_HOST??'0.0.0.0',port=Number(process.env.QUERY_API_PORT??8088),client=new QueryClient({url:process.env.CLICKHOUSE_URL??'http://127.0.0.1:8123',user:process.env.CLICKHOUSE_USER??'default',password:process.env.CLICKHOUSE_PASSWORD??'',passwordFile:process.env.CLICKHOUSE_PASSWORD_FILE??''});await client.initialize();createQueryServer({client,apiKey:process.env.QUERY_API_KEY??''}).listen(port,host,()=>console.log(JSON.stringify({level:'info',message:'query_api_started',host,port})));
+import {readFile} from 'node:fs/promises';import {QueryClient} from './clickhouse.js';import {createQueryServer} from './server.js';const host=process.env.QUERY_API_HOST??'0.0.0.0',port=Number(process.env.QUERY_API_PORT??8088),client=new QueryClient({url:process.env.CLICKHOUSE_URL??'http://127.0.0.1:8123',user:process.env.CLICKHOUSE_USER??'default',password:process.env.CLICKHOUSE_PASSWORD??'',passwordFile:process.env.CLICKHOUSE_PASSWORD_FILE??''});await client.initialize();const apiKey=process.env.QUERY_API_KEY_FILE?(await readFile(process.env.QUERY_API_KEY_FILE,'utf8')).trim():process.env.QUERY_API_KEY??'';createQueryServer({client,apiKey}).listen(port,host,()=>console.log(JSON.stringify({level:'info',message:'query_api_started',host,port})));
diff --git a/telemetry-dashboard/query-api/src/server.ts b/telemetry-dashboard/query-api/src/server.ts
index eaad7ee..31bc4b0 100644
--- a/telemetry-dashboard/query-api/src/server.ts
+++ b/telemetry-dashboard/query-api/src/server.ts
@@ -1,3 +1,86 @@
-import http from 'node:http';import {sqlString} from './clickhouse.js';
-function json(res,status,value){const b=Buffer.from(JSON.stringify(value));res.writeHead(status,{'content-type':'application/json','content-length':b.length});res.end(b);}const decode=s=>decodeURIComponent(s);
-export function createQueryServer({client,apiKey=''}){return http.createServer(async(req,res)=>{try{if(apiKey&&req.headers.authorization!==`Bearer ${apiKey}`)return json(res,401,{error:'UNAUTHORIZED'});const u=new URL(req.url,'http://query');if(req.method==='GET'&&u.pathname==='/health')return json(res,200,{status:'ok'});let m;if(req.method==='GET'&&(m=u.pathname.match(/^\/api\/v1\/tasks\/(.+)\/timeline$/))){const urn=decode(m[1]),r=await client.queryJson(`SELECT * FROM telemetry_serving.task_timeline WHERE task_entity_urn=${sqlString(urn)} ORDER BY occurred_at LIMIT 1000`);return json(res,200,{data:r.data,data_watermark:r.data.at(-1)?.projected_at??null,projection_lag:null,completeness:'best_known',source_provenance:true});}if(req.method==='GET'&&(m=u.pathname.match(/^\/api\/v1\/tasks\/(.+)\/relations$/))){const urn=decode(m[1]),r=await client.queryJson(`SELECT * FROM telemetry_core.entity_relation_fact WHERE source_entity_urn=${sqlString(urn)} OR target_entity_urn=${sqlString(urn)} ORDER BY valid_from LIMIT 1000`);return json(res,200,{data:r.data,data_watermark:r.data.at(-1)?.created_at??null,relation_confidence:[...new Set(r.data.map(x=>x.confidence_class))]});}if(req.method==='GET'&&u.pathname==='/api/v1/topology/sdar-smpp'){const tenant=u.searchParams.get('tenantId'),where=tenant?` WHERE tenant_id=${sqlString(tenant)}`:'';const r=await client.queryJson(`SELECT * FROM telemetry_serving.sdar_smpp_execution_topology${where} ORDER BY valid_from DESC LIMIT 5000`);return json(res,200,{data:r.data,data_watermark:r.data[0]?.valid_from??null});}if(req.method==='GET'&&(m=u.pathname.match(/^\/api\/v1\/records\/([^/]+)\/(.+)$/))){const system=decode(m[1]),id=decode(m[2]),r=await client.queryJson(`SELECT fact_id,fact_hash,fact_type,source_system,source_record_id,source_record_hash,tenant_id,project_id,occurred_at,payload_json,normalizer_id,normalizer_version FROM telemetry_normalized.canonical_fact_v1 WHERE source_system=${sqlString(system)} AND source_record_id=${sqlString(id)} LIMIT 10`);return json(res,200,{data:r.data,source_provenance:true});}if(req.method==='GET'&&u.pathname==='/api/v1/data-quality/summary'){const r=await client.queryJson('SELECT rule_id,severity,count() AS count,max(detected_at) AS last_detected_at FROM telemetry_serving.telemetry_data_quality GROUP BY rule_id,severity ORDER BY severity,rule_id');return json(res,200,{data:r.data});}if(req.method==='GET'&&u.pathname==='/api/v1/projections/watermarks'){const r=await client.queryJson('SELECT * FROM telemetry_serving.projection_watermark ORDER BY projection_id,projection_version');return json(res,200,{data:r.data});}if(req.method==='GET'&&(m=u.pathname.match(/^\/api\/v1\/providers\/(.+)\/health$/))){const urn=decode(m[1]),r=await client.queryJson(`SELECT * FROM telemetry_serving.provider_current_health WHERE provider_entity_urn=${sqlString(urn)} LIMIT 1`);return json(res,200,{data:r.data,data_watermark:r.data[0]?.data_watermark??null});}if(req.method==='GET'&&(m=u.pathname.match(/^\/api\/v1\/resources\/(.+)\/state$/))){const urn=decode(m[1]),r=await client.queryJson(`SELECT * FROM telemetry_serving.resource_current_state WHERE resource_entity_urn=${sqlString(urn)} LIMIT 1`);return json(res,200,{data:r.data,data_watermark:r.data[0]?.data_watermark??null});}return json(res,404,{error:'NOT_FOUND'});}catch(error){return json(res,500,{error:error.message});}});}
+import http from 'node:http';
+import { sqlString } from './clickhouse.js';
+
+function json(res,status,value){const body=Buffer.from(JSON.stringify(value));res.writeHead(status,{'content-type':'application/json','content-length':body.length});res.end(body);}
+const decode=(value)=>decodeURIComponent(value);
+
+const EVENT_FILTERS = {
+  tenantId:'tenant_id',projectId:'project_id',providerId:'provider_id',resourceId:'resource_id',
+  taskId:'task_id',operationName:'operation_name',runtimeInstanceId:'runtime_instance_id',
+  deploymentId:'deployment_id',recordId:'source_record_id',traceId:'trace_id',
+  externalExecutionId:'external_execution_id',providerEventId:'provider_event_id',
+  recordType:'record_type',eventCategory:'event_category',deliveryClass:'delivery_class'
+};
+
+function eventSearchSql(searchParams){
+  const where=[];
+  for(const [parameter,column] of Object.entries(EVENT_FILTERS)){
+    const value=searchParams.get(parameter);
+    if(value===null)continue;
+    if(value.length===0||value.length>512)throw Object.assign(new Error(`INVALID_${parameter}`),{statusCode:400});
+    where.push(`${column}=${sqlString(value)}`);
+  }
+  for(const [parameter,operator] of [['from','>='],['to','<=']]){
+    const value=searchParams.get(parameter);
+    if(value===null)continue;
+    if(Number.isNaN(Date.parse(value)))throw Object.assign(new Error(`INVALID_${parameter}`),{statusCode:400});
+    where.push(`occurred_at${operator}parseDateTime64BestEffort(${sqlString(value)},3)`);
+  }
+  const requestedLimit=Number(searchParams.get('limit')??100);
+  if(!Number.isInteger(requestedLimit)||requestedLimit<1||requestedLimit>1000)throw Object.assign(new Error('INVALID_limit'),{statusCode:400});
+  const clause=where.length?` WHERE ${where.join(' AND ')}`:'';
+  return `SELECT * FROM telemetry_serving.provider_ops_activity${clause} ORDER BY occurred_at DESC,source_record_id DESC LIMIT ${requestedLimit}`;
+}
+
+export function createQueryServer({client,apiKey=''}){
+  return http.createServer(async(req,res)=>{
+    try{
+      if(apiKey&&req.headers.authorization!==`Bearer ${apiKey}`)return json(res,401,{error:'UNAUTHORIZED'});
+      const url=new URL(req.url,'http://query');
+      if(req.method==='GET'&&url.pathname==='/health')return json(res,200,{status:'ok'});
+      if(req.method==='GET'&&url.pathname==='/api/v1/events'){
+        const result=await client.queryJson(eventSearchSql(url.searchParams));
+        return json(res,200,{data:result.data,data_watermark:result.data[0]?.ingested_at??null,source_provenance:true});
+      }
+      let match;
+      if(req.method==='GET'&&(match=url.pathname.match(/^\/api\/v1\/tasks\/(.+)\/timeline$/))){
+        const urn=decode(match[1]);
+        const result=await client.queryJson(`SELECT * FROM telemetry_serving.task_timeline WHERE task_entity_urn=${sqlString(urn)} ORDER BY occurred_at LIMIT 1000`);
+        return json(res,200,{data:result.data,data_watermark:result.data.at(-1)?.projected_at??null,projection_lag:null,completeness:'best_known',source_provenance:true});
+      }
+      if(req.method==='GET'&&(match=url.pathname.match(/^\/api\/v1\/tasks\/(.+)\/relations$/))){
+        const urn=decode(match[1]);
+        const result=await client.queryJson(`SELECT * FROM telemetry_core.entity_relation_fact WHERE source_entity_urn=${sqlString(urn)} OR target_entity_urn=${sqlString(urn)} ORDER BY valid_from LIMIT 1000`);
+        return json(res,200,{data:result.data,data_watermark:result.data.at(-1)?.created_at??null,relation_confidence:[...new Set(result.data.map(value=>value.confidence_class))]});
+      }
+      if(req.method==='GET'&&url.pathname==='/api/v1/topology/sdar-smpp'){
+        const tenant=url.searchParams.get('tenantId');
+        const where=tenant?` WHERE tenant_id=${sqlString(tenant)}`:'';
+        const result=await client.queryJson(`SELECT * FROM telemetry_serving.sdar_smpp_execution_topology${where} ORDER BY valid_from DESC LIMIT 5000`);
+        return json(res,200,{data:result.data,data_watermark:result.data[0]?.valid_from??null});
+      }
+      if(req.method==='GET'&&(match=url.pathname.match(/^\/api\/v1\/records\/([^/]+)\/(.+)$/))){
+        const system=decode(match[1]),id=decode(match[2]);
+        const result=await client.queryJson(`SELECT fact_id,fact_hash,fact_type,source_system,source_record_id,source_record_hash,tenant_id,project_id,occurred_at,payload_json,correlation_json,normalizer_id,normalizer_version FROM telemetry_normalized.canonical_fact_v1 WHERE source_system=${sqlString(system)} AND source_record_id=${sqlString(id)} LIMIT 10`);
+        return json(res,200,{data:result.data,source_provenance:true});
+      }
+      if(req.method==='GET'&&url.pathname==='/api/v1/data-quality/summary'){
+        const result=await client.queryJson('SELECT rule_id,severity,count() AS count,max(detected_at) AS last_detected_at FROM telemetry_serving.telemetry_data_quality GROUP BY rule_id,severity ORDER BY severity,rule_id');
+        return json(res,200,{data:result.data});
+      }
+      if(req.method==='GET'&&url.pathname==='/api/v1/projections/watermarks'){
+        const result=await client.queryJson('SELECT * FROM telemetry_serving.projection_watermark ORDER BY projection_id,projection_version');
+        return json(res,200,{data:result.data});
+      }
+      if(req.method==='GET'&&(match=url.pathname.match(/^\/api\/v1\/providers\/(.+)\/health$/))){
+        const urn=decode(match[1]);const result=await client.queryJson(`SELECT * FROM telemetry_serving.provider_current_health WHERE provider_entity_urn=${sqlString(urn)} LIMIT 1`);
+        return json(res,200,{data:result.data,data_watermark:result.data[0]?.data_watermark??null});
+      }
+      if(req.method==='GET'&&(match=url.pathname.match(/^\/api\/v1\/resources\/(.+)\/state$/))){
+        const urn=decode(match[1]);const result=await client.queryJson(`SELECT * FROM telemetry_serving.resource_current_state WHERE resource_entity_urn=${sqlString(urn)} LIMIT 1`);
+        return json(res,200,{data:result.data,data_watermark:result.data[0]?.data_watermark??null});
+      }
+      return json(res,404,{error:'NOT_FOUND'});
+    }catch(error){return json(res,error.statusCode??500,{error:error.message});}
+  });
+}
diff --git a/telemetry-dashboard/query-api/test/server.test.ts b/telemetry-dashboard/query-api/test/server.test.ts
index 7c7eeab..f0d33c2 100644
--- a/telemetry-dashboard/query-api/test/server.test.ts
+++ b/telemetry-dashboard/query-api/test/server.test.ts
@@ -1 +1,2 @@
 import test from 'node:test';import assert from 'node:assert/strict';import {createQueryServer} from '../src/server.js';test('query API reads serving topology',async()=>{const client={queryJson:async sql=>({data:[{relation_id:'r1'}],sql})},server=createQueryServer({client});await new Promise(r=>server.listen(0,'127.0.0.1',r));const {port}=server.address(),res=await fetch(`http://127.0.0.1:${port}/api/v1/topology/sdar-smpp`),body=await res.json();assert.equal(res.status,200);assert.equal(body.data[0].relation_id,'r1');await new Promise(r=>server.close(r));});test('query API optional key is enforced',async()=>{const server=createQueryServer({client:{queryJson:async()=>({data:[]})},apiKey:'k'});await new Promise(r=>server.listen(0,'127.0.0.1',r));const {port}=server.address(),res=await fetch(`http://127.0.0.1:${port}/health`);assert.equal(res.status,401);await new Promise(r=>server.close(r));});
+test('event search supports bounded stable SMPP dimensions',async()=>{let sql='';const server=createQueryServer({client:{queryJson:async value=>(sql=value,{data:[]})}});await new Promise(r=>server.listen(0,'127.0.0.1',r));const {port}=server.address();const response=await fetch(`http://127.0.0.1:${port}/api/v1/events?providerId=isr.vehicle.ugv.ugv1&runtimeInstanceId=production-ugv-direct-1&traceId=${'a'.repeat(32)}&limit=25`);assert.equal(response.status,200);assert.match(sql,/provider_id='isr.vehicle.ugv.ugv1'/);assert.match(sql,/runtime_instance_id='production-ugv-direct-1'/);assert.match(sql,/trace_id=/);assert.match(sql,/LIMIT 25/);await new Promise(r=>server.close(r));});
diff --git a/telemetry-processor/Dockerfile b/telemetry-processor/Dockerfile
index 85d9e45..eec7c60 100644
--- a/telemetry-processor/Dockerfile
+++ b/telemetry-processor/Dockerfile
@@ -11,10 +11,13 @@ COPY deploy/bin ./deploy/bin
 RUN npm install --include=dev --ignore-scripts && npm run build
 
 FROM node:22-alpine
+ARG VCS_REF=development
+LABEL org.opencontainers.image.revision=$VCS_REF
 WORKDIR /app
 ENV NODE_ENV=production
 COPY --from=builder /app/dist ./dist
 COPY telemetry-schema ./telemetry-schema
 COPY contracts ./contracts
+RUN mkdir -p /var/lib/smpp-telemetry/wal && chown -R node:node /var/lib/smpp-telemetry
 USER node
 CMD ["node", "dist/telemetry-processor/src/apps/index.js"]
diff --git a/telemetry-processor/src/apps/processor.ts b/telemetry-processor/src/apps/processor.ts
index 075eeda..9ce7a46 100644
--- a/telemetry-processor/src/apps/processor.ts
+++ b/telemetry-processor/src/apps/processor.ts
@@ -1,6 +1,75 @@
 import { randomUUID } from 'node:crypto';
 import { validateEnvelope, validateTrustedIngress } from '../packages/validation/validation.js';
-export class TelemetryProcessor{
-  constructor({wal,mappings,metrics,requireCollectorId=true,allowedCollectorIds=[],walMaxBytes=10*1024*1024*1024,walRejectThreshold=0.98}){this.wal=wal;this.mappings=mappings;this.metrics=metrics;this.requireCollectorId=requireCollectorId;this.allowedCollectorIds=allowedCollectorIds;this.walMaxBytes=walMaxBytes;this.walRejectThreshold=walRejectThreshold;}
-  async collect(logRecord){this.metrics.inc('processor_received_records_total');if(this.wal.stats().totalBytes>=this.walMaxBytes*this.walRejectThreshold){this.metrics.inc('processor_retryable_total',{reason:'WAL_HIGH_WATER'});return{status:'rejected_retryable',errorCode:'WAL_HIGH_WATER'};}const ingress=validateTrustedIngress(logRecord,{requireCollectorId:this.requireCollectorId,allowedCollectorIds:this.allowedCollectorIds});if(!ingress.ok){this.metrics.inc('processor_permanent_reject_total',{reason:ingress.code});return{status:'rejected_permanent',errorCode:ingress.code};}const envelope=logRecord.body, validation=validateEnvelope(envelope,logRecord.attributes);if(!validation.ok){this.metrics.inc('processor_permanent_reject_total',{reason:validation.code});return{status:'rejected_permanent',errorCode:validation.code,message:validation.message};}const receivedAt=new Date().toISOString(), mapping=this.mappings.resolve({...ingress.context,providerId:envelope.providerId,instanceId:envelope.instanceId,receivedAt:new Date(receivedAt)});if(!mapping){this.metrics.inc('processor_permanent_reject_total',{reason:'SOURCE_UNMAPPED'});return{status:'rejected_permanent',errorCode:'SOURCE_UNMAPPED'};}const sourceSystem='smpp', classification=this.wal.classify(sourceSystem,envelope.recordId,envelope.recordHash);if(classification==='duplicate'){this.metrics.inc('processor_duplicate_total');return{status:'duplicate',recordId:envelope.recordId};}if(classification==='conflict'){await this.wal.append({kind:'conflict',sourceSystem,receiptId:randomUUID(),receivedAt,trustedContext:ingress.context,mapping,envelope,acceptedRecordHash:this.wal.acceptedHash(sourceSystem,envelope.recordId),summary:'sourceRecordId already exists with a different sourceRecordHash'});this.metrics.inc('processor_conflict_total');return{status:'conflict',recordId:envelope.recordId,errorCode:'RECORD_HASH_CONFLICT'};}const entry=await this.wal.append({kind:'accepted',sourceSystem,receiptId:randomUUID(),receivedAt,trustedContext:ingress.context,mapping,envelope});this.metrics.inc('processor_accepted_records_total');return{status:'accepted',recordId:envelope.recordId,receiptId:entry.record.receiptId,wal:{segment:entry.segment,offset:entry.offset}};}
+
+function safeSourceHint(envelope) {
+  if (!envelope || typeof envelope !== 'object' || Array.isArray(envelope)) return {};
+  const safe = {};
+  for (const key of ['schemaName','schemaVersion','recordId','recordHash','recordType','providerId','instanceId']) {
+    const value = envelope[key];
+    if (typeof value === 'string' && /^[A-Za-z0-9._:-]{1,256}$/.test(value)) safe[key] = value;
+  }
+  return safe;
+}
+
+export class TelemetryProcessor {
+  constructor({wal,mappings,metrics,requireCollectorId=true,allowedCollectorIds=[],walMaxBytes=10*1024*1024*1024,walRejectThreshold=0.98}) {
+    this.wal=wal;
+    this.mappings=mappings;
+    this.metrics=metrics;
+    this.requireCollectorId=requireCollectorId;
+    this.allowedCollectorIds=allowedCollectorIds;
+    this.walMaxBytes=walMaxBytes;
+    this.walRejectThreshold=walRejectThreshold;
+  }
+
+  async #quarantine({code,message=code,receivedAt,trustedContext,envelope,mapping=null}) {
+    const entry=await this.wal.append({
+      kind:'rejected',sourceSystem:'smpp',rejectionId:randomUUID(),receivedAt,trustedContext,
+      mapping,errorCode:code,errorSummary:String(message).slice(0,256),sourceHint:safeSourceHint(envelope)
+    });
+    this.metrics.inc('processor_quarantined_total',{reason:code});
+    return entry.record.rejectionId;
+  }
+
+  async collect(logRecord) {
+    this.metrics.inc('processor_received_records_total');
+    if(this.wal.stats().totalBytes>=this.walMaxBytes*this.walRejectThreshold){
+      this.metrics.inc('processor_retryable_total',{reason:'WAL_HIGH_WATER'});
+      return{status:'rejected_retryable',errorCode:'WAL_HIGH_WATER'};
+    }
+    const ingress=validateTrustedIngress(logRecord,{requireCollectorId:this.requireCollectorId,allowedCollectorIds:this.allowedCollectorIds});
+    if(!ingress.ok){
+      this.metrics.inc('processor_permanent_reject_total',{reason:ingress.code});
+      return{status:'rejected_permanent',errorCode:ingress.code};
+    }
+    const envelope=logRecord.body;
+    const receivedAt=new Date().toISOString();
+    const validation=validateEnvelope(envelope,logRecord.attributes);
+    if(!validation.ok){
+      const mapping=this.mappings.resolve({...ingress.context,providerId:typeof envelope?.providerId==='string'?envelope.providerId:'',instanceId:typeof envelope?.instanceId==='string'?envelope.instanceId:'',receivedAt:new Date(receivedAt)});
+      const rejectionId=await this.#quarantine({code:validation.code,message:validation.message,receivedAt,trustedContext:ingress.context,envelope,mapping});
+      this.metrics.inc('processor_permanent_reject_total',{reason:validation.code});
+      return{status:'rejected_permanent',errorCode:validation.code,message:validation.message,rejectionId};
+    }
+    const mapping=this.mappings.resolve({...ingress.context,providerId:envelope.providerId,instanceId:envelope.instanceId,receivedAt:new Date(receivedAt)});
+    if(!mapping){
+      const rejectionId=await this.#quarantine({code:'SOURCE_UNMAPPED',receivedAt,trustedContext:ingress.context,envelope});
+      this.metrics.inc('processor_permanent_reject_total',{reason:'SOURCE_UNMAPPED'});
+      return{status:'rejected_permanent',errorCode:'SOURCE_UNMAPPED',rejectionId};
+    }
+    const sourceSystem='smpp';
+    const classification=this.wal.classify(sourceSystem,envelope.recordId,envelope.recordHash);
+    if(classification==='duplicate'){
+      this.metrics.inc('processor_duplicate_total');
+      return{status:'duplicate',recordId:envelope.recordId};
+    }
+    if(classification==='conflict'){
+      await this.wal.append({kind:'conflict',sourceSystem,receiptId:randomUUID(),receivedAt,trustedContext:ingress.context,mapping,envelope,acceptedRecordHash:this.wal.acceptedHash(sourceSystem,envelope.recordId),summary:'sourceRecordId already exists with a different sourceRecordHash'});
+      this.metrics.inc('processor_conflict_total');
+      return{status:'conflict',recordId:envelope.recordId,errorCode:'RECORD_HASH_CONFLICT'};
+    }
+    const entry=await this.wal.append({kind:'accepted',sourceSystem,receiptId:randomUUID(),receivedAt,trustedContext:ingress.context,mapping,envelope});
+    this.metrics.inc('processor_accepted_records_total');
+    return{status:'accepted',recordId:envelope.recordId,receiptId:entry.record.receiptId,wal:{segment:entry.segment,offset:entry.offset}};
+  }
 }
diff --git a/telemetry-processor/src/packages/exporters/clickhouse.ts b/telemetry-processor/src/packages/exporters/clickhouse.ts
index 80005e2..7a0b1f3 100644
--- a/telemetry-processor/src/packages/exporters/clickhouse.ts
+++ b/telemetry-processor/src/packages/exporters/clickhouse.ts
@@ -6,7 +6,36 @@ export class ClickHouseClient{
   #headers(contentType='text/plain'){const h={'content-type':contentType};if(this.user)h.authorization=`Basic ${Buffer.from(`${this.user}:${this.password}`).toString('base64')}`;return h;}
   async query(sql,body=''){const response=await fetch(`${this.url}/?query=${encodeURIComponent(sql)}`,{method:'POST',headers:this.#headers(body?'application/x-ndjson':'text/plain'),body});if(!response.ok)throw new Error(`CLICKHOUSE_${response.status}:${(await response.text()).slice(0,500)}`);return response.text();}
   async ping(){await this.query('SELECT 1');return true;}
-  async insert(table,rows){if(!rows.length)return;const body=`${rows.map(r=>JSON.stringify(r)).join('\n')}\n`;await this.query(`INSERT INTO ${table} FORMAT JSONEachRow`,body);}
+  async insert(table,rows){if(!rows.length)return;const body=`${rows.map(r=>JSON.stringify(r)).join('\n')}\n`;await this.query(`INSERT INTO ${table} SETTINGS date_time_input_format='best_effort' FORMAT JSONEachRow`,body);}
+}
+export function landingRow(entry){
+  const {envelope,mapping,receivedAt,trustedContext}=entry.record;
+  return {
+    tenant_id:mapping.tenantId,project_id:mapping.projectId,environment:mapping.environment,
+    mapping_version:mapping.mappingVersion,policy_version:mapping.policyVersion,
+    source_system:'smpp',source_product:mapping.sourceProduct,source_record_id:envelope.recordId,
+    source_record_hash:envelope.recordHash,source_schema_name:envelope.schemaName,
+    source_schema_version:envelope.schemaVersion,record_type:envelope.recordType,
+    event_category:envelope.eventCategory,delivery_class:envelope.deliveryClass,
+    priority:String(envelope.priority??'P2'),runtime_version:envelope.runtimeVersion,
+    runtime_instance_id:envelope.instanceId,deployment_id:trustedContext.deploymentId,
+    collector_id:trustedContext.collectorId,provider_id:envelope.providerId,
+    provider_instance_id:envelope.instanceId,task_id:envelope.taskId??null,
+    resource_id:envelope.resourceId??null,resource_type:envelope.resourceType??null,
+    external_execution_id:envelope.externalExecutionId??null,
+    operation_name:envelope.operationName??null,provider_event_id:envelope.providerEventId??null,
+    provider_event_sequence:envelope.providerEventSequence??null,event_type:envelope.eventType??null,
+    execution_mode:envelope.executionMode??null,simulation_id:envelope.simulationId??null,
+    adapter_revision:envelope.adapterRevision==null?null:String(envelope.adapterRevision),
+    observation_revision:envelope.observationRevision??null,command_sequence:envelope.commandSequence??null,
+    correlation_id:envelope.correlationId??envelope.attributes?.correlationId??null,
+    causation_record_id:envelope.causationRecordId??envelope.attributes?.causationRecordId??null,
+    trace_id:envelope.traceId??envelope.attributes?.traceId??envelope.attributes?.correlation?.traceId??null,
+    span_id:envelope.spanId??envelope.attributes?.spanId??envelope.attributes?.correlation?.spanId??null,
+    occurred_at:envelope.occurredAt,emitted_at:envelope.emittedAt,received_at:receivedAt,
+    ingested_at:new Date().toISOString(),attributes_json:JSON.stringify(envelope.attributes??{}),
+    payload_json:JSON.stringify(envelope.payload??null),envelope_json:JSON.stringify(envelope),
+    wal_segment:entry.segment,wal_offset:entry.offset,ingest_version:Date.now()
+  };
 }
-export function landingRow(entry){const {envelope,mapping,receivedAt,trustedContext}=entry.record;return {tenant_id:mapping.tenantId,project_id:mapping.projectId,environment:mapping.environment,mapping_version:mapping.mappingVersion,policy_version:mapping.policyVersion,source_system:'smpp',source_product:mapping.sourceProduct,source_record_id:envelope.recordId,source_record_hash:envelope.recordHash,source_schema_name:'ProviderOpsEnvelope',source_schema_version:envelope.schemaVersion,record_type:envelope.recordType,priority:String(envelope.priority??'P2'),runtime_instance_id:envelope.instanceId,deployment_id:trustedContext.deploymentId,collector_id:trustedContext.collectorId,provider_id:envelope.providerId,provider_instance_id:envelope.instanceId,task_id:envelope.taskId??null,resource_id:envelope.resourceId??null,operation_name:envelope.operationName??null,correlation_id:envelope.correlationId??envelope.attributes?.correlationId??null,causation_record_id:envelope.causationRecordId??envelope.attributes?.causationRecordId??null,trace_id:envelope.attributes?.traceId??envelope.attributes?.correlation?.traceId??null,span_id:envelope.attributes?.spanId??envelope.attributes?.correlation?.spanId??null,occurred_at:envelope.occurredAt,emitted_at:envelope.emittedAt,received_at:receivedAt,ingested_at:new Date().toISOString(),attributes_json:JSON.stringify(envelope.attributes??{}),payload_json:JSON.stringify(envelope.payload??null),envelope_json:JSON.stringify(envelope),wal_segment:entry.segment,wal_offset:entry.offset,ingest_version:Date.now()};}
 export function canonicalRow(fact){return {fact_id:fact.factId,fact_hash:fact.factHash,fact_type:fact.factType,fact_version:fact.factVersion,source_system:fact.sourceSystem,source_product:fact.sourceProduct,source_record_id:fact.sourceRecordId,source_record_hash:fact.sourceRecordHash,source_schema_name:fact.sourceSchemaName,source_schema_version:fact.sourceSchemaVersion,tenant_id:fact.tenantId,project_id:fact.projectId,environment:fact.environment,source_instance_urn:fact.sourceInstanceUrn,occurred_at:fact.occurredAt,received_at:fact.receivedAt,normalized_at:fact.normalizedAt,entity_refs_json:JSON.stringify(fact.entityRefs),relations_json:JSON.stringify(fact.relations),correlation_json:JSON.stringify(fact.correlation),payload_json:JSON.stringify(fact.payload),normalizer_id:fact.provenance.normalizerId,normalizer_version:fact.provenance.normalizerVersion,mapping_version:fact.provenance.mappingVersion,policy_version:fact.provenance.policyVersion};}
diff --git a/telemetry-processor/src/packages/exporters/target-manager.ts b/telemetry-processor/src/packages/exporters/target-manager.ts
index b7e846e..3e4eeec 100644
--- a/telemetry-processor/src/packages/exporters/target-manager.ts
+++ b/telemetry-processor/src/packages/exporters/target-manager.ts
@@ -2,18 +2,92 @@ import { readFile } from 'node:fs/promises';
 import { ClickHouseClient, landingRow, canonicalRow } from './clickhouse.js';
 import { SmppProviderOpsNormalizerV1 } from '../normalization/smpp-provider-ops-v1.js';
 import { CoreProjectionV1 } from '../projection/core-projection.js';
-export async function loadProjectionTargets(file){const data=JSON.parse(await readFile(file,'utf8'));if(!Array.isArray(data.targets))throw new Error('PROJECTION_TARGETS_INVALID');return data.targets;}
-export class TargetWorker{
-  constructor({target,wal,metrics,client,normalizer=new SmppProviderOpsNormalizerV1(),projection=new CoreProjectionV1(),batchSize=200}){this.target=target;this.wal=wal;this.metrics=metrics;this.client=client;this.normalizer=normalizer;this.projection=projection;this.batchSize=batchSize;this.running=false;this.lastError=null;this.checkpointId=`target:${target.targetId}`;}
+
+export async function loadProjectionTargets(file) {
+  const data=JSON.parse(await readFile(file,'utf8'));
+  if(!Array.isArray(data.targets)) throw new Error('PROJECTION_TARGETS_INVALID');
+  return data.targets;
+}
+
+export class TargetWorker {
+  constructor({target,wal,metrics,client,normalizer=new SmppProviderOpsNormalizerV1(),projection=new CoreProjectionV1(),batchSize=200}) {
+    this.target=target;this.wal=wal;this.metrics=metrics;this.client=client;
+    this.normalizer=normalizer;this.projection=projection;this.batchSize=batchSize;
+    this.running=false;this.lastError=null;this.checkpointId=`target:${target.targetId}`;
+  }
+
   async initialize(){await this.client.initialize();}
-  async flush(){if(this.running||!this.target.enabled)return;this.running=true;try{const entries=this.wal.pending(this.checkpointId,this.batchSize);if(!entries.length){this.lastError=null;return;}const tables=new Map();const push=(table,row)=>{if(!tables.has(table))tables.set(table,[]);tables.get(table).push(row);};for(const entry of entries){const allowedRoutes=entry.record.mapping?.projectionRouteIds??[];const targetRoutes=this.target.routeIds??[this.target.targetId];if(!this.target.acceptAllMappings&&!targetRoutes.some(route=>allowedRoutes.includes(route)))continue;if(entry.record.kind==='conflict'){if(this.target.writeLayers.includes('landing'))push('telemetry_landing.smpp_provider_ops_conflict_v1',{tenant_id:entry.record.mapping.tenantId,project_id:entry.record.mapping.projectId,environment:entry.record.mapping.environment,mapping_version:entry.record.mapping.mappingVersion,source_record_id:entry.record.envelope.recordId,accepted_record_hash:entry.record.acceptedRecordHash,conflicting_record_hash:entry.record.envelope.recordHash,provider_id:entry.record.envelope.providerId,record_type:entry.record.envelope.recordType,occurred_at:entry.record.envelope.occurredAt,received_at:entry.record.receivedAt,conflict_summary:entry.record.summary,wal_segment:entry.segment,wal_offset:entry.offset});continue;}if(entry.record.kind!=='accepted')continue;if(this.target.writeLayers.includes('landing'))push('telemetry_landing.smpp_provider_ops_v1',landingRow(entry));const facts=this.normalizer.normalize(entry);for(const fact of facts){if(this.target.writeLayers.includes('normalized'))push('telemetry_normalized.canonical_fact_v1',canonicalRow(fact));if(this.target.writeLayers.includes('core')||this.target.writeLayers.includes('relation'))for(const out of this.projection.project(fact)){const isRelation=out.table.endsWith('entity_relation_fact');if((isRelation&&this.target.writeLayers.includes('relation'))||(!isRelation&&this.target.writeLayers.includes('core')))push(out.table,out.row);}}}for(const [table,rows] of tables)await this.client.insert(this.target.tableMap?.[table]??table,rows);await this.wal.commit(this.checkpointId,entries.at(-1));this.lastError=null;this.metrics.inc('projection_target_batches_total',{target:this.target.targetId});}catch(error){this.lastError=error;this.metrics.inc('projection_target_failures_total',{target:this.target.targetId});}finally{this.running=false;}}
+
+  async flush(){
+    if(this.running||!this.target.enabled)return;
+    this.running=true;
+    try{
+      const entries=this.wal.pending(this.checkpointId,this.batchSize);
+      if(!entries.length){this.lastError=null;return;}
+      const tables=new Map();
+      const push=(table,row)=>{if(!tables.has(table))tables.set(table,[]);tables.get(table).push(row);};
+      for(const entry of entries){
+        const allowedRoutes=entry.record.mapping?.projectionRouteIds??[];
+        const targetRoutes=this.target.routeIds??[this.target.targetId];
+        if(!this.target.acceptAllMappings&&!targetRoutes.some(route=>allowedRoutes.includes(route)))continue;
+        if(entry.record.kind==='rejected'){
+          if(this.target.writeLayers.includes('landing'))push('telemetry_landing.smpp_provider_ops_rejected_summary_v1',{
+            rejection_id:entry.record.rejectionId,
+            collector_id:entry.record.trustedContext.collectorId,
+            source_hint:JSON.stringify(entry.record.sourceHint),
+            error_code:entry.record.errorCode,
+            error_summary:entry.record.errorSummary,
+            received_at:entry.record.receivedAt
+          });
+          continue;
+        }
+        if(entry.record.kind==='conflict'){
+          if(this.target.writeLayers.includes('landing'))push('telemetry_landing.smpp_provider_ops_conflict_v1',{
+            tenant_id:entry.record.mapping.tenantId,project_id:entry.record.mapping.projectId,
+            environment:entry.record.mapping.environment,mapping_version:entry.record.mapping.mappingVersion,
+            source_record_id:entry.record.envelope.recordId,
+            accepted_record_hash:entry.record.acceptedRecordHash,
+            conflicting_record_hash:entry.record.envelope.recordHash,
+            provider_id:entry.record.envelope.providerId,record_type:entry.record.envelope.recordType,
+            occurred_at:entry.record.envelope.occurredAt,received_at:entry.record.receivedAt,
+            conflict_summary:entry.record.summary,wal_segment:entry.segment,wal_offset:entry.offset
+          });
+          continue;
+        }
+        if(entry.record.kind!=='accepted')continue;
+        if(this.target.writeLayers.includes('landing'))push('telemetry_landing.smpp_provider_ops_v1',landingRow(entry));
+        const facts=this.normalizer.normalize(entry);
+        for(const fact of facts){
+          if(this.target.writeLayers.includes('normalized'))push('telemetry_normalized.canonical_fact_v1',canonicalRow(fact));
+          if(this.target.writeLayers.includes('core')||this.target.writeLayers.includes('relation')){
+            for(const out of this.projection.project(fact)){
+              const isRelation=out.table.endsWith('entity_relation_fact');
+              if((isRelation&&this.target.writeLayers.includes('relation'))||(!isRelation&&this.target.writeLayers.includes('core')))push(out.table,out.row);
+            }
+          }
+        }
+      }
+      for(const [table,rows] of tables)await this.client.insert(this.target.tableMap?.[table]??table,rows);
+      await this.wal.commit(this.checkpointId,entries.at(-1));
+      this.lastError=null;
+      this.metrics.inc('projection_target_batches_total',{target:this.target.targetId});
+    }catch(error){
+      this.lastError=error;
+      this.metrics.inc('projection_target_failures_total',{target:this.target.targetId});
+    }finally{this.running=false;}
+  }
+
   status(){return {targetId:this.target.targetId,targetType:this.target.targetType,enabled:this.target.enabled,running:this.running,lastError:this.lastError?.message??null,checkpoint:this.wal.checkpoint(this.checkpointId),pending:this.wal.pending(this.checkpointId,Number.MAX_SAFE_INTEGER).length};}
 }
-export class TargetManager{
-  constructor({targets,wal,metrics,batchSize=200,clientFactory}){this.targets=targets.filter(t=>t.enabled).map(target=>new TargetWorker({target,wal,metrics,batchSize,client:(clientFactory?clientFactory(target):new ClickHouseClient(target.connection))}));this.timer=null;}
-  async initialize(){for(const t of this.targets)await t.initialize();}
+
+export class TargetManager {
+  constructor({targets,wal,metrics,batchSize=200,clientFactory}){
+    this.targets=targets.filter(target=>target.enabled).map(target=>new TargetWorker({target,wal,metrics,batchSize,client:clientFactory?clientFactory(target):new ClickHouseClient(target.connection)}));
+    this.timer=null;
+  }
+  async initialize(){for(const target of this.targets)await target.initialize();}
   start(intervalMs=1000){this.timer=setInterval(()=>void this.flush(),intervalMs);this.timer.unref();void this.flush();}
-  async flush(){await Promise.all(this.targets.map(t=>t.flush()));}
-  statuses(){return this.targets.map(t=>t.status());}
-  async pingRequired(){for(const t of this.targets.filter(x=>x.target.required)){await t.client.ping();}return true;}
+  async flush(){await Promise.all(this.targets.map(target=>target.flush()));}
+  statuses(){return this.targets.map(target=>target.status());}
+  async pingRequired(){for(const target of this.targets.filter(value=>value.target.required))await target.client.ping();return true;}
 }
diff --git a/telemetry-processor/src/packages/normalization/smpp-provider-ops-v1.ts b/telemetry-processor/src/packages/normalization/smpp-provider-ops-v1.ts
index b8e27d2..7f6a4f8 100644
--- a/telemetry-processor/src/packages/normalization/smpp-provider-ops-v1.ts
+++ b/telemetry-processor/src/packages/normalization/smpp-provider-ops-v1.ts
@@ -1,10 +1,184 @@
 import { sha256Canonical, uuidV5 } from '../canonical/canonical.js';
-export function entityUrn({tenantId,sourceSystem,deploymentId,entityType,localId}){ return `urn:telemetry:${encodeURIComponent(tenantId)}:${sourceSystem}:${encodeURIComponent(deploymentId||'unknown')}:${entityType}:${encodeURIComponent(localId)}`; }
-const array=v=>v==null?[]:(Array.isArray(v)?v:[v]);
-function correlationOf(envelope){ const a=envelope.attributes??{}, c=a.correlation??{}; return { correlationId:envelope.correlationId??c.correlationId??a.correlationId??null, causationRecordId:envelope.causationRecordId??c.causationRecordId??a.causationRecordId??null, traceId:c.traceId??a.traceId??null, spanId:c.spanId??a.spanId??null, routeId:c.routeId??a.routeId??null, attemptNo:c.attemptNo??a.attemptNo??null, originSystem:c.originSystem??a.originSystem??null, originDeploymentId:c.originDeploymentId??a.originDeploymentId??null, originRuntimeInstanceIds:[...new Set([...array(c.originRuntimeInstanceIds),...array(c.originRuntimeInstanceId),...array(a.originRuntimeInstanceIds),...array(a.originRuntimeInstanceId)])], originTaskIds:[...new Set([...array(c.originTaskIds),...array(c.originTaskId),...array(a.originTaskIds),...array(a.originTaskId)])], originInvocationIds:[...new Set([...array(c.originInvocationIds),...array(c.originInvocationId),...array(a.originInvocationIds),...array(a.originInvocationId)])] }; }
+
+function requiredIdentity(value, name) {
+  if (typeof value !== 'string' || value.length === 0) throw new Error(`${name}_REQUIRED`);
+  return value;
+}
+
+export function entityUrn({ tenantId, sourceSystem, deploymentId, entityType, localId }) {
+  return `urn:telemetry:${encodeURIComponent(requiredIdentity(tenantId, 'TENANT_ID'))}:${requiredIdentity(sourceSystem, 'SOURCE_SYSTEM')}:${encodeURIComponent(requiredIdentity(deploymentId, 'DEPLOYMENT_ID'))}:${requiredIdentity(entityType, 'ENTITY_TYPE')}:${encodeURIComponent(requiredIdentity(localId, 'LOCAL_ID'))}`;
+}
+
+const array = (value) => value == null ? [] : (Array.isArray(value) ? value : [value]);
+const uniqueStrings = (values) => [...new Set(values.filter((value) => typeof value === 'string' && value.length > 0))];
+
+function correlationOf(envelope) {
+  const attributes = envelope.attributes ?? {};
+  const correlation = attributes.correlation ?? {};
+  return {
+    correlationId: envelope.correlationId ?? correlation.correlationId ?? attributes.correlationId ?? null,
+    causationRecordId: envelope.causationRecordId ?? correlation.causationRecordId ?? attributes.causationRecordId ?? null,
+    traceId: envelope.traceId ?? correlation.traceId ?? attributes.traceId ?? null,
+    spanId: envelope.spanId ?? correlation.spanId ?? attributes.spanId ?? null,
+    routeId: correlation.routeId ?? attributes.routeId ?? null,
+    attemptNo: correlation.attemptNo ?? attributes.attemptNo ?? null,
+    originSystem: correlation.originSystem ?? attributes.originSystem ?? null,
+    originDeploymentId: correlation.originDeploymentId ?? attributes.originDeploymentId ?? null,
+    originRuntimeInstanceIds: uniqueStrings([
+      ...array(correlation.originRuntimeInstanceIds),
+      ...array(correlation.originRuntimeInstanceId),
+      ...array(attributes.originRuntimeInstanceIds),
+      ...array(attributes.originRuntimeInstanceId)
+    ]),
+    originTaskIds: uniqueStrings([
+      ...array(correlation.originTaskIds),
+      ...array(correlation.originTaskId),
+      ...array(attributes.originTaskIds),
+      ...array(attributes.originTaskId)
+    ]),
+    originInvocationIds: uniqueStrings([
+      ...array(correlation.originInvocationIds),
+      ...array(correlation.originInvocationId),
+      ...array(attributes.originInvocationIds),
+      ...array(attributes.originInvocationId)
+    ])
+  };
+}
+
+const LINEAGE_FIELDS = [
+  'providerId','runtimeVersion','instanceId','taskId','resourceId','resourceType',
+  'externalExecutionId','operationName','correlationId','traceId','spanId','providerEventId',
+  'providerEventSequence','eventType','executionMode','simulationId','argumentHash',
+  'authorizationContextHash','adapterRevision','observationRevision','commandSequence',
+  'recordType','eventCategory','deliveryClass','occurredAt','emittedAt'
+];
+
+function sourcePayload(envelope) {
+  const payload = { attributes: envelope.attributes ?? {}, payload: envelope.payload ?? null };
+  for (const key of LINEAGE_FIELDS) if (envelope[key] !== undefined) payload[key] = envelope[key];
+  return payload;
+}
+
 export class SmppProviderOpsNormalizerV1 {
-  constructor(){ this.normalizerId='smpp-provider-ops-v1'; this.normalizerVersion=1; }
-  normalize(entry){ const {envelope,mapping,receivedAt,trustedContext}=entry.record, deploymentId=trustedContext.deploymentId||envelope.instanceId, corr=correlationOf(envelope); const refs=[]; const add=(entityType,localId)=>{if(localId)refs.push({entityType,localId,urn:entityUrn({tenantId:mapping.tenantId,sourceSystem:'smpp',deploymentId,entityType,localId})});}; add('runtime',envelope.instanceId); add('provider',envelope.providerId); add('task',envelope.taskId); add('resource',envelope.resourceId); add('execution',envelope.externalExecutionId); const payload={attributes:envelope.attributes??{},payload:envelope.payload??null,providerId:envelope.providerId,instanceId:envelope.instanceId,taskId:envelope.taskId??null,resourceId:envelope.resourceId??null,externalExecutionId:envelope.externalExecutionId??null,operationName:envelope.operationName??null,recordType:envelope.recordType}; const base={canonicalEnvelopeVersion:'1.0.0',factId:uuidV5(`smpp|${envelope.recordId}|${envelope.recordType}`),factType:envelope.recordType,factVersion:'1.0.0',sourceSystem:'smpp',sourceProduct:mapping.sourceProduct,sourceRecordId:envelope.recordId,sourceRecordHash:envelope.recordHash,sourceSchemaName:'ProviderOpsEnvelope',sourceSchemaVersion:envelope.schemaVersion,tenantId:mapping.tenantId,projectId:mapping.projectId,environment:mapping.environment,sourceInstance:{deploymentId,runtimeInstanceId:envelope.instanceId,providerId:envelope.providerId,providerInstanceId:envelope.instanceId},sourceInstanceUrn:entityUrn({tenantId:mapping.tenantId,sourceSystem:'smpp',deploymentId,entityType:'runtime',localId:envelope.instanceId}),entityRefs:refs,relations:[],correlation:corr,occurredAt:envelope.occurredAt,observedAt:envelope.emittedAt,receivedAt,normalizedAt:new Date().toISOString(),payload,provenance:{normalizerId:this.normalizerId,normalizerVersion:this.normalizerVersion,mappingVersion:mapping.mappingVersion,policyVersion:mapping.policyVersion}}; const relations=this.#relations(base,corr,refs,deploymentId); const material={...base,relations}; const factHash=sha256Canonical(material); return [{...material,factHash}]; }
-  #relations(fact,corr,refs,deploymentId){ if(corr.originSystem!=='sdar')return[]; const targetTask=refs.find(r=>r.entityType==='task')?.urn, targetProvider=refs.find(r=>r.entityType==='provider')?.urn; const target=targetTask??targetProvider; if(!target)return[]; const originDep=corr.originDeploymentId||'unknown'; const rels=[]; for(const id of corr.originTaskIds){ const source=entityUrn({tenantId:fact.tenantId,sourceSystem:'sdar',deploymentId:originDep,entityType:'task',localId:id}); rels.push(this.#relation(fact,source,target,'invokes','authoritative',corr)); } for(const id of corr.originInvocationIds){ const source=entityUrn({tenantId:fact.tenantId,sourceSystem:'sdar',deploymentId:originDep,entityType:'invocation',localId:id}); rels.push(this.#relation(fact,source,target,'delegates_to','authoritative',corr)); } for(const id of corr.originRuntimeInstanceIds){ const source=entityUrn({tenantId:fact.tenantId,sourceSystem:'sdar',deploymentId:originDep,entityType:'runtime',localId:id}); const provider=targetProvider??target; rels.push(this.#relation(fact,source,provider,'served_by','authoritative',corr)); } return rels; }
-  #relation(fact,source,target,type,confidence,corr){ return {relationId:uuidV5(`${fact.factId}|${source}|${target}|${type}`),relationType:type,relationVersion:1,tenantId:fact.tenantId,projectId:fact.projectId,sourceEntityUrn:source,targetEntityUrn:target,sourceSystem:'sdar',targetSystem:'smpp',validFrom:fact.occurredAt,validTo:null,correlationId:corr.correlationId,traceId:corr.traceId,causationFactId:fact.factId,routeId:corr.routeId,attemptNo:corr.attemptNo==null?null:Number(corr.attemptNo),evidenceFactIds:[fact.factId],bindingSource:'explicit_contract',confidenceClass:confidence,createdAt:new Date().toISOString(),projectionId:'smpp-sdar-relation-v1',projectionVersion:1}; }
+  constructor() {
+    this.normalizerId = 'smpp-provider-ops-v1';
+    this.normalizerVersion = 2;
+  }
+
+  normalize(entry) {
+    const { envelope, mapping, receivedAt, trustedContext } = entry.record;
+    const deploymentId = requiredIdentity(trustedContext.deploymentId, 'DEPLOYMENT_ID');
+    const correlation = correlationOf(envelope);
+    const refs = [];
+    const add = (entityType, localId) => {
+      if (typeof localId !== 'string' || localId.length === 0) return;
+      refs.push({
+        entityType,
+        localId,
+        urn: entityUrn({ tenantId: mapping.tenantId, sourceSystem: 'smpp', deploymentId, entityType, localId })
+      });
+    };
+    add('runtime', envelope.instanceId);
+    add('provider', envelope.providerId);
+    add('task', envelope.taskId);
+    add('resource', envelope.resourceId);
+    add('execution', envelope.externalExecutionId);
+
+    const base = {
+      canonicalEnvelopeVersion: '1.0.0',
+      factId: uuidV5(`smpp|${envelope.recordId}|${envelope.recordType}`),
+      factType: envelope.recordType,
+      factVersion: '1.0.0',
+      sourceSystem: 'smpp',
+      sourceProduct: mapping.sourceProduct,
+      sourceRecordId: envelope.recordId,
+      sourceRecordHash: envelope.recordHash,
+      sourceSchemaName: envelope.schemaName,
+      sourceSchemaVersion: envelope.schemaVersion,
+      tenantId: mapping.tenantId,
+      projectId: mapping.projectId,
+      environment: mapping.environment,
+      sourceInstance: {
+        deploymentId,
+        runtimeInstanceId: envelope.instanceId,
+        providerId: envelope.providerId,
+        runtimeVersion: envelope.runtimeVersion
+      },
+      sourceInstanceUrn: entityUrn({
+        tenantId: mapping.tenantId,
+        sourceSystem: 'smpp',
+        deploymentId,
+        entityType: 'runtime',
+        localId: envelope.instanceId
+      }),
+      entityRefs: refs,
+      relations: [],
+      correlation,
+      occurredAt: envelope.occurredAt,
+      observedAt: envelope.emittedAt,
+      receivedAt,
+      normalizedAt: new Date().toISOString(),
+      payload: sourcePayload(envelope),
+      provenance: {
+        normalizerId: this.normalizerId,
+        normalizerVersion: this.normalizerVersion,
+        mappingVersion: mapping.mappingVersion,
+        policyVersion: mapping.policyVersion
+      }
+    };
+    const relations = this.#relations(base, correlation, refs, deploymentId);
+    const material = { ...base, relations };
+    const { normalizedAt: _normalizedAt, ...stableHashMaterial } = material;
+    return [{ ...material, factHash: sha256Canonical(stableHashMaterial) }];
+  }
+
+  #relations(fact, correlation, refs, deploymentId) {
+    if (correlation.originSystem !== 'sdar') return [];
+    const targetTask = refs.find((ref) => ref.entityType === 'task')?.urn;
+    const targetProvider = refs.find((ref) => ref.entityType === 'provider')?.urn;
+    const target = targetTask ?? targetProvider;
+    if (!target) return [];
+    const originDeploymentId = requiredIdentity(correlation.originDeploymentId, 'ORIGIN_DEPLOYMENT_ID');
+    const relations = [];
+    for (const id of correlation.originTaskIds) {
+      const source = entityUrn({ tenantId: fact.tenantId, sourceSystem: 'sdar', deploymentId: originDeploymentId, entityType: 'task', localId: id });
+      relations.push(this.#relation(fact, source, target, 'invokes', 'authoritative', correlation));
+    }
+    for (const id of correlation.originInvocationIds) {
+      const source = entityUrn({ tenantId: fact.tenantId, sourceSystem: 'sdar', deploymentId: originDeploymentId, entityType: 'invocation', localId: id });
+      relations.push(this.#relation(fact, source, target, 'delegates_to', 'authoritative', correlation));
+    }
+    for (const id of correlation.originRuntimeInstanceIds) {
+      const source = entityUrn({ tenantId: fact.tenantId, sourceSystem: 'sdar', deploymentId: originDeploymentId, entityType: 'runtime', localId: id });
+      relations.push(this.#relation(fact, source, targetProvider ?? target, 'served_by', 'authoritative', correlation));
+    }
+    return relations;
+  }
+
+  #relation(fact, source, target, type, confidence, correlation) {
+    return {
+      relationId: uuidV5(`${fact.factId}|${source}|${target}|${type}`),
+      relationType: type,
+      relationVersion: 1,
+      tenantId: fact.tenantId,
+      projectId: fact.projectId,
+      sourceEntityUrn: source,
+      targetEntityUrn: target,
+      sourceSystem: 'sdar',
+      targetSystem: 'smpp',
+      validFrom: fact.occurredAt,
+      validTo: null,
+      correlationId: correlation.correlationId,
+      traceId: correlation.traceId,
+      causationFactId: fact.factId,
+      routeId: correlation.routeId,
+      attemptNo: correlation.attemptNo == null ? null : Number(correlation.attemptNo),
+      evidenceFactIds: [fact.factId],
+      bindingSource: 'explicit_contract',
+      confidenceClass: confidence,
+      createdAt: fact.receivedAt,
+      projectionId: 'smpp-sdar-relation-v1',
+      projectionVersion: 1
+    };
+  }
 }
diff --git a/telemetry-processor/src/packages/projection/core-projection.ts b/telemetry-processor/src/packages/projection/core-projection.ts
index 55ced24..8a1e462 100644
--- a/telemetry-processor/src/packages/projection/core-projection.ts
+++ b/telemetry-processor/src/packages/projection/core-projection.ts
@@ -1,5 +1,5 @@
-function common(fact, projectionId){ const primary=fact.entityRefs.find(r=>r.entityType==='task')?.urn??fact.entityRefs.find(r=>r.entityType==='resource')?.urn??fact.entityRefs.find(r=>r.entityType==='provider')?.urn??fact.sourceInstanceUrn; return {fact_id:fact.factId,fact_hash:fact.factHash,fact_type:fact.factType,source_system:fact.sourceSystem,source_product:fact.sourceProduct,source_record_id:fact.sourceRecordId,source_record_hash:fact.sourceRecordHash,tenant_id:fact.tenantId,project_id:fact.projectId,environment:fact.environment,primary_entity_urn:primary,provider_entity_urn:fact.entityRefs.find(r=>r.entityType==='provider')?.urn??'',task_entity_urn:fact.entityRefs.find(r=>r.entityType==='task')?.urn??null,resource_entity_urn:fact.entityRefs.find(r=>r.entityType==='resource')?.urn??null,operation_name:fact.payload.operationName??null,occurred_at:fact.occurredAt,payload_json:JSON.stringify(fact.payload),correlation_json:JSON.stringify(fact.correlation),source_schema_name:fact.sourceSchemaName,source_schema_version:fact.sourceSchemaVersion,normalizer_id:fact.provenance.normalizerId,normalizer_version:fact.provenance.normalizerVersion,projection_id:projectionId,projection_version:1,projected_at:new Date().toISOString()}; }
+function common(fact, projectionId){ const primary=fact.entityRefs.find(r=>r.entityType==='task')?.urn??fact.entityRefs.find(r=>r.entityType==='resource')?.urn??fact.entityRefs.find(r=>r.entityType==='provider')?.urn??fact.sourceInstanceUrn; return {fact_id:fact.factId,fact_hash:fact.factHash,fact_type:fact.factType,source_system:fact.sourceSystem,source_product:fact.sourceProduct,source_record_id:fact.sourceRecordId,source_record_hash:fact.sourceRecordHash,tenant_id:fact.tenantId,project_id:fact.projectId,environment:fact.environment,primary_entity_urn:primary,provider_entity_urn:fact.entityRefs.find(r=>r.entityType==='provider')?.urn??'',task_entity_urn:fact.entityRefs.find(r=>r.entityType==='task')?.urn??null,resource_entity_urn:fact.entityRefs.find(r=>r.entityType==='resource')?.urn??null,runtime_instance_id:fact.sourceInstance.runtimeInstanceId,deployment_id:fact.sourceInstance.deploymentId,external_execution_id:fact.payload.externalExecutionId??null,provider_event_id:fact.payload.providerEventId??null,trace_id:fact.correlation.traceId,span_id:fact.correlation.spanId,operation_name:fact.payload.operationName??null,occurred_at:fact.occurredAt,payload_json:JSON.stringify(fact.payload),correlation_json:JSON.stringify(fact.correlation),source_schema_name:fact.sourceSchemaName,source_schema_version:fact.sourceSchemaVersion,normalizer_id:fact.provenance.normalizerId,normalizer_version:fact.provenance.normalizerVersion,projection_id:projectionId,projection_version:1,projected_at:new Date().toISOString()}; }
 const specialized={
-  'provider.task.lifecycle':'task_lifecycle_fact','provider.command.lifecycle':'command_lifecycle_fact','provider.scheduler.decision':'decision_fact','provider.authorization.decision':'decision_fact','provider.recovery.lifecycle':'recovery_fact','provider.ttl.lifecycle':'recovery_fact','resource.state':'resource_state_fact','resource.health':'resource_health_fact','execution.progress':'execution_progress_fact','resource.metric':'measurement_fact','resource.measurement.fact':'measurement_fact','adapter.rpc':'provider_operation_fact','provider.configuration.changed':'provider_operation_fact'
+  'provider.task.lifecycle':'task_lifecycle_fact','provider.command.lifecycle':'command_lifecycle_fact','provider.scheduler.decision':'decision_fact','provider.recovery.lifecycle':'recovery_fact','provider.ttl.lifecycle':'recovery_fact','provider.resource.state':'resource_state_fact','provider.resource.health':'resource_health_fact','provider.execution.progress':'execution_progress_fact','provider.resource.metric':'measurement_fact'
 };
 export class CoreProjectionV1 { constructor(){this.projectionId='core-smpp-v1';this.projectionVersion=1;} project(fact){ const rows=[{table:'telemetry_core.provider_operation_fact',row:common(fact,this.projectionId)}]; const table=specialized[fact.factType]; if(table&&table!=='provider_operation_fact')rows.push({table:`telemetry_core.${table}`,row:common(fact,this.projectionId)}); for(const relation of fact.relations??[]) rows.push({table:'telemetry_core.entity_relation_fact',row:{relation_id:relation.relationId,relation_type:relation.relationType,relation_version:relation.relationVersion,tenant_id:relation.tenantId,project_id:relation.projectId,source_entity_urn:relation.sourceEntityUrn,target_entity_urn:relation.targetEntityUrn,source_system:relation.sourceSystem,target_system:relation.targetSystem,valid_from:relation.validFrom,valid_to:relation.validTo,correlation_id:relation.correlationId,trace_id:relation.traceId,causation_fact_id:relation.causationFactId,route_id:relation.routeId,attempt_no:relation.attemptNo,evidence_fact_ids:relation.evidenceFactIds,binding_source:relation.bindingSource,confidence_class:relation.confidenceClass,created_at:relation.createdAt,projection_id:relation.projectionId,projection_version:relation.projectionVersion}}); return rows; } }
diff --git a/telemetry-processor/src/packages/source-mapping/source-mapping.ts b/telemetry-processor/src/packages/source-mapping/source-mapping.ts
index 47a1f07..d82e88a 100644
--- a/telemetry-processor/src/packages/source-mapping/source-mapping.ts
+++ b/telemetry-processor/src/packages/source-mapping/source-mapping.ts
@@ -1,8 +1,8 @@
 import { readFile } from 'node:fs/promises';
 const match=(expected,actual)=>expected==='*'||expected===actual;
 export class SourceMappings {
-  constructor(file){ this.file=file; this.version=0; this.mappings=[]; }
-  async load(){ const data=JSON.parse(await readFile(this.file,'utf8')); if(!Array.isArray(data.mappings)) throw new Error('SOURCE_MAPPINGS_INVALID'); this.version=Number(data.version??1); this.mappings=data.mappings; }
+  constructor(file,{allowWildcards=false}={}){ this.file=file; this.allowWildcards=allowWildcards; this.version=0; this.mappings=[]; }
+  async load(){ const data=JSON.parse(await readFile(this.file,'utf8')); if(!Array.isArray(data.mappings)) throw new Error('SOURCE_MAPPINGS_INVALID'); if(!this.allowWildcards&&data.mappings.some(item=>[item.collectorId,item.trustDomain,item.deploymentId,item.providerId,item.instanceId].some(value=>value==='*')))throw new Error('SOURCE_MAPPING_WILDCARD_FORBIDDEN'); this.version=Number(data.version??1); this.mappings=data.mappings; }
   resolve({ collectorId='', trustDomain='', deploymentId='', providerId, instanceId, receivedAt=new Date() }){
     const when=receivedAt.getTime();
     const mapping=this.mappings.find(item=>item.status==='active'&&match(item.collectorId??'*',collectorId)&&match(item.trustDomain??'*',trustDomain)&&match(item.deploymentId??'*',deploymentId)&&match(item.providerId,providerId)&&match(item.instanceId??'*',instanceId)&&Date.parse(item.validFrom??'1970-01-01T00:00:00Z')<=when&&(item.validTo==null||Date.parse(item.validTo)>when));
diff --git a/telemetry-processor/src/packages/validation/validation.ts b/telemetry-processor/src/packages/validation/validation.ts
index 798b72c..c19ca0f 100644
--- a/telemetry-processor/src/packages/validation/validation.ts
+++ b/telemetry-processor/src/packages/validation/validation.ts
@@ -2,12 +2,23 @@ import { calculateProviderOpsRecordHash } from '../canonical/canonical.js';
 const UUID = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
 const HASH = /^[a-f0-9]{64}$/;
 export const ALLOWED_TYPES = new Set([
-  'provider.task.lifecycle','provider.command.lifecycle','adapter.rpc','provider.scheduler.decision',
-  'provider.recovery.lifecycle','provider.ttl.lifecycle','provider.authorization.decision',
-  'provider.configuration.changed','resource.state','resource.metric','resource.health',
-  'execution.progress','resource.measurement.fact'
+  'provider.task.lifecycle','provider.command.lifecycle','provider.scheduler.decision',
+  'provider.recovery.lifecycle','provider.ttl.lifecycle','provider.resource.state',
+  'provider.resource.metric','provider.resource.health','provider.execution.progress',
+  'provider.business_event.source.lifecycle','provider.business_event.ingest.lifecycle',
+  'provider.business_event.publication.lifecycle','provider.business_event.stream.lifecycle',
+  'provider.business_event.continuity','provider.business_event.delivery.lifecycle',
+  'provider.business_event.relation.lifecycle'
 ]);
-const FORBIDDEN_KEY = /(authorization(?!ContextHash)|cookie|password|api.?key|secret|private.?key|database.?url|connection.?string|stack|cause|raw.?input|raw.?answer)/i;
+export const ALLOWED_EVENT_CATEGORIES = new Set([
+  'task.lifecycle','command.lifecycle','command.dispatch','scheduler.decision',
+  'recovery.lifecycle','ttl.lifecycle','resource.state','resource.metric','resource.health',
+  'execution.progress','business_event.lifecycle'
+]);
+export const ALLOWED_DELIVERY_CLASSES = new Set(['audit','operational']);
+const TRACE_ID = /^[a-f0-9]{32}$/;
+const SPAN_ID = /^[a-f0-9]{16}$/;
+const FORBIDDEN_KEY = /(authorization(?!ContextHash)|cookie|password|passwd|api.?key|secret|private.?key|database.?url|connection.?string|token|jwt|stack|cause|raw.?input|raw.?answer|adapter.?payload)/i;
 const FORBIDDEN_VALUE = /(-----BEGIN [A-Z ]*PRIVATE KEY-----|\bBearer\s+[A-Za-z0-9._~+\/-]+=*|(?:postgres|mysql|mongodb(?:\+srv)?):\/\/[^\s]+:[^\s]+@)/i;
 function scan(value, state, depth = 0) {
   if (depth > state.maxDepth) throw Object.assign(new Error('PAYLOAD_TOO_DEEP'), { code: 'PAYLOAD_TOO_DEEP' });
@@ -30,7 +41,11 @@ export function validateTrustedIngress(logRecord, { requireCollectorId = true, a
   const collectorId = String(attrs['telemetry.source.collector_id'] ?? '');
   if (requireCollectorId && !collectorId) return { ok:false, code:'COLLECTOR_ID_REQUIRED' };
   if (allowedCollectorIds.length && !allowedCollectorIds.includes(collectorId)) return { ok:false, code:'COLLECTOR_ID_NOT_ALLOWED' };
-  return { ok:true, context:{ collectorId, trustDomain:String(attrs['telemetry.source.trust_domain']??''), deploymentId:String(attrs['telemetry.source.deployment_id']??logRecord.resource?.['deployment.environment']??''), ingressMode:String(attrs['telemetry.ingress.mode']??'gateway') } };
+  const trustDomain=String(attrs['telemetry.source.trust_domain']??'');
+  const deploymentId=String(attrs['telemetry.source.deployment_id']??'');
+  if (!trustDomain) return { ok:false, code:'TRUST_DOMAIN_REQUIRED' };
+  if (!deploymentId) return { ok:false, code:'DEPLOYMENT_ID_REQUIRED' };
+  return { ok:true, context:{ collectorId, trustDomain, deploymentId, ingressMode:String(attrs['telemetry.ingress.mode']??'gateway') } };
 }
 export function validateEnvelope(envelope, otlpAttributes = {}, limits = {}) {
   const fail = (code, message = code) => ({ ok:false, code, message });
@@ -42,11 +57,19 @@ export function validateEnvelope(envelope, otlpAttributes = {}, limits = {}) {
   if (!UUID.test(String(envelope.recordId))) return fail('RECORD_ID_INVALID');
   if (!HASH.test(String(envelope.recordHash))) return fail('RECORD_HASH_INVALID');
   if (!ALLOWED_TYPES.has(String(envelope.recordType))) return fail('RECORD_TYPE_UNSUPPORTED');
-  if (envelope.deliveryClass !== 'durable' || envelope.eventCategory !== 'audit') return fail('DELIVERY_CLASS_INVALID');
+  if (!ALLOWED_EVENT_CATEGORIES.has(String(envelope.eventCategory))) return fail('EVENT_CATEGORY_UNSUPPORTED');
+  if (!ALLOWED_DELIVERY_CLASSES.has(String(envelope.deliveryClass))) return fail('DELIVERY_CLASS_UNSUPPORTED');
   if (!envelope.providerId || !envelope.instanceId) return fail('SOURCE_IDENTITY_MISSING');
+  if (envelope.traceId !== undefined && !TRACE_ID.test(String(envelope.traceId))) return fail('TRACE_ID_INVALID');
+  if (envelope.spanId !== undefined && !SPAN_ID.test(String(envelope.spanId))) return fail('SPAN_ID_INVALID');
   if (Number.isNaN(Date.parse(envelope.occurredAt)) || Number.isNaN(Date.parse(envelope.emittedAt))) return fail('TIMESTAMP_INVALID');
-  if (otlpAttributes['sdar.record.id'] && otlpAttributes['sdar.record.id'] !== envelope.recordId) return fail('OTLP_RECORD_ID_MISMATCH');
-  if (otlpAttributes['sdar.record.hash'] && otlpAttributes['sdar.record.hash'] !== envelope.recordHash) return fail('OTLP_RECORD_HASH_MISMATCH');
+  for (const key of ['sdar.schema.name','sdar.schema.version','sdar.record.id','sdar.record.hash']) {
+    if (otlpAttributes[key] === undefined || otlpAttributes[key] === '') return fail('OTLP_CONTRACT_ATTRIBUTE_MISSING',key);
+  }
+  if (otlpAttributes['sdar.schema.name'] !== envelope.schemaName) return fail('OTLP_SCHEMA_NAME_MISMATCH');
+  if (otlpAttributes['sdar.schema.version'] !== envelope.schemaVersion) return fail('OTLP_SCHEMA_VERSION_MISMATCH');
+  if (otlpAttributes['sdar.record.id'] !== envelope.recordId) return fail('OTLP_RECORD_ID_MISMATCH');
+  if (otlpAttributes['sdar.record.hash'] !== envelope.recordHash) return fail('OTLP_RECORD_HASH_MISMATCH');
   if (otlpAttributes['telemetry.contract.version'] && otlpAttributes['telemetry.contract.version'] !== envelope.schemaVersion) return fail('OTLP_SCHEMA_VERSION_MISMATCH');
   if (calculateProviderOpsRecordHash(envelope) !== envelope.recordHash) return fail('RECORD_HASH_MISMATCH');
   try { scan(envelope,{nodes:0,maxDepth:limits.maxDepth??12,maxNodes:limits.maxNodes??5000,maxStringLength:limits.maxStringLength??16384,maxArrayLength:limits.maxArrayLength??1000,maxObjectProperties:limits.maxObjectProperties??500}); }
diff --git a/telemetry-processor/test/helpers.ts b/telemetry-processor/test/helpers.ts
index 341b03c..892045f 100644
--- a/telemetry-processor/test/helpers.ts
+++ b/telemetry-processor/test/helpers.ts
@@ -1,4 +1,4 @@
 import {calculateProviderOpsRecordHash} from '../src/packages/canonical/canonical.js';
-export function envelope(overrides={}){const e={schemaName:'sdar.provider.ops.event',schemaVersion:'1.1.0',recordId:'d56fda7f-f41a-5f54-96c8-0ee0edcb7de5',recordHash:'',recordType:'provider.task.lifecycle',eventCategory:'audit',deliveryClass:'durable',providerId:'warehouse-provider',runtimeVersion:'2.0.0-rc.1',instanceId:'runtime-replica-1',taskId:'task-smpp-1',operationName:'inventory.recount',occurredAt:'2026-07-18T03:12:10.000Z',emittedAt:'2026-07-18T03:12:10.120Z',attributes:{terminal:false},payload:{previousState:'queued',currentState:'working'},...overrides};e.recordHash=calculateProviderOpsRecordHash(e);return e;}
-export function logRecord(e=envelope(),extra={}){return{body:e,attributes:{'sdar.record.id':e.recordId,'sdar.record.hash':e.recordHash,'telemetry.channel':'smpp.provider_ops','telemetry.contract.version':'1.1.0',...(extra.attributes??{})},resource:{'telemetry.source.system':'smpp','telemetry.source.collector_id':'smpp-gateway-1','telemetry.source.trust_domain':'local-compose','telemetry.source.deployment_id':'development',...(extra.resource??{})}};}
+export function envelope(overrides={}){const e={schemaName:'sdar.provider.ops.event',schemaVersion:'1.1.0',recordId:'d56fda7f-f41a-5f54-96c8-0ee0edcb7de5',recordHash:'',recordType:'provider.task.lifecycle',eventCategory:'task.lifecycle',deliveryClass:'audit',providerId:'warehouse-provider',runtimeVersion:'2.0.0-rc.1',instanceId:'runtime-replica-1',taskId:'task-smpp-1',operationName:'inventory.recount',occurredAt:'2026-07-18T03:12:10.000Z',emittedAt:'2026-07-18T03:12:10.120Z',attributes:{terminal:false},payload:{previousState:'queued',currentState:'working'},...overrides};e.recordHash=calculateProviderOpsRecordHash(e);return e;}
+export function logRecord(e=envelope(),extra={}){return{body:e,attributes:{'sdar.schema.name':e.schemaName,'sdar.schema.version':e.schemaVersion,'sdar.record.id':e.recordId,'sdar.record.hash':e.recordHash,'telemetry.channel':'smpp.provider_ops','telemetry.contract.version':'1.1.0',...(extra.attributes??{})},resource:{'telemetry.source.system':'smpp','telemetry.source.collector_id':'smpp-gateway-1','telemetry.source.trust_domain':'local-compose','telemetry.source.deployment_id':'development',...(extra.resource??{})}};}
 export const mapping={tenantId:'t1',projectId:'p1',environment:'test',mappingVersion:2,policyVersion:1,sourceProduct:'sdar-mcp-provider-platform',projectionRouteIds:['standalone-smpp']};
diff --git a/telemetry-processor/test/normalization.test.ts b/telemetry-processor/test/normalization.test.ts
index e982e5f..6d82e4a 100644
--- a/telemetry-processor/test/normalization.test.ts
+++ b/telemetry-processor/test/normalization.test.ts
@@ -1 +1 @@
-import test from 'node:test';import assert from 'node:assert/strict';import {SmppProviderOpsNormalizerV1} from '../src/packages/normalization/smpp-provider-ops-v1.js';import {calculateProviderOpsRecordHash} from '../src/packages/canonical/canonical.js';import {envelope,mapping} from './helpers.js';function entry(e){return{record:{envelope:e,mapping,receivedAt:'2026-07-18T03:12:11Z',trustedContext:{deploymentId:'smpp-d1',collectorId:'c1'}}};}test('normalizer emits canonical fact and source-neutral provenance',()=>{const f=new SmppProviderOpsNormalizerV1().normalize(entry(envelope()))[0];assert.equal(f.sourceSystem,'smpp');assert.equal(f.factHash.length,64);assert.match(f.sourceInstanceUrn,/urn:telemetry:/);});test('2 SDAR x 2 SMPP relations remain distinct N:N facts',()=>{const n=new SmppProviderOpsNormalizerV1(),rels=[];for(const [i,smppTask] of ['p1','p2'].entries()){const e=envelope({recordId:i===0?'d56fda7f-f41a-5f54-96c8-0ee0edcb7de5':'b56fda7f-f41a-5f54-96c8-0ee0edcb7de5',taskId:smppTask,attributes:{correlation:{originSystem:'sdar',originDeploymentId:'sdar-d1',originTaskIds:['s1','s2']}}});e.recordHash=calculateProviderOpsRecordHash(e);rels.push(...n.normalize(entry(e))[0].relations);}assert.equal(rels.length,4);assert.equal(new Set(rels.map(r=>r.relationId)).size,4);assert.equal(new Set(rels.map(r=>`${r.sourceEntityUrn}->${r.targetEntityUrn}`)).size,4);});
+import test from 'node:test';import assert from 'node:assert/strict';import {SmppProviderOpsNormalizerV1} from '../src/packages/normalization/smpp-provider-ops-v1.js';import {calculateProviderOpsRecordHash} from '../src/packages/canonical/canonical.js';import {envelope,mapping} from './helpers.js';function entry(e){return{record:{envelope:e,mapping,receivedAt:'2026-07-18T03:12:11Z',trustedContext:{deploymentId:'smpp-d1',collectorId:'c1'}}};}test('normalizer emits canonical fact and source-neutral provenance',()=>{const f=new SmppProviderOpsNormalizerV1().normalize(entry(envelope()))[0];assert.equal(f.sourceSystem,'smpp');assert.equal(f.factHash.length,64);assert.match(f.sourceInstanceUrn,/urn:telemetry:/);});test('2 SDAR x 2 SMPP relations remain distinct N:N facts',()=>{const n=new SmppProviderOpsNormalizerV1(),rels=[];for(const [i,smppTask] of ['p1','p2'].entries()){const e=envelope({recordId:i===0?'d56fda7f-f41a-5f54-96c8-0ee0edcb7de5':'b56fda7f-f41a-5f54-96c8-0ee0edcb7de5',taskId:smppTask,attributes:{correlation:{originSystem:'sdar',originDeploymentId:'sdar-d1',originTaskIds:['s1','s2']}}});e.recordHash=calculateProviderOpsRecordHash(e);rels.push(...n.normalize(entry(e))[0].relations);}assert.equal(rels.length,4);assert.equal(new Set(rels.map(r=>r.relationId)).size,4);assert.equal(new Set(rels.map(r=>`${r.sourceEntityUrn}->${r.targetEntityUrn}`)).size,4);});test('canonical top-level trace/span win and source lineage is preserved',()=>{const e=envelope({traceId:'1'.repeat(32),spanId:'2'.repeat(16),providerEventId:'evt-1',providerEventSequence:7,resourceId:'ugv-01',resourceType:'ugv',externalExecutionId:'execution-1',eventType:'resource.state',attributes:{traceId:'3'.repeat(32),spanId:'4'.repeat(16)}});e.recordHash=calculateProviderOpsRecordHash(e);const fact=new SmppProviderOpsNormalizerV1().normalize(entry(e))[0];assert.equal(fact.correlation.traceId,'1'.repeat(32));assert.equal(fact.correlation.spanId,'2'.repeat(16));assert.equal(fact.payload.providerEventId,'evt-1');assert.equal(fact.payload.providerEventSequence,7);assert.equal(fact.payload.externalExecutionId,'execution-1');assert.equal(fact.payload.resourceType,'ugv');});
diff --git a/telemetry-processor/test/otlp-ingest.test.ts b/telemetry-processor/test/otlp-ingest.test.ts
index 04ab3f2..5eaf6dc 100644
--- a/telemetry-processor/test/otlp-ingest.test.ts
+++ b/telemetry-processor/test/otlp-ingest.test.ts
@@ -23,6 +23,8 @@ function request(e) {
     'telemetry.source.trust_domain': 'local-compose',
     'telemetry.source.deployment_id': 'development'
   }) }, scopeLogs: [{ logRecords: [{ body: av(e), attributes: attrs({
+    'sdar.schema.name': e.schemaName,
+    'sdar.schema.version': e.schemaVersion,
     'sdar.record.id': e.recordId,
     'sdar.record.hash': e.recordHash,
     'telemetry.channel': 'smpp.provider_ops',
diff --git a/telemetry-processor/test/processor.test.ts b/telemetry-processor/test/processor.test.ts
index 4e3be66..81fa414 100644
--- a/telemetry-processor/test/processor.test.ts
+++ b/telemetry-processor/test/processor.test.ts
@@ -1 +1 @@
-import test from 'node:test';import assert from 'node:assert/strict';import {mkdtemp} from 'node:fs/promises';import {tmpdir} from 'node:os';import {join} from 'node:path';import {WalStore} from '../src/packages/wal/wal.js';import {TelemetryProcessor} from '../src/apps/processor.js';import {Metrics} from '../src/packages/metrics/metrics.js';import {envelope,logRecord,mapping} from './helpers.js';async function setup(){const wal=new WalStore({directory:await mkdtemp(join(tmpdir(),'proc-'))});await wal.initialize();const mappings={resolve:()=>mapping};return{wal,p:new TelemetryProcessor({wal,mappings,metrics:new Metrics(),allowedCollectorIds:['smpp-gateway-1']})};}test('accept, duplicate and conflict',async()=>{const {p}=await setup(),e=envelope();assert.equal((await p.collect(logRecord(e))).status,'accepted');assert.equal((await p.collect(logRecord(e))).status,'duplicate');const c=envelope({payload:{currentState:'failed'}});assert.equal((await p.collect(logRecord(c))).status,'conflict');});test('rejects forged collector identity',async()=>{const {p}=await setup();assert.equal((await p.collect(logRecord(envelope(),{resource:{'telemetry.source.collector_id':'evil'}}))).errorCode,'COLLECTOR_ID_NOT_ALLOWED');});test('rejects secrets before WAL',async()=>{const {p,wal}=await setup(),e=envelope({payload:{password:'secret'}});e.recordHash=(await import('../src/packages/canonical/canonical.js')).calculateProviderOpsRecordHash(e);assert.equal((await p.collect(logRecord(e))).errorCode,'SENSITIVE_KEY_DETECTED');assert.equal(wal.entries.length,0);});
+import test from 'node:test';import assert from 'node:assert/strict';import {mkdtemp} from 'node:fs/promises';import {tmpdir} from 'node:os';import {join} from 'node:path';import {WalStore} from '../src/packages/wal/wal.js';import {TelemetryProcessor} from '../src/apps/processor.js';import {Metrics} from '../src/packages/metrics/metrics.js';import {envelope,logRecord,mapping} from './helpers.js';async function setup(){const wal=new WalStore({directory:await mkdtemp(join(tmpdir(),'proc-'))});await wal.initialize();const mappings={resolve:()=>mapping};return{wal,p:new TelemetryProcessor({wal,mappings,metrics:new Metrics(),allowedCollectorIds:['smpp-gateway-1']})};}test('accept, duplicate and conflict',async()=>{const {p}=await setup(),e=envelope();assert.equal((await p.collect(logRecord(e))).status,'accepted');assert.equal((await p.collect(logRecord(e))).status,'duplicate');const c=envelope({payload:{currentState:'failed'}});assert.equal((await p.collect(logRecord(c))).status,'conflict');});test('rejects forged collector identity',async()=>{const {p}=await setup();assert.equal((await p.collect(logRecord(envelope(),{resource:{'telemetry.source.collector_id':'evil'}}))).errorCode,'COLLECTOR_ID_NOT_ALLOWED');});test('stores only a safe rejection summary for secret-bearing input',async()=>{const {p,wal}=await setup(),e=envelope({payload:{password:'secret-do-not-store'}});e.recordHash=(await import('../src/packages/canonical/canonical.js')).calculateProviderOpsRecordHash(e);assert.equal((await p.collect(logRecord(e))).errorCode,'SENSITIVE_KEY_DETECTED');assert.equal(wal.entries.length,1);assert.equal(wal.entries[0].record.kind,'rejected');assert.doesNotMatch(JSON.stringify(wal.entries[0].record),/secret-do-not-store/);});
diff --git a/telemetry-processor/test/source-conformance.test.ts b/telemetry-processor/test/source-conformance.test.ts
new file mode 100644
index 0000000..ad15602
--- /dev/null
+++ b/telemetry-processor/test/source-conformance.test.ts
@@ -0,0 +1,42 @@
+import test from 'node:test';
+import assert from 'node:assert/strict';
+import { readFile } from 'node:fs/promises';
+import { calculateProviderOpsRecordHash } from '../src/packages/canonical/canonical.js';
+import { ALLOWED_DELIVERY_CLASSES, ALLOWED_EVENT_CATEGORIES, ALLOWED_TYPES, validateEnvelope } from '../src/packages/validation/validation.js';
+
+const vectorPath=new URL('telemetry-schema/contracts/test-vectors/smpp-local-source-provider-resource-state-1.1.0.json',`file://${process.cwd()}/`);
+const vector=JSON.parse(await readFile(vectorPath,'utf8'));
+const sourceCapture=JSON.parse(await readFile(new URL('reports/smpp-stable-integration/SMPP_TELEMETRY_SOURCE_CAPTURE.json',`file://${process.cwd()}/`),'utf8'));
+const envelope=vector.envelope;
+const attributes={
+  'sdar.schema.name':envelope.schemaName,'sdar.schema.version':envelope.schemaVersion,
+  'sdar.record.id':envelope.recordId,'sdar.record.hash':envelope.recordHash,
+  'telemetry.contract.version':envelope.schemaVersion
+};
+
+test('local SMPP source vector has exact canonical hash parity',()=>{
+  assert.equal(calculateProviderOpsRecordHash(envelope),vector.expectedRecordHash);
+  assert.equal(vector.expectedRecordHash,'57afebfcb2fcd7b2eb7a7ea2b79f7348d57b279ebf06cb87fe0a03621440dfee');
+  assert.deepEqual(validateEnvelope(envelope,attributes),{ok:true});
+});
+
+test('schema copies remain byte-identical',async()=>{
+  const [projectCopy,warehouseCopy]=await Promise.all([
+    readFile(new URL('contracts/provider-ops-envelope.schema.json',`file://${process.cwd()}/`)),
+    readFile(new URL('telemetry-schema/contracts/smpp/provider-ops-envelope-1.1.0.schema.json',`file://${process.cwd()}/`))
+  ]);
+  assert.deepEqual(projectCopy,warehouseCopy);
+});
+
+test('validator allowlists equal the current SMPP ProviderOps source capture',()=>{
+  assert.deepEqual([...ALLOWED_TYPES],sourceCapture.recordTypes);
+  assert.deepEqual([...ALLOWED_EVENT_CATEGORIES],sourceCapture.eventCategories);
+  assert.deepEqual([...ALLOWED_DELIVERY_CLASSES],sourceCapture.deliveryClasses);
+});
+
+test('unknown schema major and OTLP/body mismatch fail closed',()=>{
+  assert.equal(validateEnvelope({...envelope,schemaVersion:'2.0.0'},attributes).code,'SCHEMA_VERSION_UNSUPPORTED');
+  assert.equal(validateEnvelope(envelope,{...attributes,'sdar.record.hash':'0'.repeat(64)}).code,'OTLP_RECORD_HASH_MISMATCH');
+  const missing={...attributes};delete missing['sdar.schema.name'];
+  assert.equal(validateEnvelope(envelope,missing).code,'OTLP_CONTRACT_ATTRIBUTE_MISSING');
+});
diff --git a/telemetry-schema/contracts/smpp/provider-ops-envelope-1.1.0.schema.json b/telemetry-schema/contracts/smpp/provider-ops-envelope-1.1.0.schema.json
index 711f2d1..dc4c860 100644
--- a/telemetry-schema/contracts/smpp/provider-ops-envelope-1.1.0.schema.json
+++ b/telemetry-schema/contracts/smpp/provider-ops-envelope-1.1.0.schema.json
@@ -24,8 +24,7 @@
       "const": "sdar.provider.ops.event"
     },
     "schemaVersion": {
-      "type": "string",
-      "pattern": "^1\\."
+      "const": "1.1.0"
     },
     "recordId": {
       "type": "string",
@@ -37,21 +36,46 @@
     },
     "recordType": {
       "type": "string",
-      "minLength": 1,
-      "maxLength": 128
+      "enum": [
+        "provider.task.lifecycle",
+        "provider.command.lifecycle",
+        "provider.scheduler.decision",
+        "provider.recovery.lifecycle",
+        "provider.ttl.lifecycle",
+        "provider.resource.state",
+        "provider.resource.metric",
+        "provider.resource.health",
+        "provider.execution.progress",
+        "provider.business_event.source.lifecycle",
+        "provider.business_event.ingest.lifecycle",
+        "provider.business_event.publication.lifecycle",
+        "provider.business_event.stream.lifecycle",
+        "provider.business_event.continuity",
+        "provider.business_event.delivery.lifecycle",
+        "provider.business_event.relation.lifecycle"
+      ]
     },
     "eventCategory": {
       "type": "string",
       "enum": [
-        "audit",
-        "diagnostic"
+        "task.lifecycle",
+        "command.lifecycle",
+        "command.dispatch",
+        "scheduler.decision",
+        "recovery.lifecycle",
+        "ttl.lifecycle",
+        "resource.state",
+        "resource.metric",
+        "resource.health",
+        "execution.progress",
+        "business_event.lifecycle"
       ]
     },
     "deliveryClass": {
       "type": "string",
       "enum": [
-        "durable",
-        "best_effort"
+        "audit",
+        "operational"
       ]
     },
     "providerId": {
@@ -75,7 +99,7 @@
     },
     "resourceId": {
       "type": "string",
-      "maxLength": 256
+      "maxLength": 512
     },
     "resourceType": {
       "type": "string",
@@ -95,11 +119,11 @@
     },
     "traceId": {
       "type": "string",
-      "maxLength": 64
+      "pattern": "^[a-f0-9]{32}$"
     },
     "spanId": {
       "type": "string",
-      "maxLength": 32
+      "pattern": "^[a-f0-9]{16}$"
     },
     "providerEventId": {
       "type": "string",
@@ -155,4 +179,4 @@
     "payload": {}
   },
   "additionalProperties": true
-}
\ No newline at end of file
+}
diff --git a/telemetry-schema/contracts/test-vectors/smpp-local-source-provider-resource-state-1.1.0.json b/telemetry-schema/contracts/test-vectors/smpp-local-source-provider-resource-state-1.1.0.json
new file mode 100644
index 0000000..41cda74
--- /dev/null
+++ b/telemetry-schema/contracts/test-vectors/smpp-local-source-provider-resource-state-1.1.0.json
@@ -0,0 +1,36 @@
+{
+  "sourceRepository": "zhouwen-giser/sdar-mcp-provider-platform",
+  "sourceBranch": "codex/goal-11-npc-tank-simulation-real-interface",
+  "sourceSha": "678521e35c83793a59e045ef9bf59b4df842962e",
+  "stableComparisonSha": "cce7ea0cb0fe1b13328a48189a76d9b1688f49f4",
+  "generator": "packages/observability/src/event-envelope.ts#createProviderOpsEnvelope",
+  "envelope": {
+    "schemaName": "sdar.provider.ops.event",
+    "schemaVersion": "1.1.0",
+    "recordId": "6eb4e972-be6e-5708-98ef-95dd5f263b96",
+    "recordType": "provider.resource.state",
+    "eventCategory": "resource.state",
+    "deliveryClass": "audit",
+    "providerId": "isr.vehicle.ugv.ugv1",
+    "runtimeVersion": "2.0.0-rc.1",
+    "instanceId": "production-ugv-direct-1",
+    "resourceId": "ugv-01",
+    "resourceType": "ugv",
+    "providerEventId": "source-vector-001",
+    "providerEventSequence": 1,
+    "eventType": "resource.state",
+    "traceId": "0123456789abcdef0123456789abcdef",
+    "spanId": "0123456789abcdef",
+    "attributes": {
+      "source": "provider_telemetry_ingress"
+    },
+    "payload": {
+      "state": "ready",
+      "reasonCode": "HEALTHY"
+    },
+    "occurredAt": "2026-08-12T08:00:00.000Z",
+    "emittedAt": "2026-08-12T08:00:00.250Z",
+    "recordHash": "57afebfcb2fcd7b2eb7a7ea2b79f7348d57b279ebf06cb87fe0a03621440dfee"
+  },
+  "expectedRecordHash": "57afebfcb2fcd7b2eb7a7ea2b79f7348d57b279ebf06cb87fe0a03621440dfee"
+}
diff --git a/telemetry-schema/migrations/003_landing.sql b/telemetry-schema/migrations/003_landing.sql
index a8f09f5..5f3203e 100644
--- a/telemetry-schema/migrations/003_landing.sql
+++ b/telemetry-schema/migrations/003_landing.sql
@@ -1,6 +1,6 @@
 CREATE TABLE IF NOT EXISTS telemetry_landing.smpp_provider_ops_v1 (
  tenant_id String,project_id String,environment LowCardinality(String),mapping_version UInt32,policy_version UInt32,source_system LowCardinality(String),source_product String,source_record_id UUID,source_record_hash FixedString(64),source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),record_type LowCardinality(String),priority LowCardinality(String),runtime_instance_id String,deployment_id String,collector_id String,provider_id String,provider_instance_id String,task_id Nullable(String),resource_id Nullable(String),operation_name Nullable(String),correlation_id Nullable(String),causation_record_id Nullable(String),trace_id Nullable(String),span_id Nullable(String),occurred_at DateTime64(3,'UTC'),emitted_at DateTime64(3,'UTC'),received_at DateTime64(3,'UTC'),ingested_at DateTime64(3,'UTC'),attributes_json String,payload_json String,envelope_json String,wal_segment UInt64,wal_offset UInt64,ingest_version UInt64
-) ENGINE=ReplacingMergeTree(ingest_version) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,source_system,source_record_id,source_record_hash) TTL occurred_at + INTERVAL 180 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_landing.smpp_provider_ops_conflict_v1 (tenant_id String,project_id String,environment LowCardinality(String),mapping_version UInt32,source_record_id UUID,accepted_record_hash FixedString(64),conflicting_record_hash FixedString(64),provider_id String,record_type LowCardinality(String),occurred_at DateTime64(3,'UTC'),received_at DateTime64(3,'UTC'),conflict_summary String,wal_segment UInt64,wal_offset UInt64) ENGINE=ReplacingMergeTree(received_at) PARTITION BY toYYYYMM(received_at) ORDER BY (tenant_id,project_id,source_record_id,accepted_record_hash,conflicting_record_hash,wal_segment,wal_offset) TTL received_at + INTERVAL 365 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_landing.smpp_provider_ops_rejected_summary_v1 (rejection_id UUID,collector_id String,source_hint String,error_code String,error_summary String,received_at DateTime64(3,'UTC')) ENGINE=MergeTree PARTITION BY toYYYYMM(received_at) ORDER BY (error_code,received_at,rejection_id) TTL received_at + INTERVAL 90 DAY DELETE;
+) ENGINE=ReplacingMergeTree(ingest_version) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,source_system,source_record_id,source_record_hash) TTL toDateTime(occurred_at) + INTERVAL 180 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_landing.smpp_provider_ops_conflict_v1 (tenant_id String,project_id String,environment LowCardinality(String),mapping_version UInt32,source_record_id UUID,accepted_record_hash FixedString(64),conflicting_record_hash FixedString(64),provider_id String,record_type LowCardinality(String),occurred_at DateTime64(3,'UTC'),received_at DateTime64(3,'UTC'),conflict_summary String,wal_segment UInt64,wal_offset UInt64) ENGINE=ReplacingMergeTree(received_at) PARTITION BY toYYYYMM(received_at) ORDER BY (tenant_id,project_id,source_record_id,accepted_record_hash,conflicting_record_hash,wal_segment,wal_offset) TTL toDateTime(received_at) + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_landing.smpp_provider_ops_rejected_summary_v1 (rejection_id UUID,collector_id String,source_hint String,error_code String,error_summary String,received_at DateTime64(3,'UTC')) ENGINE=MergeTree PARTITION BY toYYYYMM(received_at) ORDER BY (error_code,received_at,rejection_id) TTL toDateTime(received_at) + INTERVAL 90 DAY DELETE;
 CREATE VIEW IF NOT EXISTS telemetry_landing.provider_ops_raw_v1 AS SELECT tenant_id,project_id,environment,mapping_version,source_record_id AS record_id,source_record_hash AS record_hash,record_type,source_schema_name AS schema_name,source_schema_version AS schema_version,provider_id,runtime_instance_id AS instance_id,task_id,resource_id,operation_name,correlation_id,occurred_at,emitted_at,received_at,ingested_at,attributes_json,payload_json,envelope_json,wal_segment,wal_offset,ingest_version FROM telemetry_landing.smpp_provider_ops_v1;
diff --git a/telemetry-schema/migrations/005_core.sql b/telemetry-schema/migrations/005_core.sql
index 51ab230..2d3a84b 100644
--- a/telemetry-schema/migrations/005_core.sql
+++ b/telemetry-schema/migrations/005_core.sql
@@ -1,10 +1,10 @@
-CREATE TABLE IF NOT EXISTS telemetry_core.provider_operation_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL occurred_at + INTERVAL 365 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_core.task_lifecycle_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL occurred_at + INTERVAL 365 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_core.command_lifecycle_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL occurred_at + INTERVAL 365 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_core.resource_state_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL occurred_at + INTERVAL 365 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_core.resource_health_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL occurred_at + INTERVAL 365 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_core.execution_progress_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL occurred_at + INTERVAL 365 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_core.measurement_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL occurred_at + INTERVAL 365 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_core.decision_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL occurred_at + INTERVAL 365 DAY DELETE;
-CREATE TABLE IF NOT EXISTS telemetry_core.recovery_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL occurred_at + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_core.provider_operation_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL toDateTime(occurred_at) + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_core.task_lifecycle_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL toDateTime(occurred_at) + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_core.command_lifecycle_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL toDateTime(occurred_at) + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_core.resource_state_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL toDateTime(occurred_at) + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_core.resource_health_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL toDateTime(occurred_at) + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_core.execution_progress_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL toDateTime(occurred_at) + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_core.measurement_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL toDateTime(occurred_at) + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_core.decision_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL toDateTime(occurred_at) + INTERVAL 365 DAY DELETE;
+CREATE TABLE IF NOT EXISTS telemetry_core.recovery_fact (fact_id UUID,fact_hash FixedString(64),fact_type LowCardinality(String),source_system LowCardinality(String),source_product String,source_record_id String,source_record_hash FixedString(64),tenant_id String,project_id String,environment LowCardinality(String),primary_entity_urn String,provider_entity_urn String,task_entity_urn Nullable(String),resource_entity_urn Nullable(String),operation_name Nullable(String),occurred_at DateTime64(3,'UTC'),payload_json String,correlation_json String,source_schema_name LowCardinality(String),source_schema_version LowCardinality(String),normalizer_id LowCardinality(String),normalizer_version UInt32,projection_id LowCardinality(String),projection_version UInt32,projected_at DateTime64(3,'UTC')) ENGINE=ReplacingMergeTree(projected_at) PARTITION BY toYYYYMM(occurred_at) ORDER BY (tenant_id,project_id,primary_entity_urn,occurred_at,fact_id,projection_id,projection_version) TTL toDateTime(occurred_at) + INTERVAL 365 DAY DELETE;
 CREATE TABLE IF NOT EXISTS telemetry_core.entity_relation_fact (relation_id UUID,relation_type LowCardinality(String),relation_version UInt32,tenant_id String,project_id String,source_entity_urn String,target_entity_urn String,source_system LowCardinality(String),target_system LowCardinality(String),valid_from DateTime64(3,'UTC'),valid_to Nullable(DateTime64(3,'UTC')),correlation_id Nullable(String),trace_id Nullable(String),causation_fact_id Nullable(UUID),route_id Nullable(String),attempt_no Nullable(UInt32),evidence_fact_ids Array(UUID),binding_source LowCardinality(String),confidence_class LowCardinality(String),created_at DateTime64(3,'UTC'),projection_id LowCardinality(String),projection_version UInt32) ENGINE=ReplacingMergeTree(created_at) PARTITION BY toYYYYMM(valid_from) ORDER BY (tenant_id,project_id,source_entity_urn,target_entity_urn,relation_type,valid_from,relation_id);
diff --git a/telemetry-schema/migrations/006_serving.sql b/telemetry-schema/migrations/006_serving.sql
index 97a50aa..d2f6cb2 100644
--- a/telemetry-schema/migrations/006_serving.sql
+++ b/telemetry-schema/migrations/006_serving.sql
@@ -1,7 +1,7 @@
 CREATE VIEW IF NOT EXISTS telemetry_serving.provider_activity_summary AS SELECT tenant_id,project_id,provider_entity_urn,fact_type,count() AS event_count,min(occurred_at) AS first_occurred_at,max(occurred_at) AS last_occurred_at FROM telemetry_core.provider_operation_fact GROUP BY tenant_id,project_id,provider_entity_urn,fact_type;
 CREATE VIEW IF NOT EXISTS telemetry_serving.task_timeline AS SELECT tenant_id,project_id,task_entity_urn,occurred_at,fact_type,operation_name,payload_json,source_system,source_record_id,fact_id,projected_at FROM telemetry_core.task_lifecycle_fact WHERE task_entity_urn IS NOT NULL;
 CREATE VIEW IF NOT EXISTS telemetry_serving.sdar_smpp_execution_topology AS SELECT tenant_id,project_id,relation_id,relation_type,source_entity_urn,target_entity_urn,source_system,target_system,valid_from,valid_to,route_id,attempt_no,confidence_class,evidence_fact_ids FROM telemetry_core.entity_relation_fact WHERE (source_system='sdar' AND target_system='smpp') OR (source_system='smpp' AND target_system='sdar');
-CREATE VIEW IF NOT EXISTS telemetry_serving.resource_current_state AS SELECT tenant_id,project_id,resource_entity_urn,argMax(payload_json,occurred_at) AS payload_json,max(occurred_at) AS occurred_at,max(projected_at) AS data_watermark FROM telemetry_core.resource_state_fact WHERE resource_entity_urn IS NOT NULL GROUP BY tenant_id,project_id,resource_entity_urn;
-CREATE VIEW IF NOT EXISTS telemetry_serving.provider_current_health AS SELECT tenant_id,project_id,provider_entity_urn,argMax(payload_json,occurred_at) AS payload_json,max(occurred_at) AS occurred_at,max(projected_at) AS data_watermark FROM telemetry_core.resource_health_fact GROUP BY tenant_id,project_id,provider_entity_urn;
+CREATE VIEW IF NOT EXISTS telemetry_serving.resource_current_state AS SELECT fact.tenant_id,fact.project_id,fact.resource_entity_urn,argMax(fact.payload_json,fact.occurred_at) AS payload_json,max(fact.occurred_at) AS occurred_at,max(fact.projected_at) AS data_watermark FROM telemetry_core.resource_state_fact AS fact WHERE fact.resource_entity_urn IS NOT NULL GROUP BY fact.tenant_id,fact.project_id,fact.resource_entity_urn;
+CREATE VIEW IF NOT EXISTS telemetry_serving.provider_current_health AS SELECT fact.tenant_id,fact.project_id,fact.provider_entity_urn,argMax(fact.payload_json,fact.occurred_at) AS payload_json,max(fact.occurred_at) AS occurred_at,max(fact.projected_at) AS data_watermark FROM telemetry_core.resource_health_fact AS fact GROUP BY fact.tenant_id,fact.project_id,fact.provider_entity_urn;
 CREATE VIEW IF NOT EXISTS telemetry_serving.projection_watermark AS SELECT projection_id,projection_version,max(projected_at) AS data_watermark,count() AS projected_facts FROM telemetry_core.provider_operation_fact GROUP BY projection_id,projection_version;
 CREATE VIEW IF NOT EXISTS telemetry_serving.telemetry_data_quality AS SELECT 'record_hash_conflict' AS rule_id,tenant_id,project_id,toString(source_record_id) AS subject_id,received_at AS detected_at,'critical' AS severity FROM telemetry_landing.smpp_provider_ops_conflict_v1 UNION ALL SELECT 'normalization_dead_letter', '' AS tenant_id, '' AS project_id, source_record_id AS subject_id, created_at AS detected_at, 'error' AS severity FROM telemetry_normalized.normalization_dead_letter_v1;
diff --git a/telemetry-schema/migrations/007_stable_smpp_lineage.sql b/telemetry-schema/migrations/007_stable_smpp_lineage.sql
new file mode 100644
index 0000000..3f63cd4
--- /dev/null
+++ b/telemetry-schema/migrations/007_stable_smpp_lineage.sql
@@ -0,0 +1,85 @@
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS event_category LowCardinality(String) AFTER record_type;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS delivery_class LowCardinality(String) AFTER event_category;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS runtime_version String AFTER priority;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS resource_type Nullable(String) AFTER resource_id;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER resource_type;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER operation_name;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS provider_event_sequence Nullable(UInt64) AFTER provider_event_id;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS event_type Nullable(String) AFTER provider_event_sequence;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS execution_mode Nullable(String) AFTER event_type;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS simulation_id Nullable(String) AFTER execution_mode;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS adapter_revision Nullable(String) AFTER simulation_id;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS observation_revision Nullable(UInt64) AFTER adapter_revision;
+ALTER TABLE telemetry_landing.smpp_provider_ops_v1 ADD COLUMN IF NOT EXISTS command_sequence Nullable(UInt64) AFTER observation_revision;
+
+ALTER TABLE telemetry_core.provider_operation_fact ADD COLUMN IF NOT EXISTS runtime_instance_id String AFTER resource_entity_urn;
+ALTER TABLE telemetry_core.provider_operation_fact ADD COLUMN IF NOT EXISTS deployment_id String AFTER runtime_instance_id;
+ALTER TABLE telemetry_core.provider_operation_fact ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER deployment_id;
+ALTER TABLE telemetry_core.provider_operation_fact ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER external_execution_id;
+ALTER TABLE telemetry_core.provider_operation_fact ADD COLUMN IF NOT EXISTS trace_id Nullable(String) AFTER provider_event_id;
+ALTER TABLE telemetry_core.provider_operation_fact ADD COLUMN IF NOT EXISTS span_id Nullable(String) AFTER trace_id;
+
+ALTER TABLE telemetry_core.task_lifecycle_fact ADD COLUMN IF NOT EXISTS runtime_instance_id String AFTER resource_entity_urn;
+ALTER TABLE telemetry_core.task_lifecycle_fact ADD COLUMN IF NOT EXISTS deployment_id String AFTER runtime_instance_id;
+ALTER TABLE telemetry_core.task_lifecycle_fact ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER deployment_id;
+ALTER TABLE telemetry_core.task_lifecycle_fact ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER external_execution_id;
+ALTER TABLE telemetry_core.task_lifecycle_fact ADD COLUMN IF NOT EXISTS trace_id Nullable(String) AFTER provider_event_id;
+ALTER TABLE telemetry_core.task_lifecycle_fact ADD COLUMN IF NOT EXISTS span_id Nullable(String) AFTER trace_id;
+
+ALTER TABLE telemetry_core.command_lifecycle_fact ADD COLUMN IF NOT EXISTS runtime_instance_id String AFTER resource_entity_urn;
+ALTER TABLE telemetry_core.command_lifecycle_fact ADD COLUMN IF NOT EXISTS deployment_id String AFTER runtime_instance_id;
+ALTER TABLE telemetry_core.command_lifecycle_fact ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER deployment_id;
+ALTER TABLE telemetry_core.command_lifecycle_fact ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER external_execution_id;
+ALTER TABLE telemetry_core.command_lifecycle_fact ADD COLUMN IF NOT EXISTS trace_id Nullable(String) AFTER provider_event_id;
+ALTER TABLE telemetry_core.command_lifecycle_fact ADD COLUMN IF NOT EXISTS span_id Nullable(String) AFTER trace_id;
+
+ALTER TABLE telemetry_core.resource_state_fact ADD COLUMN IF NOT EXISTS runtime_instance_id String AFTER resource_entity_urn;
+ALTER TABLE telemetry_core.resource_state_fact ADD COLUMN IF NOT EXISTS deployment_id String AFTER runtime_instance_id;
+ALTER TABLE telemetry_core.resource_state_fact ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER deployment_id;
+ALTER TABLE telemetry_core.resource_state_fact ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER external_execution_id;
+ALTER TABLE telemetry_core.resource_state_fact ADD COLUMN IF NOT EXISTS trace_id Nullable(String) AFTER provider_event_id;
+ALTER TABLE telemetry_core.resource_state_fact ADD COLUMN IF NOT EXISTS span_id Nullable(String) AFTER trace_id;
+
+ALTER TABLE telemetry_core.resource_health_fact ADD COLUMN IF NOT EXISTS runtime_instance_id String AFTER resource_entity_urn;
+ALTER TABLE telemetry_core.resource_health_fact ADD COLUMN IF NOT EXISTS deployment_id String AFTER runtime_instance_id;
+ALTER TABLE telemetry_core.resource_health_fact ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER deployment_id;
+ALTER TABLE telemetry_core.resource_health_fact ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER external_execution_id;
+ALTER TABLE telemetry_core.resource_health_fact ADD COLUMN IF NOT EXISTS trace_id Nullable(String) AFTER provider_event_id;
+ALTER TABLE telemetry_core.resource_health_fact ADD COLUMN IF NOT EXISTS span_id Nullable(String) AFTER trace_id;
+
+ALTER TABLE telemetry_core.execution_progress_fact ADD COLUMN IF NOT EXISTS runtime_instance_id String AFTER resource_entity_urn;
+ALTER TABLE telemetry_core.execution_progress_fact ADD COLUMN IF NOT EXISTS deployment_id String AFTER runtime_instance_id;
+ALTER TABLE telemetry_core.execution_progress_fact ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER deployment_id;
+ALTER TABLE telemetry_core.execution_progress_fact ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER external_execution_id;
+ALTER TABLE telemetry_core.execution_progress_fact ADD COLUMN IF NOT EXISTS trace_id Nullable(String) AFTER provider_event_id;
+ALTER TABLE telemetry_core.execution_progress_fact ADD COLUMN IF NOT EXISTS span_id Nullable(String) AFTER trace_id;
+
+ALTER TABLE telemetry_core.measurement_fact ADD COLUMN IF NOT EXISTS runtime_instance_id String AFTER resource_entity_urn;
+ALTER TABLE telemetry_core.measurement_fact ADD COLUMN IF NOT EXISTS deployment_id String AFTER runtime_instance_id;
+ALTER TABLE telemetry_core.measurement_fact ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER deployment_id;
+ALTER TABLE telemetry_core.measurement_fact ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER external_execution_id;
+ALTER TABLE telemetry_core.measurement_fact ADD COLUMN IF NOT EXISTS trace_id Nullable(String) AFTER provider_event_id;
+ALTER TABLE telemetry_core.measurement_fact ADD COLUMN IF NOT EXISTS span_id Nullable(String) AFTER trace_id;
+
+ALTER TABLE telemetry_core.decision_fact ADD COLUMN IF NOT EXISTS runtime_instance_id String AFTER resource_entity_urn;
+ALTER TABLE telemetry_core.decision_fact ADD COLUMN IF NOT EXISTS deployment_id String AFTER runtime_instance_id;
+ALTER TABLE telemetry_core.decision_fact ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER deployment_id;
+ALTER TABLE telemetry_core.decision_fact ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER external_execution_id;
+ALTER TABLE telemetry_core.decision_fact ADD COLUMN IF NOT EXISTS trace_id Nullable(String) AFTER provider_event_id;
+ALTER TABLE telemetry_core.decision_fact ADD COLUMN IF NOT EXISTS span_id Nullable(String) AFTER trace_id;
+
+ALTER TABLE telemetry_core.recovery_fact ADD COLUMN IF NOT EXISTS runtime_instance_id String AFTER resource_entity_urn;
+ALTER TABLE telemetry_core.recovery_fact ADD COLUMN IF NOT EXISTS deployment_id String AFTER runtime_instance_id;
+ALTER TABLE telemetry_core.recovery_fact ADD COLUMN IF NOT EXISTS external_execution_id Nullable(String) AFTER deployment_id;
+ALTER TABLE telemetry_core.recovery_fact ADD COLUMN IF NOT EXISTS provider_event_id Nullable(String) AFTER external_execution_id;
+ALTER TABLE telemetry_core.recovery_fact ADD COLUMN IF NOT EXISTS trace_id Nullable(String) AFTER provider_event_id;
+ALTER TABLE telemetry_core.recovery_fact ADD COLUMN IF NOT EXISTS span_id Nullable(String) AFTER trace_id;
+
+CREATE VIEW IF NOT EXISTS telemetry_serving.provider_ops_activity AS
+SELECT tenant_id,project_id,environment,source_record_id,source_record_hash,record_type,
+       event_category,delivery_class,provider_id,runtime_version,runtime_instance_id,deployment_id,
+       task_id,resource_id,resource_type,external_execution_id,operation_name,provider_event_id,
+       provider_event_sequence,event_type,execution_mode,simulation_id,adapter_revision,
+       observation_revision,command_sequence,correlation_id,trace_id,span_id,occurred_at,
+       emitted_at,received_at,ingested_at
+FROM telemetry_landing.smpp_provider_ops_v1;
diff --git a/telemetry-schema/tools/deployment.test.ts b/telemetry-schema/tools/deployment.test.ts
index 377c2b5..2af7c87 100644
--- a/telemetry-schema/tools/deployment.test.ts
+++ b/telemetry-schema/tools/deployment.test.ts
@@ -4,10 +4,20 @@ import { readFile, access } from 'node:fs/promises';
 const root=new URL(`file://${process.cwd()}/`);
 test('one-click deployment assets exist and include all containers',async()=>{
   const compose=await readFile(new URL('compose.yaml',root),'utf8');
-  for(const service of ['clickhouse:','telemetry-processor:','otel-collector:','query-api:','grafana:']) assert.match(compose,new RegExp(`\\n  ${service}`));
+  for(const service of ['clickhouse:','telemetry-migrate:','telemetry-processor:','otel-collector:','query-api:','grafana:']) assert.match(compose,new RegExp(`\\n  ${service}`));
   assert.match(compose,/config\/generated\/source-mappings\.json/);
+  assert.match(compose,/service_completed_successfully/);
+  assert.match(compose,/TELEMETRY_BIND_ADDRESS/);
+  assert.doesNotMatch(compose,/ports: \["\$\{OTLP_GRPC_PORT/);
   await access(new URL('deploy.sh',root));
-  await access(new URL('docs/%E4%B8%AD%E6%96%87%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md',root));
+  await access(new URL('docs/SMPP_%E9%81%A5%E6%B5%8B%E5%B9%B3%E5%8F%B0%E4%B8%AD%E6%96%87%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md',root));
+});
+test('ClickHouse DateTime64 retention expressions are compatible with the pinned image',async()=>{
+  for(const name of ['003_landing.sql','005_core.sql']){
+    const sql=await readFile(new URL(`telemetry-schema/migrations/${name}`,root),'utf8');
+    assert.match(sql,/TTL toDateTime\(/);
+    assert.doesNotMatch(sql,/TTL (?:occurred_at|received_at) \+/);
+  }
 });
 test('projection target output includes standalone and SDAR shadow targets',async()=>{
   const cfg=JSON.parse(await readFile(new URL('config/projection-targets.example.json',root),'utf8'));
diff --git a/tools/render-source-vector-otlp.ts b/tools/render-source-vector-otlp.ts
new file mode 100644
index 0000000..32b5c17
--- /dev/null
+++ b/tools/render-source-vector-otlp.ts
@@ -0,0 +1,80 @@
+import { readFile } from 'node:fs/promises';
+import { resolve } from 'node:path';
+import { calculateProviderOpsRecordHash, uuidV5 } from '../telemetry-processor/src/packages/canonical/canonical.js';
+
+const VECTOR_PATH = resolve(
+  process.cwd(),
+  'telemetry-schema/contracts/test-vectors/smpp-local-source-provider-resource-state-1.1.0.json'
+);
+
+function anyValue(value) {
+  if (typeof value === 'string') return { stringValue: value };
+  if (typeof value === 'boolean') return { boolValue: value };
+  if (typeof value === 'number' && Number.isInteger(value)) return { intValue: String(value) };
+  if (typeof value === 'number') return { doubleValue: value };
+  if (Array.isArray(value)) return { arrayValue: { values: value.map(anyValue) } };
+  if (value && typeof value === 'object') {
+    return {
+      kvlistValue: {
+        values: Object.entries(value).map(([key, child]) => ({ key, value: anyValue(child) }))
+      }
+    };
+  }
+  return { stringValue: '' };
+}
+
+function attributes(values) {
+  return Object.entries(values).map(([key, value]) => ({ key, value: anyValue(value) }));
+}
+
+const mode = process.argv[2] ?? 'original';
+const vector = JSON.parse(await readFile(VECTOR_PATH, 'utf8'));
+const envelope = structuredClone(vector.envelope);
+
+if (mode === 'conflict') {
+  envelope.payload = { ...envelope.payload, state: 'degraded', reasonCode: 'QUALIFICATION_CONFLICT' };
+  envelope.recordHash = calculateProviderOpsRecordHash(envelope);
+} else if (mode === 'outage-recovery') {
+  envelope.recordId = uuidV5('goal-01-clickhouse-outage-recovery');
+  envelope.providerEventId = 'qualification-outage-recovery-001';
+  envelope.providerEventSequence = 2;
+  envelope.occurredAt = '2026-08-12T08:05:00.000Z';
+  envelope.emittedAt = '2026-08-12T08:05:00.250Z';
+  envelope.payload = { ...envelope.payload, state: 'ready', reasonCode: 'OUTAGE_RECOVERED' };
+  envelope.recordHash = calculateProviderOpsRecordHash(envelope);
+} else if (mode === 'malformed') {
+  delete envelope.deliveryClass;
+} else if (mode === 'oversized') {
+  envelope.payload = { diagnostic: 'x'.repeat(20_000) };
+  envelope.recordHash = calculateProviderOpsRecordHash(envelope);
+} else if (mode === 'sensitive') {
+  envelope.payload = { password: 'qualification-secret-must-not-persist' };
+  envelope.recordHash = calculateProviderOpsRecordHash(envelope);
+} else if (mode !== 'original' && mode !== 'unselected') {
+  throw new Error(`Unsupported mode: ${mode}`);
+}
+
+const contractAttributes = mode === 'unselected'
+  ? { 'sdar.schema.name': 'example.unselected', 'sdar.schema.version': '1.0.0' }
+  : {
+      'sdar.schema.name': envelope.schemaName,
+      'sdar.schema.version': envelope.schemaVersion,
+      'sdar.record.id': envelope.recordId,
+      'sdar.record.hash': envelope.recordHash
+    };
+
+const request = {
+  resourceLogs: [{
+    resource: { attributes: attributes({ 'service.name': 'sdar-mcp-tasks-provider-runtime' }) },
+    scopeLogs: [{
+      scope: { name: 'sdar.provider.ops', version: envelope.runtimeVersion },
+      logRecords: [{
+        timeUnixNano: String(BigInt(Date.parse(envelope.emittedAt)) * 1_000_000n),
+        body: anyValue(envelope),
+        attributes: attributes(contractAttributes)
+      }]
+    }]
+  }]
+};
+
+process.stdout.write(JSON.stringify(request));
diff --git a/tsconfig.json b/tsconfig.json
index be55af9..9e9f3d8 100644
--- a/tsconfig.json
+++ b/tsconfig.json
@@ -14,7 +14,8 @@
     "skipLibCheck": true,
     "strict": true,
     "noUncheckedIndexedAccess": true,
-    "exactOptionalPropertyTypes": true
+    "exactOptionalPropertyTypes": true,
+    "types": ["node"]
   },
   "include": [
     "telemetry-processor/src/**/*.ts",
