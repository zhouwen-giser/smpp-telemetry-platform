import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('core schema is source-neutral and N:N relation has no unique binding',async()=>{const core=await readFile(new URL('../migrations/005_core.sql',import.meta.url),'utf8');assert.match(core,/telemetry_core\.entity_relation_fact/);assert.match(core,/source_entity_urn String/);assert.match(core,/target_entity_urn String/);assert.doesNotMatch(core,/smpp_.*fact/);assert.doesNotMatch(core,/sdar_task_id/);assert.doesNotMatch(core,/UNIQUE/i);});test('warehouse layers are created',async()=>{const db=await readFile(new URL('../migrations/001_databases.sql',import.meta.url),'utf8');for(const name of ['telemetry_meta','telemetry_landing','telemetry_normalized','telemetry_core','telemetry_serving'])assert.match(db,new RegExp(name));});
