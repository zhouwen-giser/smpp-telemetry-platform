import {ClickHouseClient} from '../../telemetry-processor/src/packages/exporters/clickhouse.mjs';
const client=new ClickHouseClient({url:process.env.CLICKHOUSE_URL??'http://127.0.0.1:8123',user:process.env.CLICKHOUSE_USER??'default',password:process.env.CLICKHOUSE_PASSWORD??'',passwordFile:process.env.CLICKHOUSE_PASSWORD_FILE??''});await client.initialize();const exists=await client.query("EXISTS TABLE telemetry_landing.provider_ops_raw_v1 FORMAT TabSeparatedRaw");if(exists.trim()!=='1'){console.log(JSON.stringify({status:'skipped',reason:'v0.1 landing table not found'}));process.exit(0);}console.log(JSON.stringify({status:'manual_review_required',message:'Compatibility view exists. Use Processor replay against retained v0.1 envelope_json after source mapping is supplied; direct SQL backfill is intentionally disabled to preserve hash and mapping validation.'}));
