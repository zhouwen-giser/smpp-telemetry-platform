# SMPP 遥测平台一键部署

## 部署

1. 安装 Docker Engine、Docker Compose v2 和 OpenSSL。
2. 如 SMPP Runtime 在其他主机，将 `.env.example` 中的 `TELEMETRY_PUBLIC_HOST` 改为本机可达 IP；需要 mTLS 时按 `deploy/stable-smpp-integration/README.md` 准备证书。
3. 执行：

```sh
chmod +x deploy.sh
./deploy.sh
```

脚本会自动创建 `.env`、生成 ClickHouse/Processor/Grafana 密钥、生成精确来源映射、执行数据库迁移并等待服务健康。

## 端口

- OTLP/HTTP 固定发布为 `0.0.0.0:4318`，以供外部 SMPP Runtime 推送。
- OTLP/gRPC、Query API、Grafana、ClickHouse 使用 `.env` 中的 `TELEMETRY_BIND_ADDRESS`，默认只绑定 `127.0.0.1`。
- Collector 健康、内部指标和 Runtime Prometheus 投影端口只绑定 `127.0.0.1`。
- Processor 不发布到宿主机。

`0.0.0.0:4318` 会监听所有 IPv4 网卡。生产部署应使用 qualification mTLS 配置，并通过主机防火墙仅允许 SMPP Runtime 来源地址访问 TCP/4318。

## 验证与停止

```sh
./status.sh
docker compose ps
docker compose down
```

默认停止命令保留 ClickHouse、WAL 和 Grafana 数据卷。
