# SMPP 遥测平台一键部署

## 部署

1. 使用原生 Linux ARM64 服务器，并安装 Docker Engine、Docker Compose v2 和 OpenSSL。CPU 必须在 `/proc/cpuinfo` 中暴露 `crc32`。
2. 推荐至少 32 GiB 内存和 80 GiB 可用磁盘。内存有限时准备 swap，并在 `.env` 中设置 `CLICKHOUSE_BUILD_JOBS=1`。
3. 将 `.env.example` 复制为 `.env`，把 `TELEMETRY_PUBLIC_HOST` 改成 SMPP Runtime 可访问的服务器 IP/DNS；不能填写 `0.0.0.0`。需要 mTLS 时按 `deploy/stable-smpp-integration/README.md` 准备证书。
4. 执行：

```sh
chmod +x deploy.sh
./deploy.sh
```

脚本会自动生成 ClickHouse/Processor/Grafana 密钥和精确来源映射，从 ClickHouse 官方固定源码提交构建 ARM64 `armv8+crc` 镜像，原生执行二进制门禁，执行数据库迁移并等待服务健康。首次源码构建可能持续数小时；重复部署会复用 Docker 缓存。

源码固定为 ClickHouse `v25.3.14.14-lts`（提交 `84d6b30ad528e77d787ab7a2437406c1e2a5887a`），完整主仓库和递归 submodule 已打包为本地 `.tar.zst`，不包含预编译 ClickHouse 二进制。目标机编译不会访问 GitHub，只需访问 Debian 软件源和拉取其他服务的基础镜像；源码归档会在解包前执行 SHA-256 校验。

## 端口

- OTLP/HTTP 固定发布为 `0.0.0.0:4318`，以供外部 SMPP Runtime 推送。
- OTLP/gRPC、Query API、Grafana、ClickHouse 使用 `.env` 中的 `TELEMETRY_BIND_ADDRESS`，默认只绑定 `127.0.0.1`。
- Collector 健康、内部指标和 Runtime Prometheus 投影端口只绑定 `127.0.0.1`。
- Processor 不发布到宿主机。

`0.0.0.0:4318` 会监听所有 IPv4 网卡。生产部署应使用 qualification mTLS 配置，并通过主机防火墙仅允许 SMPP Runtime 来源地址访问 TCP/4318。

## 验证与停止

```sh
./status.sh
docker compose ps
docker compose down
```

默认停止命令保留 ClickHouse、WAL 和 Grafana 数据卷。

已有失败或旧版本容器时无需先清理，直接重新执行 `deploy.sh`。除非明确要永久删除全部遥测数据，否则不要执行 `docker compose down -v` 或 `reset.sh`。
