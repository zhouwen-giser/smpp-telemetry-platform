import {loadConfig,loadTls} from '../packages/config/config.mjs';import {WalStore} from '../packages/wal/wal.mjs';import {SourceMappings} from '../packages/source-mapping/source-mapping.mjs';import {Metrics} from '../packages/metrics/metrics.mjs';import {loadProjectionTargets,TargetManager} from '../packages/exporters/target-manager.mjs';import {TelemetryProcessor} from './processor.mjs';import {createServer} from './server.mjs';
const config=await loadConfig(),tlsOptions=await loadTls(config.tls),metrics=new Metrics(),wal=new WalStore({directory:config.walDir,segmentMaxBytes:config.walSegmentMaxBytes});await wal.initialize();const mappings=new SourceMappings(config.sourceMappingsFile);await mappings.load();const targetConfigs=await loadProjectionTargets(config.projectionTargetsFile),targets=new TargetManager({targets:targetConfigs,wal,metrics,batchSize:config.exportBatchSize});await targets.initialize();targets.start(config.exportIntervalMs);const processor=new TelemetryProcessor({wal,mappings,metrics,requireCollectorId:config.requireCollectorId,allowedCollectorIds:config.allowedCollectorIds,walMaxBytes:config.walMaxBytes,walRejectThreshold:config.walRejectThreshold});const server=createServer({config,tlsOptions,processor,wal,targets,metrics});server.listen(config.port,config.host,()=>console.log(JSON.stringify({level:'info',message:'telemetry_processor_started',host:config.host,port:config.port,targets:targets.statuses().map(x=>x.targetId)})));for(const signal of ['SIGINT','SIGTERM'])process.on(signal,()=>server.close(()=>process.exit(0)));
